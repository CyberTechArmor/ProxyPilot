# Harness learnings registry

The ratchet principle: **every human-caught defect becomes a template, a
rule, or a machine check** — so each failure class can only happen once.
When a human review finds a defect in generated output, triage it into one
of those three destinations and record it here (project of origin, defect,
mechanism, and what now prevents it). A learning that lives only in chat
history prevents nothing. `HARNESS-INVESTIGATION.md` holds the full
evidence trail for the project-32 batch.

| # | Origin | Defect | Mechanism | Prevented by |
| --- | --- | --- | --- | --- |
| 1 | Project 32 (Spec Ops Hub) | Shipped app had zero edit/delete capability — permanent workflow dead ends (barrier task could never unblock) | Extractor told to be literal; static mockups can't show mutation flows; nothing compensated | **Machine check + template:** `completeInventoryCrud` adds implied edit/delete/status actions (marked inferred) at approval; `lintInventory` warns on suspicious shapes; extraction prompt's MUTATION COVERAGE rule |
| 2 | Project 32 | Demo-pressed "Blocked" chip recorded as the default state and shipped that way | Inventory had no default-vs-demonstrated structure; mockups had no annotation channel | **Template + rule:** `data-demo-state` annotation contract in the mockup renderer; required `default_state` per screen; lint flags screens without one |
| 3 | Project 32 | MVP build skipped exactly the Define step that owns editability rules | `isFastBuildMode` fast path: zero-cost define segment, no floor | **Rule:** standard CRUD rules pack (`rules-pack-logic.js`) injected into every fast build as a binding floor; inventory/instruction outrank explicit deviations |
| 4 | Project 32 | Inventory actions could be silently dropped by the build (nothing diffed contract vs shipped) | No parity check between inventory and implementation | **Machine check:** action-parity finish gate — every mutation action must be surfaced in UI source (implemented or visibly badged "Not built yet"); silently-missing rejects the finish |
| 5 | Project 32 | Anomaly tripwire fired after deploy, as a note — the under-verified change was already live | Tripwire ran post-`finishCycle`, "never blocks" by design | **Machine check:** `anomalySignals` now runs before deploy and HOLDS it (flag + "review, then press Deploy"); previous deploy keeps serving |
| 6 | Project 32 | Authored acceptance scenarios were never executed — smoke ran a generic pass | Finish acceptance was prose; the `requiredIds` execution path existed but nothing fed it from the builder side | **Machine check:** finish `acceptance_ids` → checks added to `state/ui-checks.json` are hard-executed against the deployed app; an undefined id fails smoke |
| 7 | Project 32 | Diff-accurate summary rejected by the over-claim detector (brace shorthand, slash-joined lists, word runs) | Low-precision path-claim extractor | **Machine check (fixed):** brace expansion, file-list splitting, prose-run rejection, directory-prefix coverage; the exact rejected summary is the regression test |
| 8 | Projects 25–27 (earlier) | Baked-in dark/neon theme overrode per-project design specs ("geometry obeyed, color ignored") | Locked design system + "obey EXACTLY" prompt + prior-mockup style inheritance | **Template + rule:** brief-outranks-system precedence; restyle briefs strip inherited styles and render at full depth; light-first token design system v2 |
| 9 | Projects 25–27 (earlier) | Generated mockups shipped layout defects (overlapping columns, unfilled bars) that only human review caught | Design bar was prose-only | **Machine check:** `mockup-checks-logic.js` battery on every saved render (tokens-only colors, themes+toggle, data-bound bars, canonical rows, detail bands, computed AA) + `scripts/verify-mockup.mjs` runtime layout checks |
| 10 | Project 33 (Spec Ops Hub v2) | Theme/empty-state mockup sections became standalone screens; each burned a full per-screen build (a dark-mode COPY shipped as its own route with a second theme switch) | Extractor splits every section into a screen; screen plan fans out per screen | **Template + machine check:** SCREEN SECTIONS contract forbids variant sections; `mergeVariantScreens` folds "(Light)/(Dark)/(Empty)/…" into the base screen at approval AND at plan time |
| 11 | Project 33 | Anomaly tripwire held all six feature deploys as 'bugfix' cycles (false-positive storm) | Stale `state/acceptance.json` from an earlier bugfix cycle persisted in the working tree and fed `acceptanceRecord`'s kind | **Machine check (fixed):** a spec the cycle didn't write never feeds the record/tripwire kind; held deploys log the hold instead of a false "No run contract" |
| 12 | Harness Controls session (budget removal) | `modelMaxOutputTokens is not defined` at runtime — a paid design turn failed on the Notes project | Helper wired into concept.js without its import; `node --check` can't see undefined identifiers, and the sandbox can't import concept.js (better-sqlite3) so no test executed the path | **Machine check:** `mock2-import-check.test.js` — native-free static scan: any mock2 module that CALLS a listed cross-module helper must import or declare it; the original defect shape is the regression case |
| 13 | Notes project (annotate screenshot) | Opening the annotate-on-screenshot dialog crashed the backend — the operator's running 4-hour build was marked "orphaned by restart" and failed | `captureOneScreenshot` launches Playwright in the backend process; Node exits on any unhandled async rejection, and the backend had no global handlers, so one stray driver error killed the server mid-build | **Rule + machine check:** global `unhandledRejection`/`uncaughtException` log-and-keep-serving net in index.js (registered before listen); `backend-safety-net.test.js` asserts it stays; Chromium launches now run `--disable-dev-shm-usage --disable-gpu` with a 30s launch timeout |
| 14 | Notes project (annotate screenshot, root cause) | design-review.js could never LOAD: it shipped importing `costCentsForUsage` from usage-logic.js (lives in quota-logic.js) and `effectivePrice` (lives in connectors.js) — every dynamic import rejected, which crashed the backend pre-net (#13) and hung the dialog after; four rounds of timeout hardening were treating the symptom | The module was only ever imported dynamically, the sandbox can't load it (better-sqlite3 chain), and no static check verified that named imports exist in their source module | **Machine check:** `mock2-import-check.test.js` now verifies every same-dir named import resolves to a real export (template-factory files excluded — their template strings embed the generated app's own imports); the broken import is fixed at the source |
| 15 | Manual TLS certs (mode toggle) | Toggling Global TLS mode returned "certificate not found" — the toggle never changed the mode | Express matches routes in registration order; the parametric `PUT /:id` (cert rotate) was registered before the literal `PUT /tls-mode`, so `PUT /tls-certs/tls-mode` matched `/:id` with id="tls-mode", the cert lookup failed, and it 404'd | **Rule + machine check:** literal routes are registered before parametric `/:id` routes (the moved handler carries a comment saying why); `tls-mode-route-order.test.js` is a native-free source check asserting `PUT /tls-mode` precedes every `/:id` route — the original defect shape is the regression case |
| 16 | Project 36 (Idea) | Shipped app looked nothing like the approved mockup — it referenced ZERO of the approved design variables, declared 32 of its own, hand-wrote ~18KB of CSS, and dropped the dark theme so the shipped theme toggle did nothing. Every gate passed | `design.css` was rendered from an LLM-extracted JSON into a FIXED 20-value schema (10 colors, 4 fonts, 3 radii, spacing, shadow), so a mockup carrying ~35 variables across three surface levels plus a full `[data-theme="dark"]` block was flattened to 1,909 chars with no dark theme and no component CSS. The build was told to "match the tokens" and there was nothing there to match | **Template + machine check:** `renderDesignCssFromMockup` lifts the mockup's own fenced `==tokens==` block AND its component CSS verbatim into `state/design.css` (dark theme included); `checkDesignAdherence` measures approved-vs-used variables and reports in the design review; the backend-owned `design-adherence` baseline gate rides every FULL battery and reds the build on zero-adoption, a parallel palette, or a dropped dark theme |
| 17 | Project 36 (Idea) | The after-build design review produced NOTHING across three builds, and a build that was never reviewed looked exactly like one reviewed and found clean | `maybeAutoDesignReview` discarded `runDesignReview`'s return value and swallowed throws into a `console.warn`; every failure path (no connector ready, no screenshot captured, project not online) was silent, with no chat message and no log line | **Rule + machine check:** every non-trivial skip/failure now posts a system chat message naming the reason; the call site logs on both sides of the fire-and-forget so "never started" and "started and failed" are distinguishable in the server log |
| 18 | Project 36 (Idea) | The downloaded build log was unusable as review evidence — every file write, test run and exec dump stopped mid-sentence | `MAX_EVENT_CONTENT_CHARS` was a flat 8,000 while the model receives up to `MAX_TOOL_RESULT_CHARS` (200,000) per tool result, so what the build saw and what the operator could read back differed by 25x — and head-only truncation threw away the END of a dump, which is where the error message is | **Machine check:** per-kind budgets in `cycle-events-logic.js` (tool_result 64k, ai_message 32k), head+tail truncation that states how much went missing, `MOCK2_MAX_EVENT_CHARS` override clamped to a ceiling, all covered by `mock2-design-adherence.test.js` |
| 19 | Project 37 ("the last app") | The shipped app had none of the base app's features — no theme toggle, branding, legal pages, asset library, machine API keys or read-only SQL — and in places re-derived its own settings storage on top of the one already there | The build prompt never mentioned `src/platform/*`. Two prompt builders describe the shell (`buildRunnerSystemPrompt`, `buildRunnerClaudeMd`) and neither named the platform module, so the model could not use what it did not know about and had no reason to preserve it | **Rule + machine check:** a shared `PLATFORM_SECTION` (one constant, embedded verbatim in BOTH builders so they cannot drift) states what the module provides, that it is load-bearing, and how to extend it; the `platform-intact` baseline gate reds on a deleted file, a lost export, an unmounted route, a second branding/legal/api-key/assets table, or a new page that omits `theme.js` |
| 20 | Every project provisioned before a base-app change | Base-app improvements reached NEW projects only; an existing project was frozen at whatever the scaffold looked like on the day it was provisioned | `MOCK2_SCAFFOLD_VERSION` was declared in `scaffold.js` and read by NOTHING. There was no version marker in the project repo and no upgrade path at all | **Machine check:** `state/platform-version.json` is seeded with every project and compared against the current scaffold; `planBaseAppUpgrade` rewrites PLATFORM-OWNED paths only (never application code — asserted by test) and runs automatically before every non-resume build, plus a manual route |
| 21 | Project 36 → 37 | The default path from design approval to a live app — MVP build, then quick updates — ran a gate battery of literally ZERO, so the design-adherence gate added for #16 could never fire on the path where the failure actually happened | `filterGatesForBuildMode` was all-or-nothing: full got every gate, mvp and quick got `[]`. "MVP means fast" had been implemented as "MVP means unchecked" | **Machine check:** cumulative gate profiles (quick ⊂ mvp ⊂ full). No mode runs zero gates. MVP runs the look-and-act battery (design adherence, platform intact, mobile overflow, no dead controls) and skips only the slow half; quick runs typecheck + platform-intact blocking with design-adherence ADVISORY so pre-existing debt cannot wedge a one-line edit; an UNTAGGED operator gate defaults to full-only so a slow custom check never lands in a fast lane. A test asserts a freshly scaffolded app passes every gate in every profile |
| 22 | Project 37 | "The mockup preview didn't work" — with no error anywhere to say why | The preview route has three failure paths (project offline, no mockup, file unreadable) and all three answered with JSON. Rendered inside an iframe that is served to an opaque sandboxed origin, a `{"error":…}` blob is indistinguishable from a broken frame; the first-render placeholder write was also wrapped in `catch { /* preview-only nicety */ }`, so a failed write left no pointer, no preview and no trace | **Template + rule:** `previewErrorCard` (pure, tested) renders every failure as a readable, phone-safe HTML card with the status kept honest for programmatic callers; the swallowed placeholder-write failure now posts to chat |
| 23 | Harness (this change) | An "advisory" gate blocked the build exactly like a normal one — the wrapper meant to neutralise its exit code never executed | `asAdvisory` appended `rc=$?` after the gate script inline, but every gate script ends in `exit 0`/`exit 1` and `exit` terminates the whole shell | **Machine check:** the gate runs in a subshell so its exit is its own; `mock2-baseline-gates.test.js` executes the wrapper under `sh` and asserts it reports the finding AND exits 0 — the original defect shape is the regression case |
| 24 | Every project, since the platform module existed | The approved design reached the screens the build wrote and STOPPED at the edge of them: the header, nav, buttons, cards, theme toggle, legal footer and sign-in page rendered in a fixed blue-grey no matter what was approved — a two-palette app by construction | The mockup emits `--surface-1`/`--text-1`/`--accent-on`; the shell reads `--app-surface`/`--app-text`/`--app-primary`. Two disjoint namespaces. Measured: the shell reads 24 variables, an approved design defined THREE, and 14 `--app-*` names were declared NOWHERE — every use falling back to a hardcoded hex (`var(--app-text,#12263f)`). The design-adherence gate could not see it because it deliberately excludes base.css/platform.css from "app CSS" | **Template + machine check:** `buildTokenBridgeCss` emits a `==bridge==` block into design.css re-expressing every shell name in terms of the approved design (:root-only — custom properties re-substitute, so one block follows both themes; each mapping keeps the shell default as its var() fallback, so a sparse mockup narrows the bridge instead of blanking the shell). design-adherence now fails when design.css does not drive `--app-*`, by either route. A test asserts 24/24 coverage |
| 25 | Every project | The platform silently overrode the approved design on every colliding name | Pages linked `design.css` FIRST and `base.css` second; PLATFORM_CSS redeclares five names the design also defines (`--bg`, `--surface-2`, `--accent`, `--danger`, `--ok`), and later wins in CSS | **Machine check:** base.css first, design.css last on every generated page; the gate fails a page that links them the other way. Found while fixing this: the sign-in page aliased `--bg: var(--app-bg)` while the bridge defines `--app-bg: var(--bg)` — a var() CYCLE that drops both properties. Its locals are now page-private `--lg-*`, and a test scans every generated page for the cycle shape |
| 26 | Every project | The theme toggle applied to NO page and the branding/legal footer never rendered — both shipped, both loaded by nothing | No generated page had a `<script src="/theme.js">` or `/platform.js` tag. The unit suite reported green because its fixture HAND-WROTE an app-shell.html carrying the script tag no real page had — a fixture that asserted the thing it was supposed to test | **Machine check + rule:** every page head loads theme.js synchronously (deferred flashes white at dark-mode users) and platform.js deferred; platform-intact fails a `<head>` without theme.js. The fixture is now the REAL tree (`buildSeedFiles` + `buildAuthWiredFiles` + the real design.css) — a fixture for "does the real thing work" must be the real thing |
| 27 | Every project | The platform's own features — app name, logo, privacy policy, terms, asset library, machine API keys, read-only SQL — existed only as API endpoints. An operator handed the finished app had no way to reach any of them | Two causes: the mockup system prompt never mentioned sign-in, admin, branding or legal, so the extracted inventory carried none of them and the shell had no nav entry; and no admin UI was ever generated for them | **Template + machine check:** the mockup prompt now states what the base app already provides and requires nav entries for it; `platformAdminMarkup` + `public/platform-admin.js` ship a real admin console section for all five, platform-owned so they ride the upgrade path; platform-intact fails an admin.html that carries the cards but not the script that drives them |
| 28 | ProxyPilot admin, on a phone | On the design and build pages the composer was clipped mid-sentence and the Menu/Chat/Preview bar was entirely off screen — then "fixed itself" after one scroll | The app shell used Tailwind's `h-screen` = `100vh`. On BOTH Android Chrome/Edge and iOS Safari, `100vh` is the LARGE viewport — the height the page would have with the URL bar hidden — and it does not shrink while the bar is showing. The layout was pinned to a height ~110px taller than the visible area, with the bottom bar at the end of it. Scrolling auto-hid the URL bar, which is why it looked intermittent rather than broken | **Machine check:** an `.h-viewport` utility (`100vh` then `100dvh`, so a browser without dvh keeps the old behaviour instead of collapsing) replaces every `h-screen`/`min-h-screen` className; `pwa.test.js` fails if any className reintroduces `h-screen`. Plus `viewport-fit=cover` and a `.pb-safe` bottom inset, without which the bar sits under the iPhone home indicator |
| 29 | ProxyPilot admin (PWA) | Requirement: a server update must never leave a browser serving the old build | Three independent ways it can: HTTP caching of the two files that POINT AT the build (`index.html`, `sw.js`), a service worker that serves HTML cache-first, and a static `sw.js` that never changes bytes so the browser never installs a new worker | **Rule + machine check:** `cacheControlFor` (pure, tested) marks `index.html`/`sw.js`/`manifest` no-cache and `/assets/*` immutable — safe because the build content-hashes them; the worker serves NAVIGATIONS network-first so even a stale worker returns fresh HTML naming the new assets, never caches `/api`, and deletes every non-current cache on activate; the worker is EMITTED by a Vite plugin with a per-build id so its bytes always differ. `pwa.test.js` asserts each of those, including that the emitted id was really substituted |
| 30 | VAPID backfill (this change) | `install.sh` and `update.sh` each generated a VAPID key pair that DID NOT MATCH — a public key from one pair and a private key from another | Both scripts called the generator twice, once with `--public` and once with `--private`. Each invocation mints its own pair, so the two halves were unrelated. In production this presents as every push returning 401 with nothing anywhere to explain it | **Machine check:** `validateVapidConfig` derives the public point from the private scalar and refuses a mismatch by name; `web-push-logic.test.js` asserts neither script calls the generator per-key and that both keys come from one run. Found by running the real backfill against a fixture .env and validating the result — not by reading the script |
| 31 | Baseline gates | The design-adherence test passed alone and failed in the full suite | Every gate script wrote to FIXED `/tmp` paths (`/tmp/pp-approved`, `/tmp/pp-app.css`); the test runner runs files in parallel, so concurrent gate runs clobbered each other's scratch files. Harmless in production (each project's gates run in their own container) but a real flake, and one shared host away from a real corruption | **Machine check:** each gate run takes a private `mktemp -d` with a cleanup trap; three consecutive full-suite runs confirmed stable |
| 32 | Web Push delivery | Push had three ways to fail silently, none of which throws: an unhandled `pushsubscriptionchange` (the service retires an endpoint and delivery just stops weeks later), a `push` handler that returns without `showNotification` (Chrome shows its own "site updated in the background" notice and eventually REVOKES push permission), and a subscription stored with malformed keys (fails on every send forever, discovered long after the subscribe that caused it) | Web Push reports success at every layer that can see it: the push service returns 201 because it cannot read the body either, and the browser discards what it cannot decrypt without telling anyone | **Template + machine check:** the service worker handles `pushsubscriptionchange` by re-subscribing with the same VAPID key and posting the new endpoint; every `push` path calls `showNotification`, including the parse-failure path; `validatePushKeys` rejects a bad subscription at SUBSCRIBE time. Verified by sending a real encrypted push at a stub push service and DECRYPTING it the way a browser does — the only check that covers the whole chain rather than its pieces |
| 33 | ProxyPilot admin, phone | The navigation drawer could not be scrolled — everything past "Users" was unreachable, with no scrollbar and no way down | The nav was `flex-1` with neither `min-h-0` nor `overflow-y-auto`. A flex child's default `min-height: auto` means it refuses to shrink below its content, so the list simply overflowed the drawer and was clipped. Compounded by `inset-y-0` on the FIXED drawer, which resolves against the initial containing block — the LARGE viewport on mobile — so even the drawer's own footer sat under the URL bar | **Machine check:** `flex-1 min-h-0 overflow-y-auto` on the nav and `h-viewport` on the drawer; `pwa.test.js` reads the actual className and fails if any of the three is dropped. The `min-h-0`-missing shape is the general lesson: every scrollable flex child in this codebase needs it |
| 34 | Mockup preview | A failed preview rendered as Chrome's broken-page icon and nothing else — no reason, no retry, indistinguishable from "still rendering" | Two layers. The route answered failures with JSON, and a JSON body in a frame under `X-Content-Type-Options: nosniff` is refused outright — hence the icon rather than readable text (fixed earlier by serving an HTML card). But the panel ALSO cannot diagnose itself: the mockup is served to an opaque sandboxed origin, so `contentDocument` is unreadable and `load` fires for a browser error page exactly as for a real document | **Template + rule:** a cheap `/mockup-preview/status` probe answers the same three questions as JSON without reading the mockup body; the panel calls it on mount and every reload and renders the reason over the frame with a Try-again. The operator never sees an unexplained grey square |
| 35 | Push test send | The card read "On for this device. 1 device subscribed in total." directly above the toast "The test did not go through — this browser is not subscribed". Both were rendered from the same page at the same moment | Two halves of one design error. The test-send route looked the subscription up with `listSubscriptions({ userId })`, but a subscription's identity is its ENDPOINT (the UNIQUE key) — a row can carry a different or null user_id for ordinary reasons: subscribed under another account, re-registered by the service worker's `pushsubscriptionchange` handler (which has no session), restored from a backup. And the card's "On for this device" was read from the BROWSER alone, so the two sides could drift with nothing to reconcile them | **Rule + machine check:** `getSubscriptionByEndpoint` is the lookup, with a graceful fallback and an actionable `resubscribe: true` when there genuinely is nothing; the card re-posts its subscription on every load — an idempotent upsert keyed on the endpoint — so browser and server disagreeing is unrepresentable rather than merely unlikely. Tests assert the lookup is by endpoint and that the self-heal is present |
| 36 | Project 37 (every build) | The very first build's deploy was HELD every time. The app never came up, the design review screenshotted `ERR_CONNECTION_REFUSED` four times and critiqued the error page, and the operator had to notice and press Deploy by hand | `classifyTaskKind` matched the bare word `cannot` anywhere in the instruction. ProxyPilot's OWN canned initial-build brief contains "Any inventory feature you **cannot** finish this cycle must be visibly marked" — so the platform classified its own feature instruction as a BUGFIX, which armed the no-op anomaly tripwire (#5), which held the deploy of a project that had never deployed anything | **Machine check:** the classifier recognises the platform's own instruction openings explicitly and matches defect PHRASES (`fails (`, `does not save`, `cannot log in`) instead of defect WORDS; the tripwire never holds a project's FIRST deploy, because there is no previous good state to protect. Both canned instructions are regression fixtures |
| 37 | Project 37 | "The app did not work until redeployed." Nothing in the platform ever asked whether the app was actually serving — a build could close clean, pass every gate and leave nothing on the port | The deploy step reported its own result and stopped there. A held deploy, a crashed unit and a port taken by a stale process all looked identical afterwards: a green build | **Machine check + rule:** `probeServing`/`ensureServing` run after every build close and redeploy when the app is not answering (posting to chat when they do); `GET /serving` + `POST /deploy` are not cycle-bound, so they work when the last build is old or absent; a Deploy control with a live Live/Not-answering readout sits in the Flightdeck top bar, the mobile preview strip, and the empty preview state |
| 38 | Project 37 (every design review) | Every design review screenshot was of the SIGN-IN page, so the critique described the sign-in gate and could never compare the built app to the approved mockup — the one thing it exists to do | The review's only credentials came from a fixture-user block the build MODEL writes into `state/ui-checks.json`. When it had written none (an MVP build, an early cycle, a model that skipped it), there was nothing to sign in with, and the honest "no fixture logins exist yet" banner made a permanent blindness look like a transient state | **Rule + machine check:** the platform provisions its OWN admin on the auth component's reserved `@fixture.invalid` domain — a domain that component already excludes from "a real user exists", so the operator's first-admin bootstrap is untouched. The capture tries fixture users, then the platform account, and VERIFIES it got in by navigating to `/` rather than assuming the login took. Verified end to end against a stand-in auth app: create, idempotent re-run, and the operator's bootstrap still open afterwards |
| 39 | Project 37 | The shipped app asked "New to-do" through `window.prompt` — a raw browser dialog in an otherwise styled app | The base app's own `admin.js` used `window.prompt`/`confirm`/`alert` in six places, so the model was copying the house style. Nothing checked | **Template + machine check:** `public/platform.js` ships `pp.alert`/`pp.confirm`/`pp.prompt` built on the shell's modal; the scaffold uses them; the mvp-tier `no-native-dialogs` gate reds a build that calls the native ones. The gate's first version was a FALSE NEGATIVE — backslashes mangled through Python→JS template literal→shell left `grep: Unmatched ( or \(` and it passed everything; it is now written with `[(]`/`[.]` character classes and no backslashes at all, and is tested against both a real scaffold (pass) and the reported defect (fail) |
| 40 | Project 37 | The "Install app" button was welded to the bottom-right corner of every screen forever, and the base app could not send a notification at all | The install prompt was implemented as a permanent floating pill — a permanent ask rather than an invitation — and Web Push simply did not exist in the scaffold | **Template:** the invitation is now a one-time styled modal (Install / Not now, "not now" remembered for 30 days) with a permanent entry in the Notifications panel, and the base app ships real Web Push — VAPID keys minted on first use into a singleton DB row, endpoint-keyed subscriptions, aes128gcm encryption, dead-subscription cleanup, `notifyUser`/`notifyAll` for feature code. Verified by transpiling the GENERATED `push.ts` with Node's type stripping, encrypting a message and DECRYPTING it as a browser would, plus verifying the VAPID JWT against its own public key |
| 41 | ProxyPilot build chat, phone | The composer and the send buttons did not fit on screen, and two permanently-disabled buttons sat below the text box using space the conversation needed | The chat Card claimed an intrinsic `min-h-[26rem]` inside a container that had less than that, so the page scrolled; the textarea reserved its tall form whether or not anything was in it; and Ask / Quick update were rendered `disabled` rather than absent | **Template + machine check:** a `fill` variant that owns its box and scrolls internally, a textarea that grows from one line to at most 35% of the viewport as you type, and send buttons that appear only when there is something to send (text, an image, or a pin). Verified by rendering the real class chain in Chromium at 360×640, 375×667, 390×844 and 768×1024, in both the idle and typing states, asserting zero page scroll on either axis and every control inside the viewport |

| 42 | Every generated app | The build had no way to test the UI it was writing. ProxyPilot drove a browser, but only from the HOST — `state/ui-checks.json` is a declarative JSON spec the platform executes against the DEPLOYED app — so the model could not write a real test, run one while working, or see a failure before the deploy | Browser testing lived entirely on the platform side. "Does the UI work" was answered either by a JSON spec written to satisfy a coverage gate or by nothing at all; the operator's word for the result was "junior" | **Template + machine check:** the scaffold ships a real Playwright suite — `playwright.config.ts` whose `webServer` builds, migrates a SCRATCH database and serves the app itself (so `npm run test:e2e` needs nothing deployed), desktop + mobile projects, and `e2e/platform.spec.ts` asserting the base app's own guarantees. A new mvp-tier `e2e` gate runs it BEFORE the deploy. Two escape hatches, both narrow: no Playwright and no browser binary skip green (an environment problem must never red a build); a failing test never does. Every one of those states is executed under a real `sh` in the test |
| 43 | Base app, every project | `GET /api/health` answered **503 BOOTSTRAP_REQUIRED** until a human created the first administrator | `bootstrapGate()` was mounted before the health route, conflating "is this process serving?" — what a load balancer, an uptime monitor and an orchestrator ask — with "has someone finished setting it up?". A freshly deployed app read as unhealthy and, depending on the platform, would be restarted or pulled from rotation before anyone could sign in. ProxyPilot's own deploy probe hits `/` (302, accepted) so it never saw it | **Template + machine check:** health is mounted before both the auth and bootstrap gates; a test asserts the mount ORDER in the emitted `src/app.ts`. Found by running the app's own Playwright suite against a fresh database — the first thing that ever asked the app this question |
| 44 | Base app, every project | The sign-in screen — the one screen every app has, and the only one the design review could see — carried NO legal footer and NO theme control, although `platform.js` and `theme.js` were both loaded on it | Both modules were written to be mounted and neither was: `platform.js` exposed `Branding.footerHtml()` and left it to each page (no page did it), and `theme.js` drives any `.theme-toggle` element (no page rendered one). The module's own header comment says the copyright notice and Privacy/Terms links exist to render on the sign-in screen | **Template + machine check:** `platform.js` now mounts the footer itself into every `[data-legal-footer]` slot, twice (immediately with the fallback so the screen is never legally bare, then again once branding loads); the sign-in page carries the slot and a 44px `.theme-toggle`. Tests assert the mount function exists and that login.html carries both. The general lesson: a module that "exposes" UI and trusts pages to mount it ships as nothing |
| 45 | Base app, every project | Clicking the theme toggle did nothing visible. The icon changed, the page did not; it took two clicks to see anything | The cycle was a fixed list `system → light → dark`, and the default is `system`. On a device set to light — most of them — the first click moved system → light, which is the same pixels. The three-state model was right; starting from a fixed list instead of from what the user is LOOKING AT was not | **Template + machine check:** the cycle starts from the effective theme (`system → opposite → the other → system`), so every click flips the screen with all three preferences still reachable. The test executes the real `theme.js` in a stubbed DOM for a light device and a dark one and asserts the applied `data-theme` changes on EVERY click — the property that matters, not the list |
| 46 | ProxyPilot, design stage | Assets could only be collected in the BUILD phase, which is after the mockup has already been made | The asset library was wired into Flightdeck only. A logo, a screenshot of the thing being replaced, or the copy a screen should carry is exactly the context a mockup should be made FROM — collecting it afterwards is collecting it too late | **Template:** Assets is a third panel in the design workspace on a phone (the same three the build phase offers) and a Preview\|Assets strip beside the mockup on desktop; before the first mockup exists it sits directly under the design chat |

| 47 | Project 38 | The build FAILED after a successful deploy. The app was live, serving and working; the cycle went red, and the operator had to press "Continue build" | The smoke gate ran after deploy and refused `state/ui-checks.json`: every step used `{"action":"expect_visible","selector":"#x"}` and the parser wanted `{"expect_visible":"#x"}`; the roster was `users:[{role,email,password}]` and it wanted a `login` block spelling out the sign-in form; and `expect_value` — which the ui-interaction gate's own advice tells the model to use — was not a step kind at all. Three shape mismatches, all in the harness's favour, none of them a defect in the app | **Template + machine check:** the parser accepts BOTH spellings and normalizes (`expect_value` folds onto the fill it verifies, a user roster becomes a login block using the base app's own sign-in form, `login: "<email>"` resolves back to a role). Project 38's exact spec is the regression fixture. And the backstop for the next unanticipated spelling: a spec that will not PARSE no longer fails a cycle whose deploy succeeded — the app stays live, the problem is stated in chat, and the build completes as pending verification instead of throwing the deploy away |
| 48 | Every project, since the shell existed | "No theme change light/dark." theme.js was loaded on every screen, applied the stored preference, and offered NO WAY TO CHANGE IT | Three of the four generated pages rendered no `.theme-toggle` element. theme.js drives any element with that class by delegation — and nothing ever created one outside the sign-in page | **Template + machine check:** every shell page ships a 44px `.theme-toggle` and a `[data-legal-footer]` slot, and theme.js AUTO-MOUNTS a control into the page header when it finds none — which is how an existing project gets one from a base-app upgrade without rewriting pages a build may have restyled. Verified in a browser on both a legacy page (no toggle in the markup) and a new one: exactly one control, and it flips on every click |
| 49 | Every project | The admin screens' toggle rows rendered as one word per line, overlapping whatever was next to them | `.switch` was written for `<label class="switch"><input><i></label>` — a 44px inline-block styling a separate `<i>` track with the input visually hidden. Every actual consumer writes `<div class="switch"><input><label>text</label></div>` with no `<i>` at all, so the input was invisible and the label was crammed into 44px | **Template + machine check:** `.switch` is a flex ROW; the track is the input itself (`min-height` too, or `.field input`'s 44px stretches it into a square); the label is a flexible column with `min-width:0`. A test asserts the CSS matches the markup every consumer actually writes — the general lesson is that CSS and markup that disagree ship as damage, and only one of them is checked |
| 50 | Project 38 | "The theme was better but still off from the design/mockup" — the app used 9 of 55 approved design variables and the design-adherence gate PASSED | Every failure rule was keyed on how many variables the app DECLARED (>= 8, >= 12). Project 38 declared only 6 of its own while writing 15.8KB of CSS with the colours typed straight in, so it passed under all of them. The gate was measuring the wrong thing: what matters is how much of the approved design is REPRODUCED, not how the app spells its own values | **Machine check:** two rules that do not depend on the declaration count — under 25% coverage with real CSS behind it fails outright, and 20+ distinct hardcoded colours at under 50% coverage fails. Colour literals inside `var(--x, #fallback)` are stripped first, or every correctly-bridged app would fail. All three cases (project 38's shape, a faithful app, good-coverage-but-hardcoded) are executed against the real gate under `sh` |
| 51 | Baseline gates (twice) | A gate shipped a broken regex and passed everything, silently | A backslash eaten travelling through a JS template literal: `no-native-dialogs` emitted `grep: Unmatched ( or \(`, and the design gate's colour counter emitted a literal BACKSPACE where a word boundary was meant and counted zero colours in a file full of them. Both cases ran, printed "passed", and checked nothing | **Machine check:** every gate script is scanned for control characters (the fingerprint of an eaten escape) and syntax-checked under `sh -n`. Writing the explanatory comment for this rule WITH the escapes in it reintroduced the bug inside a comment, and the new test caught it — which is the argument for the test in one line. The standing rule: emitted shell uses bracket expressions, never backslash escapes |

| 52 | Project 39 | The shipped app looked nothing like the mockup — plain elements on the base shell — and the design-adherence gate PASSED, reporting "the app has not written substantial CSS of its own." It also cost LESS than the build before it | The gate read `public/*.css` and nothing else. The build wrote 328 lines of HTML, 493 of JS and ZERO stylesheet, so it measured 0 bytes of app CSS and took the "nothing to judge yet" early exit. `mobile-overflow` skipped for the identical reason ("no app stylesheets to check"). Two of the four look-and-feel gates were disarmed by the same omission, and the omission was the CHEAPER path — which is the part that matters: it must never cost less to skip the design than to follow it | **Machine check:** both gates now read the app's HTML — inline `<style>` blocks count as app CSS, and the markup's class names answer the question a stylesheet cannot. The early exit requires no screens AND no styling (the scaffold's own placeholder page is excluded by its marker sentence, so a project on cycle one is still exempt). Project 39's exact shape is the regression fixture |
| 53 | Project 39 | Nothing anywhere asked whether the built screens reproduced the mockup's STRUCTURE — only whether they referenced its colours | `state/design.css` carries the approved mockup's component CSS lifted verbatim, so the mockup's cards, lists, nav bars and chips have NAMES — and no check ever compared those names against the built markup. An app can reference every approved variable and still lay a screen out nothing like the contract | **Machine check + rule:** the gate extracts the class selectors design.css defines and the classes the built markup actually uses, and fails a build whose screens use almost none of them. Adoption counts by EITHER route — variables or components — because an app that puts the mockup's class names on its elements and writes almost no CSS is faithful, not lazy; only both signals being weak condemns a build. The build instruction and the platform prompt now say what design.css actually contains, because the model was never told |
| 54 | Projects 38 and 39 | Two builds in a row shipped a `state/ui-checks.json` that passed the ui-interaction gate and then failed AFTER deploy on the strict parser | The gate checked only that `id`/`page`/`paths`/`steps` EXIST. The model iterated to green against a validator it could satisfy without being correct, and met the real rules only once the app was live | **Machine check:** the gate applies the same step rules as the connector — both spellings, `expect_value` following its fill, unknown actions named — and when a step has no recognised action it prints that step's ACTUAL KEYS, so it is fixable in one pass instead of guessed at. A validator a model can satisfy without being right teaches the wrong thing |
| 55 | Project 39 | The build wrote 84 lines of real Playwright specs and not one of them ran: `e2e: no browser installed; skipped` | The browser install runs during DEPLOY, and the gate battery runs BEFORE the deploy — so the first build of any project always skips e2e, and a project whose base app was deployed before the feature existed skips it forever | **Machine check:** the install also runs from `maybeUpgradeBaseApp`, the hook that fires before every build. Idempotent — it returns immediately once the browser cache is populated |
| 56 | Baseline gates (third time) | An awk regex literal in a gate collapsed to a syntax error and the gate silently appended nothing for a whole release | An escaped slash eaten by the JS template literal. The control-character scan added after the previous two incidents does NOT catch this one: an eaten backslash-slash leaves printable characters | **Machine check:** every gate is now EXECUTED against a real scaffold and required to leave stderr clean — a mangled regex almost always makes grep/awk/sed print a diagnostic, whatever the escape was. Verified by reintroducing the exact bug and watching the guard fire. Standing rule, now three incidents old: emitted shell uses bracket expressions or `index()`, never a backslash escape |

| 57 | Project 39 | The build that shipped an app looking nothing like its mockup was never reviewed at all | The whole post-build chain — serving check, reviewer account, design review — hung off `onRequestClosed` firing with status `succeeded`. A build that ends in PENDING VERIFICATION never closes its request (it waits on the operator's checklist), so it got none of it. Worse, the smoke-spec backstop added the week before routes MORE builds down that path by design, so the builds most in need of a second pair of eyes were the ones guaranteed not to get one. There were also THREE terminal paths that deploy, and only one of them reviewed | **Machine check + rule:** the chain is one exported `afterBuildReview(projectId)` — serving, account, critique, with the queue-idle guard inside it so every caller behaves identically — and it fires from every terminal that put something live. The test enumerates EVERY `pending_verification` terminal in runner.js and requires each to call it; checking only the first is how the second stayed unwired |
| 58 | Design review | Visual drift could read as clean. The adherence numbers were printed BELOW the accessibility list and only when there were findings | The message was assembled findings-first, and the one question the review exists to answer was a footnote | **Template:** drift is the second line, always, with the arithmetic behind it — approved variables used, approved component classes used, own variables declared — and a `**Visual drift**` headline when any finding is high. A clean app reads `Design adherence:` with the same numbers, so the operator sees the measurement either way |
| 59 | Design review | `checkDesignAdherence` had the same CSS-only blindness the gate did: an app that shipped screens and no stylesheet produced no high finding | It took `{ designCss, appCss }` and never saw the markup — so project 39's shape (328 lines of HTML, zero CSS) was invisible to the review exactly as it was to the gate. Fixing one and not the other would have left the drift undetected on the very path the review now runs from | **Machine check:** it takes `appHtml` too, counts component-class adoption alongside variable adoption, and raises SCREENS_UNSTYLED / COMPONENTS_UNUSED. Adoption counts by EITHER route, and the low-coverage finding is suppressed when the app consumes the design through its components — found by running the review's own logic over a faithful fixture and watching it complain about 4/34 variables while reading 12/12 components |
| 60 | mobile-overflow | A page with no stylesheet skipped the gate entirely — including the checks that have nothing to do with CSS | The viewport-meta check sat BELOW an early `exit 0` taken when no app CSS was found. So the build shape that most needs asking "do your pages even declare a viewport" was the one shape never asked. Found by running the whole battery end to end rather than the gate's own unit test | **Machine check:** the HTML checks (viewport meta, inline fixed widths) run unconditionally; only the CSS-specific rules are skipped for want of CSS. The general lesson: an early exit guarded on ONE input silently disables every check below it, including the ones that input has nothing to do with |
| 61 | The gate battery | Six gates each had a passing unit test, and an app sailed through all six | Every per-gate test asserted its own rule against its own fixture. Nothing ever ran the WHOLE battery over one realistic app, so a gate going blind was invisible — the suite stayed green while the product regressed | **Machine check:** one end-to-end test builds a real mockup → design.css → two apps (project 39's shape, and the same app built on the design) and runs EVERY baseline gate over both, requiring the first to red and the second to pass every gate with clean stderr. It found two further defects on its first run |

| 62 | Project 40 | Every design-review screenshot was the sign-in page, and all three smoke checks ran `[anonymous /]` and timed out at the auth gate — on a build whose deploy had SUCCEEDED | The reviewer account could not be created. `POST /api/auth/bootstrap/superadmin` is guarded by "no real user exists yet", and the first thing any operator does is create their administrator — so on every project a human had actually signed into, the only provisioning path was closed forever | **Template + machine check:** the platform stops asking the app's HTTP surface for something it can do itself and seeds the row DIRECTLY in the container's database, on the auth component's reserved `@fixture.invalid` domain, which `usersExist()` excludes — so the operator's first-admin bootstrap is untouched. The hash format is the component's own (`scrypt$N$r$p$salt$hash`); drift there produces a row that exists and can never sign in, which is the worst shape because everything looks provisioned. Verified against a real Postgres and a real built app: `SEED:ok:admin`, `POST /api/auth/login -> 200`, and `canCreateSuperadmin` still `true` |
| 63 | Project 40 | With a seeded account that demonstrably signed in over HTTP, the BROWSER still could not get past the sign-in page | The page carries three forms — `#form-bootstrap`, `#form-login`, `#form-setup` — and shows exactly one, chosen at runtime from the bootstrap status. `locator(sel).first()` resolved to the HIDDEN create-administrator fields, so "signing in" POSTed to `/auth/bootstrap/superadmin`. The credentials were right; the selector was aimed at the wrong form | **Machine check + template:** an `apiSignIn` door that posts `/api/auth/login` from inside the page (cookies land in the capture's own context — no selectors, no guessing which form shows, immune to a build restyling the page) is tried first, and the form fallback resolves `visible=true` instead of `.first()`. Proven against a real browser and a real app, both doors, five consecutive runs |
| 64 | Base app, every project | A fresh app showed ONLY "create the first administrator". Any account that already existed — the platform's fixture reviewer, an LDAP user, a second admin — had no door at all | The sign-in page picked one of its three forms and offered no way to reach another. The bootstrap form is the right DEFAULT on a fresh app; being the only option is what made the other accounts unreachable, for automation and for the operator alike | **Template + machine check:** the page carries "Already have an account? Sign in" and a way back, both `type="button"` (a submit there would post the bootstrap form AND be what a generic `button[type=submit]` selector clicks) and both 44px. The OUTGOING login.html hash is registered as upgradable, or every existing project would have stayed on the one-door page while the tests passed — a test asserts that registration too |
| 65 | Every browser check, since the PWA landed | Checks failed intermittently: a login that had plainly succeeded reported "signed in: false", gotos aborted, evaluates died with "execution context destroyed" | `install.js` reloads the page when a newly installed service worker takes control — and every check and every screenshot runs in a BRAND NEW browser context, so that reload is not an edge case, it is the first navigation. It landed in the middle of whatever the check was doing. Intermittent, so it read as the app being flaky | **Machine check:** automation contexts are created with `serviceWorkers: 'block'`, and navigations go through a `gotoStable` that retries an aborted load. The PWA's own behaviour is covered by the app's Playwright suite, where a reload is the thing under test rather than noise on top of everything else |
| 66 | Every form sign-in, since ui-checks existed | `loginAs` returned while the login POST was still in flight, so the caller tested "am I signed in?" against a page that was still signing in — and recorded a failed login on a successful one | It waited with `waitForLoadState('networkidle')`. The submit handler is an XHR followed by `location.assign('/')`: at the moment of the click there is no navigation and no in-flight request yet, so networkidle RESOLVES IMMEDIATELY on the already-idle page. The wait looked correct and was a no-op | **Machine check:** it waits for the URL to leave `/login` instead — the observable that actually marks the sign-in landing. Deterministic before the fix (0/5 with the reload race removed), 5/5 after. The general lesson: waiting for "idle" right after the click that STARTS the work waits for nothing |

| 67 | Every project, since the asset library existed | A logo, brand colours or real wording uploaded during the DESIGN stage never reached the mockup. The library was collected on the design page, shown on the design page, and read only by the BUILD turn | `buildAssetSection` was wired into runner.js and runner-sdk.js and nowhere else. The design stage — the stage whose entire job is deciding what things look like — never called it. And `buildAssetContext` names images without showing them, which is correct for a build turn and useless for a render: a logo has to be LOOKED at to be placed, colour-matched and weighted | **Template + machine check:** the concept turn reads the library once and uses it twice — the design partner is told what is on hand (so it stops asking for a logo that was already uploaded, and says so once when the library is empty), and the RENDER is given the pictures themselves, ranked pinned → logo → reference → screenshot and capped at four because every image costs real tokens on a long render. The mockup's framing is deliberately stronger than the build's ("not optional reference"): on a mockup the logo and the wording ARE the brief |
| 68 | ProxyPilot, phone | Tapping **Details** on the workspace's bottom bar landed on a page with no bottom bar — the bar you tapped it from was gone, and with it the way back to Preview or Assets | Details was a sibling tab of the workspace, so it fell outside the component that draws the bar. The bar's own destination was the one place it did not follow | **Template:** the panel state is lifted out of both phone workspaces (mockup and Flightdeck) and mirrored to the page, so Details renders the SAME bar with Details marked active and a tap hands you back to the panel you name. Details is chromeless there too — otherwise the shell's top bar and the tab strip stack on top of a bar that already offers both. Verified in Chromium at 360/375/390: no horizontal scroll, every control ≥44px, and the bar actually on screen at the bottom rather than pushed below the fold |
| 69 | ProxyPilot, phone | While the container was provisioning, the setup steps rendered only in the Preview panel — a different panel from the one the operator was told to start in | The progress list lived inside `PreviewPlaceholder`. On a phone the panels are mutually exclusive, so someone sitting in the chat (where the app says "describe what you want to build") saw nothing happening at all | **Template:** the same live step list renders in the design chat, with the note that anything typed now runs the moment provisioning finishes. The general lesson: on a single-panel layout, "it is shown on the other panel" is the same as not shown |

| 70 | Base app, every project since the shell existed | **The Admin nav link rendered for every signed-in user, whatever their role.** The markup carries `hidden` and the shell unhides it only for administrators — and it showed anyway | `base.css` defined `.hidden` (the CLASS) and never `[hidden]` (the ATTRIBUTE). The browser's own rule for the attribute is `display:none` at the lowest possible precedence, so any author rule that sets display beats it — and `.btn` sets `display:inline-flex`. Every element in the shell carrying the attribute was visible. `login.html` had `[hidden]{display:none!important}` in its own style block, so the author knew; the shared stylesheet did not | **Template + machine check:** `[hidden]{display:none !important}` in base.css (platform-owned, so every existing project gets it on the next upgrade), and a platform baseline check that signs in as a VIEWER and asserts the admin link is not offered. Found on the first real run of the viewer fixture — nothing had ever signed into a generated app as anything but an administrator |
| 71 | Every generated app | Nothing ever asked whether a route guarded in the UI was guarded on the SERVER — the single most common real defect in a generated app | Two reasons, both structural. There was one fixture account and it was an admin, so there was no one to ask the question as; and the check vocabulary had no way to say "this must NOT be offered" — `expect_absent` did not exist, so the assertion was not writable even by hand | **Template + machine check:** a second fixture on the same reserved domain, seeded in the same pass, taking the LEAST privileged role the project has (a viewer that quietly became an admin would make every permission check pass and prove nothing). `expect_absent` joins the step vocabulary, and four platform baseline checks run on every cycle. Proven in both directions against a real app: green when the guard holds, and RED when the server-side role check is removed — while the nav-link check still passes, which is exactly why the URL-typing check is the real one |
| 72 | The smoke connector | On a cycle where the model wrote no usable `state/ui-checks.json`, NOTHING exercised the rendered app — the cycle most likely to be broken was the one with no browser checking at all | The connector only ran checks `if (file.exists)` and the file is written by the build model. Projects 39 and 40 both hit it | **Machine check:** the platform's own baseline is derived from what the platform SHIPS (so it cannot be written wrong), is appended to whatever the model wrote rather than replacing it, and runs on an empty spec too. It never satisfies the model's coverage gate, so it cannot be used to avoid writing real checks. A baseline failure is reported as "BASE APP check failed (not your change)", because reading it as a defect in the build diff wastes an hour |
| 73 | The fixture seeder | A database that was not up reported "the seeder produced no result" — the least useful sentence available | `await client.connect()` sat OUTSIDE the try block, so a refused connection killed the process with a stack trace instead of printing the one labelled line the parser reads | **Machine check:** connect() moved inside the try; a refused connection now reports `SEED:error:connect ECONNREFUSED …`. Verified by pointing it at a dead port. The general lesson: setup that runs before the error handler is setup whose failures have no handler |

| 74 | Every generated app | The operator's logo reached the mockup and never reached the app. The favicon, the phone home-screen icon, the install prompt and the task switcher all still showed the scaffold's generic blue circle — everywhere the app was seen OUTSIDE its own pages, it was anonymous | Icons were written once, at scaffold time, from a hardcoded SVG. The logo usually arrives AFTER provisioning (the operator uploads it during the design stage), so there was no moment at which anything looked for it | **Template + machine check:** the pre-build hook writes the project's logo as `public/app-icon.<ext>`, regenerates the manifest and rewrites a fenced icon block in every page head. A favicon asset beats a full logo (a wordmark in a 192px square is illegible), a mark wider than 1.6:1 is DECLINED with a sentence the operator can act on rather than masked into a smear, and only an SVG claims `maskable` — a raster is letterboxed instead of having its edges cropped on every Android home screen. The scaffold's mark stays as the manifest's last entry, because a manifest whose only icon 404s shows nothing at all |
| 75 | Every deploy, since the probe existed | "The app did not work until redeployed", and "there are parts that don't work, code error" — on builds the harness reported as deployed and healthy | `probeServing` asked for `/` and accepted any status below 500. On an auth-gated app that is the 302 to /login — and a dead database, a failed migration, a 500ing API and a blank white page ALL produce exactly that 302, because the gate redirect happens before any of them is reached. "The app deployed" meant "a process is listening" | **Machine check:** a readiness probe that asks the questions a deploy probe should — the app's own `/api/health` must be 200 (a 503 there is the app SAYING it is not ready, and used to read as "below 500, so fine"), the sign-in page must RENDER, the stylesheet must be served, and — using the platform fixture — one SIGNED-IN request must come back with the app. That last one is the question the redirect was hiding. Proven against a real app in both directions: 6/6 when healthy, and FAIL on a 500 behind the gate while anonymous `/` still returned the same 302 the old probe called healthy. It reports and never blocks: the deploy already happened, and the operator needs to know rather than be stopped |
| 76 | The post-build chain test | An assertion that the chain runs in order stopped covering the last step the moment a step was added | It sliced a fixed 3000 characters from the function. Adding readiness pushed `maybeAutoDesignReview` past the window, and the assertion failed loudly — which was luck. A slightly shorter addition would have left it silently passing while checking nothing about the final step | **Machine check:** the slice runs to the end of the function instead of a fixed length, and the order is asserted pairwise over a named list, so a new step is either in the list or plainly missing from it. The general lesson: a test that samples a fixed window of a growing thing degrades into a test of the beginning of it |

| 77 | ProxyPilot, design stage | The provisioning step list rendered TWICE on the same screen — once in a placeholder card and once, identically, in the chat directly below it | Learning 69 added the steps to the chat because on a phone the Preview panel is a different screen. On the STACKED desktop layout both are visible at once, so the same spinner and the same four lines appeared twice. Fixing "it is not visible here" by adding a second copy is how a duplicate is born | **Template:** the placeholder is suppressed while provisioning (the chat owns the steps) and returns once the project is up, which is when it earns its place — it is what explains where the preview will appear |
| 78 | ProxyPilot, phone | The assets question "pops up for half a second and disappears" | It was gated on the chat having NO messages at all. A system message — a provisioning note, a queued-action note — is the platform talking, not the operator submitting a prompt, and provisioning notes are the norm on the phone. The modal appeared the moment the asset count landed and vanished when the chat loaded and turned out to contain one. The same bug was on desktop; nothing had tripped it there | **Machine check:** "the conversation has not started" means no USER message, and the modal waits for both the chat and the count to load before deciding at all — deciding on the initial null state and re-deciding IS the flash. The check that verifies it SAMPLES FOR SIX SECONDS rather than looking once: a check that looks immediately cannot see something that disappears a beat later, which is exactly why the first version of it passed on a broken build |
| 79 | ProxyPilot, design stage | Saying yes to "do you want to load logos or assets?" pointed at a panel behind the modal instead of opening it | The question and the library were separate surfaces, so yes meant "go and find it" — and on a phone that panel is a different screen entirely | **Template:** yes opens the library ITSELF over the chat, and "Add to chat" is the way out — it closes, confirms what is now in the library, and puts the cursor in the composer, so the next thing the operator does is write the prompt those assets are for |
| 80 | Every project | A logo uploaded AFTER the app was built was never applied. It reached the next build's context — and nothing said it was NEW | The library was read in full on every build turn, so a fresh logo arrived in a pile of standing reference material framed as "use it when relevant". The build had no way to tell "the logo you have been using all along" from "the logo that arrived five minutes ago", so it had no reason to go back and change anything. The operator had to notice, and to ask | **Machine check:** the fingerprint of what each build SAW is stored on the project, so the delta is arithmetic rather than a judgement call, and a changed library gets its own section that says apply it — not merely know it. Two properties are load-bearing and both are asserted: the FIRST build is not told everything is new (it would shout about material given in full in the same turn), and the fingerprint is recorded only where the build SHIPPED — recording it at detection time would mean a build that failed before applying the logo had "seen" it, and the change would be lost in silence |
| 81 | Project 42 (N7), every project | On the sign-in screen the Privacy / Terms links rendered as raw browser `<button>` chrome and the theme control as a full-width blue bar — and clicking a link produced an unstyled wall of text — while the screen around them wore the approved design | The footer is mounted by `platform.js` and styled by `base.css`, and `login.html` does not link `base.css` (it never has: it is a self-contained page with its own inline `<style>`, and a build is free to restyle it). So the markup arrived and the CSS did not: `.legal-link` and `.legal-page` matched nothing, and `.theme-toggle` lost to the page's own `.btn {width:100%}`. Learning 44 fixed the MOUNT and left the styling on the same assumption that had just been proven wrong | **Template + machine check:** the chrome CSS is one exported block (`LEGAL_CHROME_CSS`) that `base.css` carries AND `platform.js` injects into `<head>` before it mounts anything — the module that mounts UI carries the CSS for it. Every variable reads through a two-level fallback so it renders with base.css, with only the design's token bridge, or with neither; the block declares platform-owned class names only, asserted by a test, because it is injected last on every page. `login.html` now links `/design.css`, which its own header comment had claimed for months. A signed-OUT baseline check opens a legal page and closes it — the existing baseline asserted the footer SLOT exists, which was true the whole time | 
| 82 | Project 42 (N7) | Reading the privacy policy and pressing Back left a sign-in form that could no longer be submitted | The legal page was swapped into `document.body` via `innerHTML` and swapped back on Back. Re-inserting markup re-creates every element WITHOUT its event listeners, and a `<script src>` re-inserted through `innerHTML` never executes — so `login.js` neither survived nor re-ran. Nothing failed visibly; the button simply stopped working | **Template:** legal pages open as an overlay appended over the page (Escape closes, focus is restored, scroll is locked), so the DOM underneath is never touched. The general lesson: `innerHTML` round-tripping is not a way to hide and restore a screen — it silently discards everything that is not markup |
| 83 | Project 42 (N7) | `runner crashed: withPlatformLogin is not defined` — a $6.58 build went red in the post-deploy smoke stage, AFTER the app had deployed and was serving | `smoke.js` called the helper and did not import it. A machine check for exactly this class already existed (learning 12) — and it scanned a HAND-MAINTAINED list of helper names that nobody had added this one to | **Machine check:** the candidate list is DERIVED — every name exported by exactly one sibling `mock2/` module, flagged wherever another module calls it without importing or declaring it. Nothing to remember and nothing to maintain. Verified by re-breaking `smoke.js` and watching it fail. Comments and string contents are blanked by a state scanner first, after the regex version read the shell glob `/*/ ` inside one of caddy.js's template literals as a block comment and swallowed that file's whole import block. The general lesson: a ratchet whose coverage depends on someone remembering to widen it is not a ratchet |

| 84 | Project 42 (N7), every build | The gate battery reported **8/8 green** on a cycle where one gate ran nothing (`e2e: no browser installed; skipped`) and another passed at 40% of the approved design | Gate status was `exit === 0 ? 'passed' : 'failed'`, so a gate that correctly exits 0 to avoid redding a build for an environment problem was indistinguishable from one that did the work. And design-adherence's failing bars are deliberately generous (a quarter of the approved variables OR classes), so a build just over the line printed the same single word — "Passed." — as one that reproduced the design | **Machine check:** `gateStatusFromOutput` adds a `skipped` status, derived from a property every gate already has — a gate that ran prints `OK`/`Passed.`, a gate that bailed says "skipped" and prints no verdict. `skipped` still resolves the battery green (blocking a checkpoint on a missing browser binary would be a regression); it is the REPORT that stops lying, in the cycle log and as an amber icon in the progress tree. design-adherence gained a non-blocking `PARTIAL` verdict for a build under half on BOTH adoption routes — never one route, because a build consuming the design through its components legitimately references few variables, and a gate that cries wolf gets ignored. Its numbers go on the last line, because the cycle report keeps only each gate's final three |
| 85 | Project 42 (N7) | The build spent **34 of its 73 turns and 41% of its $6.58** hand-rolling an integration test: a scratch Postgres, migrations, booting the server, four searches for `signAccessToken`, a hand-minted token, a CRUD round-trip | The platform runs all of that itself after deploy — readiness probe, migration dry-run on a scratch database, browser connector with its own fixture account. Nothing told the build that. Worse, the finish contract defines "verified" assumptions and the model read that maximally: it built a whole second harness to earn the word. Those turns sat at the fattest end of a context that had grown to 154k, which is why so few output tokens cost so much | **Rule:** a prompt section naming exactly what ProxyPilot runs for you after finish, and what therefore not to rebuild (no scratch databases, no booting the server, no minting your own auth tokens) — plus the bar restated: "verified" means you READ the source and can name the file, it does not mean you booted the app. What IS worth running locally is named too, so this reads as a redirect and not a prohibition |
| 86 | Projects 38 and 42 | A build that had never written `state/ui-checks.json` reverse-engineered the format by reading the gate's own source at `/srv/gates/01-ui-interaction.sh` and grepping the filesystem for the schema | The gate's missing-file branch said "add interaction checks covering the touched screens" and stopped. The platform owns that format; making each build rediscover it is a round-trip the platform can simply not charge for — and two builds in a row wrote a spec that cleared the gate and then failed the STRICTER post-deploy parser, after a successful deploy | **Template + machine check:** the gate prints the whole file, and the same template is in the runner prompt so it never has to be reached. A test takes what the gate prints, asserts it is valid JSON (an illustrative `//` comment in it is a trap, not documentation — the first version had two), feeds it back to the gate that printed it, and then to the strict post-deploy parser. All three must accept it |
| 87 | Project 42 (N7) | Action parity rejected the finish for a "silently missing" `Delete asset` that the platform admin console had shipped all along — a confirm-guarded `DELETE /branding/assets/:id` under a different label. Seven searches and a round-trip to discover the feature was already there, then the build renamed a control to satisfy the grep | The gate matched the inventory's label as an exact string. A build's only way to clear it was to make the source say what the contract said, which is the gate teaching a build to edit labels for the detector — the exact thing the runner prompt forbids | **Machine check:** three outcomes instead of two. Exact label → `present`; every significant word of the label on ONE line of UI source → `drifted`, reported and never blocking, because the action is in the app and only its name differs; nothing at all → `missing`, which still rejects. Only a silent drop costs a round-trip now |
| 88 | ProxyPilot, design stage | A mockup render runs for two to five minutes and the design chat showed ONE muted line that each phase overwrote — no sense of progress, nothing to read — while the build chat next door showed a proper timeline | The two chats grew separately. The build chat had an accumulating ActivityStream, a MutationObserver that follows growing content, and scroll-intent tracking that pauses when the reader scrolls up; the design chat had a single status line and a one-shot "land on the newest message" that did not follow anything. Two chats narrating the same kind of work in two different ways is two things to learn, and the poorer one reads as the poorer product | **Template + machine check:** the design chat renders the same ActivityStream, accumulating the job's narration into a timeline — with same-phase lines REPLACED rather than stacked, so a character count that refreshes every 1.5s of a five-minute render does not print thirty near-identical rows — and it uses the build chat's exact auto-scroll: follow while working, pause on a wheel/touch gesture, re-arm on return to the bottom. The check drives a LIVE turn whose narration advances and whose reply grows, and was confirmed to FAIL cleanly on the previous build naming both missing behaviours |
| 89 | The design-chat check itself | The check passed, then failed twice on identical code, then passed again | The harness advances its narration counter on every chat poll and that counter is per PROCESS, so a reused harness starts the next run past the phases the check looks for. The failure read as a defect in the app rather than in the fixture, and cost a stash-and-rebuild cycle chasing it | **Machine check + rule:** the check's own header and the test README say to start a fresh harness, and the check degrades to a NAMED failure ("no scrollable chat box found") instead of a Playwright timeout stack when the timeline is absent. The general lesson: a fixture that carries state between runs makes the SECOND run a different test from the first, and the difference surfaces as a lie about the product |
| 90 | Project 43 (N8) | The app deployed, passed its health check, and **nobody could sign in** — the live URL served a JSON error body and `GET /login` answered 401, as did every stylesheet | The build added an ordinary feature router — `router.use(requireAuth)` inside, full paths declared on it, so `app.use(notesRoutes)` at the root — and the scaffold's `app.ts` put that insertion point ABOVE the line serving the sign-in page. A router mounted with no path prefix runs its router-level middleware for EVERY request, whatever the paths inside it say, so requireAuth 401'd `/login` before the sign-in route was reached. It typechecks, it is internally consistent, and the app starts | **Template + machine check + rule:** the scaffold registers `/login` and the static mount directly under `bootstrapGate()`, above every router, so a new project cannot be shadowed. A `signin-reachable` baseline gate (tier quick, never advisory) fails any `app.use(x)` at the root above the sign-in route, names the line, and gives both remedies — move it below, or give it a path prefix. The runner prompt names the trap with the exact broken shape. Proven by booting real Express in both orders: old order health=200 login=401 css=401; new order login=200 css=200 and the notes API still 401 |
| 91 | Project 43 (N8) | The failure reached the operator as "3 console error(s)" and "Timeout 5000ms exceeded" — three resumed cycles and **$9.81** chasing symptoms of an app nobody could open | Two reporting layers hid it. The HTTP smoke layer asked for `/` and accepted anything under 500 — but on a gated app a 401 at `/` is NORMAL, so that question cannot tell a working app from an unreachable one, and it reported `GET / → 401` as a pass. And the readiness probe, which asks the right question in one line ("the sign-in page does not render, so nobody can get into the app"), hung off `afterBuildReview` — which fires only when a request closes SUCCEEDED or lands in pending verification. On a FAILED build it never ran, which is exactly backwards | **Machine check:** the HTTP layer probes `/login` as its own check — 200/3xx/404 pass, 401/403 fail loudly and name the cause. Readiness now also runs when the smoke gate fails after a deploy, and when it fires its line goes FIRST in the cycle error: the cause above the symptoms. The verdict moved into `smoke-triggers.js` so a test can reach it — `smoke.js` cannot be imported outside a real install, which is why this classification had none. The general lesson: a health check that a broken app also passes is not a health check |
| 92 | Project 43 (N8), the fix for #90 | The app loaded, the sign-in screen rendered perfectly — and it showed **Sign in** on an app with no accounts, with the create-the-first-administrator link hidden. "It broke the first user signup to super admin" | The build applied HALF the fix: it moved the sign-in page above its feature router (so `/login` was 200 and styled) and left that router above `app.use('/api', authRoutes)`. So `/api/auth/bootstrap/status` answered 401 — and login.js reads any non-ok status there as "a user already exists", shows the sign-in form and sets `alt.hidden = !canBootstrap`. A locked door with no handle, and nothing said why. My own `signin-reachable` gate PASSED it, because it only checked what sat above the sign-in ROUTE; and my own prompt had recommended the thing that produces the other half of it — `app.use('/api', yours)` above `app.use('/api', authRoutes)` shadows `/api/auth/*` just the same | **Machine check + template + rule:** the gate's boundary is now the LAST platform mount or page route, not the sign-in line, and it flags path-prefixed mounts too — position is what matters, not the prefix. The scaffold ships an `ADD YOUR ROUTES BELOW THIS LINE` marker at exactly that boundary. The readiness probe gained a `SIGNUP` check on `/api/auth/bootstrap/status` (200/404 pass, 401/403 fail) so the state is named in one line instead of inferred. The prompt now says the rule plainly — YOUR ROUTERS GO LAST — and that a path prefix is not a substitute. Verified by booting four mount orders on real Express: both half-fixed shapes serve `/login` with 200 and still hide the signup form, and readiness catches both. The general lesson: a gate scoped to the symptom you saw last time passes the next arrangement of the same defect |
| 93 | Project 44 (N9) | "There is limited time from seeing the create super admin first user, then when the app finishes I'm unable to log in" — so the operator asked the BUILD to create their account instead, and the build refused twice at $1.94 | Nothing was expiring. The first account belongs to the operator and the build is rightly forbidden to make it, but the ONLY thing that ever announced the door was the app's own sign-in page, mid-build, which an operator sees if they happen to be looking. A rule you can only satisfy inside a window nobody controls reads as a window you missed — and it pushes people to ask the build to break the rule, which is exactly what happened | **Template + rule:** an **App access** panel on the project: what accounts the app actually has (naming the platform's own `@fixture.invalid` fixtures and saying they do not count), and a first-administrator form the operator uses on their own schedule, after the build. It goes through the app's OWN `POST /api/auth/bootstrap/superadmin`, so the race guard, the hashing and the audit entry stay the app's and the platform never writes a user row; the password is typed by the operator, delivered through a file rather than an argv (asserted by a test that reads the recorded command line), never stored and never echoed. A chat message posts at build close while the door is open, so the invitation arrives when the operator can act on it. `canAttempt` (open AND reachable) is what the UI acts on — on a shadowed app the roster fallback says "no real users" and offering a form there sends them to a 401. The general lesson: a rule the product cannot help you follow is a rule the product is asking you to break |
| 94 | Project 44 (N9) — the root cause behind 93 | The app answered `{"canCreateSuperadmin":false}` while its entire users table was ONE row: `design-review@fixture.invalid`, the account the platform seeds for its own smoke checks. So the platform's test fixture consumed the operator's first-admin bootstrap, mid-build — which is precisely why the create-super-admin form "appeared briefly and was gone by the time the build finished" | The auth component's `usersExist()` excludes `@fixture.invalid` for exactly this reason. But `seedBuiltinComponents` only replaced a stored component version that could not WIRE the bootstrap — a liveness test, not a currency test. Once any wiring version was stored, the bundled document was never applied again however much its content moved on, so every install predating the exclusion kept the old auth code forever and every project provisioned from it inherited the bug | **Machine check + template:** the seed rule is content-based — a sha256 over the bundled files, contract and usage notes, compared with the stored current version; differing content on a platform-owned version (created_by 0) inserts a new one, and differing content on an OPERATOR-owned version is reported loudly instead of passing as "current version wires". The App access panel names the contradiction rather than repeating it — "an account already exists" was the platform saying its own bug back to the person it happened to — and offers a domain-scoped **Free the first-admin slot** action. Its SQL goes in through a FILE: the nested `psql -c "… LIKE '%@fixture.invalid' …"` form parses under `sh -n` and silently loses the literal's quotes, which a stub-`su` test caught. The general lesson: "is it working?" and "is it current?" are different questions, and an upgrade rule that only asks the first one freezes every fix that follows |
| 95 | Project 44 (N9), the remedy for 94 | "Free the first-admin slot" — the button whose whole job was to unlock an operator locked out of their own app — answered **400 Internal server error**, saying nothing about why | `request()` hands its options straight to `fetch()`, and `fetch()` coerces a non-BodyInit body with `String()`, so `body: {}` becomes the literal `"[object Object]"`. `express.json()` rejects that with a SyntaxError carrying `status: 400`, and the backend's global handler renders it as `{"error":"Internal server error"}` in production. Both new API-client methods had a bare object instead of `JSON.stringify(...)`; the GET worked because it has no body, so the panel looked healthy right up to the click | **Machine check:** `frontend-api-client.test.js` asserts every `body` inside a `request(...)` call is a string expression, catching BOTH `body: {…}` and the shorthand `{ method: 'POST', body }` — the shorthand is how the broken method was first written, and a `body:`-only regex walks past it. It reads the frontend source from the backend's runner (precedent: web-push-logic.test.js), blanks comments first because the client's prose names body shapes constantly, and balances brackets so the failure message quotes a value that actually appears in the file. Proven by re-breaking api.js and watching it name `api.js:1540 — body: {}`. The general lesson: a convention that lives only in the shape of neighbouring lines is not a convention, it is a habit — and the first hand-written line that does not copy a neighbour breaks it silently |

## Triage guidance (binding for future findings)

When a human review catches a defect in generated output:

1. Name the failure class, not the instance.
2. Pick the destination — **template** (the generator can no longer produce
   the shape), **rule** (a binding instruction/rules-pack line), or
   **machine check** (a gate/linter that catches it before a human would).
   Prefer them in that order; prose guidance alone is not a destination.
3. Land it with a test that encodes the original instance, and add a row
   here.
| 96 | Project 44 (N9), every app before it | Every generated app served **"© 2026 Application"** in its footer, on its sign-in screen and in its Privacy and Terms pages — the first thing anyone saw of an app the operator had named "N9" | The operator names the project at provision, and the platform used it for the container, the PWA manifest and the dashboard — then seeded the branding row with the column default. `ensureSeeded()` inserted `{ id: 1 }` and let `org_name text NOT NULL DEFAULT 'Application'` decide. Nothing was broken, nothing failed a gate, and the name the platform already held was one field away | **Template:** `src/platform/branding.ts` bakes `SEED_ORG_NAME` from the project name (same 120-char bound as the column, same fallback as the PWA manifest, via one exported `seedOrgName`), and `ensureSeeded()` also HEALS a row still holding the placeholder — the fix reaches existing apps through the platform-owned upgrade path, not only new ones. The placeholder is the sole value treated as "never named", so an operator's own choice is never reverted. Platform module `v8`. The general lesson: an identity the platform is given at provision and then drops is a defect the whole product carries, not a cosmetic one |
| 97 | The seeded auth component, every app with users | Every app provisioned with auth shipped `staff.view` and `adp.sync` in its Admin → Permissions matrix — surfaces from the ADP project the component was extracted from, which the app in front of the operator does not have. Worse, `defaultAllows` returned false for anything NOT in that map, including for the administrator | The map was lifted intact from its origin app, and `usage_md` said "edit it to match your app's surfaces" — guidance a build model may skim. The closed-set default is the real trap: an undeclared key is invisible to `GET /admin/permissions`, rejected by `PUT /admin/permissions` as `UNKNOWN_PERMISSION`, and denied to everyone — so a build that adds a permissioned surface and forgets to declare its key locks the administrator out of the app's own feature AND out of the fix | **Template + machine check:** the shipped map is now only what the component's own admin screens check, and `defaultAllows` grants **admin** any permission the map has not heard of — every other role still authorizes purely through the map, and an explicit override row still wins, so the fallback is neither a ceiling nor a hole. Two tests in `mock2-component-contract.test.js` erase the file's type annotations and RUN it, asserting the behaviour rather than the source text. The general lesson: a default that is safe in the app a component came from is a landmine in the app it lands in, and "documented in usage_md" is not a control |
| 98 | The design system, every project | An operator asked how to get "more professional/fancy/cutting-edge" UI. The honest answer was that the harness had **no vocabulary for movement at all**: `DEFAULT_TOKENS` carried colour, type, radius, spacing and shadow, the only motion in a generated app was a skeleton shimmer and a toggle transition, and the one motion-shaped rule in the platform stylesheet DISABLED animation | Motion was never made a token, so a build had nothing approved to reach for — and a build that animated anyway was inventing vocabulary the adherence gate then counted against it. The design system could specify what an app looks like and had no way to say how it moves | **Template + machine check:** three durations and three easings (`--dur-fast|base|slow`, `--ease-standard|entrance|exit`) in `DEFAULT_TOKENS`, in `MOCKUP_TOKENS`' new `==motion==` fence, in the extractor prompt, in the token bridge, and consumed by five shell classes a screen SELECTS (`.enter`, `.enter-fade`, `.stagger`, `.press`, `.pulse-once`) instead of hand-rolling keyframes. Durations are clamped as well as validated — 4s is valid CSS and a broken interface. Proven end-to-end in a real browser: a mockup that re-values `--dur-base: 260ms` makes the SHELL's `.enter` run at 0.26s. The bug that mattered was in my own sanitiser — it demanded a leading zero, so `cubic-bezier(.2,0,0,1)`, the exact value the platform's own template emits, was silently replaced by the default. The general lesson: a design system whose sanitiser refuses the design system is a loop that looks like it works |
| 99 | Every build since the design review shipped | The post-build review opens a browser, signs in, screenshots the app, critiques it — and posts the result to the chat. **Nothing carried a finding into the next build.** Three builds could receive the same critique and act on none of them, and the only sign was the operator noticing the same sentence three times | `afterBuildReview` calls `runDesignReview` with `apply: false`, so findings lived exactly as long as the message they were posted in. The next build started from the same mockup with no knowledge that anyone had looked at the running app | **Template:** a findings LEDGER at `state/design-findings.json` — merged by finding rather than by review, so a defect raised three times reads as one defect the app keeps shipping; a finding the latest review no longer raises resolves itself; open findings ride the next build's first turn in BOTH runners. Matching is wording SIMILARITY, not a key: the review is a model and does not phrase a defect identically twice, so any text-derived key reports every repeat as new and the ledger never converges (caught by the second phrasing of the same finding scoring as two rows). A finding briefed to eight builds unchanged goes stale rather than costing every build forever. The general lesson: a critique that reaches no one is not a review, it is a diary |
| 100 | design-adherence, every build | The gate had exactly ONE class number — how much of the approved mockup's vocabulary appears in the built markup — so a build that thought of a BETTER element scored the same as one that ignored the design, and a build that traced the mockup scored best of all | Adoption was being measured as if it were quality. The mockup is approved at the moment the operator has seen the least, and everything downstream judges the app against it, so a screen invented in build six had no approved classes by construction | **Machine check + template:** the gate now counts what the build DEFINED that the design does not, and asks the question that matters — is it built FROM the design system or beside it. Token-clean invention is reported as the system growing (with the shortfall still printed, so nothing is hidden); invention with the colours typed in forfeits the "I consume the design through its components" waiver, because a build inventing its own vocabulary cannot also claim to be living inside the mockup's. And a **promotion** path makes it permanent: `design-promote-logic.js` lifts an element's rules — including its `@media` rules, since a component promoted without them looks right on a laptop and overflows on a phone — into a fenced block in `state/design.css` that survives re-approval. The general lesson: a gate that rewards tracing is a gate arguing against design |
| 101 | The design chat, every project | Reference images pasted into the design chat reached that turn's mockup render and no other. The asset library is read by EVERY render — so the strongest input the product has, "here is what I want it to look like", had the shortest memory in it | The two paths grew separately: `hydrateAttachments` (this turn) and `hydrateMockupAssetImages` (the library). Nothing bridged them, so an operator who pasted three screenshots watched the next iteration forget them and had to paste again | **Template:** a design-turn attachment is mirrored into the library, tagged `reference` and pinned — which is what pasting it meant — deduplicated by CONTENT so the same screenshot on three turns does not spend the render's capped image budget on three copies of one picture. A design preset now also carries components, motion and reference images (bytes on disk, like every other image here) and a project's whole look can be saved as one, so the second app starts where the first finished instead of at the platform defaults. The general lesson: the input a product treats as disposable is the one users assume it remembers |
| 102 | Every screen ever reviewed | The gate battery answers "can this app be used" — overflow, dead controls, token adherence. Nothing answered "is this screen any good to READ", so the review was asked to eyeball density and colour-coding from a JPEG: the one thing a vision model is least reliable at and a browser is exact at | Both properties are mechanically observable in the capture the review ALREADY takes, and nobody was taking the measurement. Generated screens characteristically arrive airy — three cards, a lot of padding, and a scroll for everything that matters — and reach for colour as the only status channel | **Machine check:** an in-page probe counts facts above the fold at both widths (leaves and controls, not containers — counting containers scores a deeply-nested empty layout as dense) and elements carrying a saturated status colour with no word, no glyph and no accessible name. Both advisory, both handed to the critique as evidence rather than gated on: a create form SHOULD be sparse, and sign-in-shaped routes are exempt by name. The general lesson: when a check is deterministic, asking a model to eyeball it is paying more for a worse answer |
| 103 | Every project provisioned WITHOUT the auth component | `platform-intact` red on the first build of a project that had done nothing wrong: "src/server.ts no longer calls ensureViews". The branding row and the read-only SQL views never initialised either | The AUTH-WIRED `src/server.ts` calls `ensureSeeded()` and `ensureViews()` at boot; the plain scaffold's copy never did. Two generators for one entry point, and the platform boot lived in only one of them. Every test that would have caught it asserted against the auth-wired copy, because that is the shape a real project usually has | **Template + machine check:** the scaffold's `serverTs()` boots the platform module too, guarded so a database that is not ready cannot stop the app serving, with a regression test that asserts BOTH copies. Found by running the real gate battery over a real scaffold-only project during end-to-end verification — no unit test was going to notice, because every unit test was written against the same copy the code was. The general lesson: when one thing has two generators, a test that exercises the common one proves nothing about the other |
| 104 | Every operator who wanted a better-looking app | "Make it more professional / fancy / cutting-edge" is the instruction the product invites and the worst one it can receive: it names an ADJECTIVE, so the build reaches for the things that signal effort — gradients, shadows, an accent on everything — and ships a screen harder to read than the one before it | The screens that read as designed are not styled differently, they were DECIDED differently: someone knew the screen's one job, the hardest real row, which numbers are worth tapping, how many rows must be visible, and what a status says besides being red. The operator has every one of those answers. Nothing in the product had ever asked them, so the only vocabulary available to them was adjectives | **Template:** a **Design help** interview in the Ask lane (not a fourth build-shaped button — it is a conversation that produces an instruction, which is what Ask is for). Eight questions, one per turn, each carrying WHY it is asked and an example in the SHAPE of an answer; every one skippable, with the assumption stated out loud. The last turn writes the brief: imperative, traceable to something they said, their numbers carried through verbatim, and the words "polish/modern/clean/professional/visual hierarchy" forbidden in it. Three things the design had to survive: the ask lane is STATELESS between turns, so the interview's position lives in a `[design-help n/N]` marker in the chat and is REPAIRED when the model drops it rather than trusted; an interview answer like "check the layout at 390" matches the polish intent, so design help is decided FIRST and suppresses that route outright; and the ordinary 12-message recap would have reached the brief having forgotten answer one, so the interview turn gets its own wider window. Proven by walking all eight turns with a stub model, including a skip, a dropped marker and a polish-shaped answer. The general lesson: when users keep giving you a bad instruction, the instruction is not the problem — nobody asked them the question they could actually answer |
| 105 | Project 46 (N10), and every project provisioned on platform v8 | The build hit `src/platform/branding.ts(115,14): error TS2367: This comparison appears to be unintentional because the types '"N10"' and '"Application"' have no overlap` — a typecheck failure in a PLATFORM-owned file, on its first build. It spent turns diagnosing it and then patched that file, which the next base-app upgrade would have overwritten, reintroducing the error | My own v8 branding seed. It bakes the project name in as `const SEED_ORG_NAME = "N10"` and compares it with `const PLACEHOLDER_ORG_NAME = "Application"`; TypeScript narrows both to LITERAL types, sees two different literals can never be equal, and fails the file. It passed every test here because the emitted code is TypeScript and NOTHING in this repo compiles it — the ratchets check that emitted JavaScript parses and that braces balance, neither of which a type error disturbs. The project name in the tests was "probe", and the bug is invisible until a real name meets a real compiler | **Machine check + template:** both constants are annotated `: string` (load-bearing, and the comment says so), platform module `v9` so the fix REACHES the projects already carrying v8 — a same-version content change would not have, because the upgrade is version-gated. A new RATCHET scans every emitted `.ts` for the defect class: a comparison between two unannotated string-literal constants whose values differ. Reproduced and fixed against the real compiler (v8 errors on a project named N10, v9 is clean), then swept all emitted TS for the whole comparison class. The general lesson: a generator that emits a language the test suite cannot run is a generator with no tests — and the fixture name that makes the tests pass is exactly the name no real project has |
| 106 | Project 46 (N10), build 129 | The first build deployed and then reported **5 of 7 UI checks failed** at $9.65 — including the platform's own `platform-baseline-signin-legal` — with every failure reading `locator.waitFor: Timeout 5000ms exceeded` | The app had no first administrator, which is by DESIGN (that account belongs to the operator). With zero real users the sign-in page shows the create-administrator form and hides the sign-in form, so `expect_visible #login-password` times out; and every check declaring `login: admin@fixture.invalid` fails because nothing creates that user — the build wrote fixture credentials into `state/ui-checks.json` and the platform seeds a fixture account only for the design REVIEW, never for the smoke. So on a fresh app the checks are not failing, they are not yet possible — and the report cannot tell those apart | **Parked, with the diagnosis recorded** (see docs/known-issues.md): the smoke should either seed the fixture users a project's `ui-checks.json` declares — it already does exactly this for the review account — or report "no first administrator yet, so N session checks could not run" instead of N failures. Both are real work and neither belongs in a review commit. The general lesson: a check whose precondition the product deliberately withholds will report the product's own rule as a defect, every time, until someone tells it the difference |
| 107 | Project 46 (N10) build 129, and any build whose summary ends a sentence with a filename | Two of the build's five `finish` attempts were rejected as **"summary over-claims"** — naming `theme.js/design.css.` and `src/platform/branding.ts.` — and the model correctly called it a false positive both times, because `src/platform/branding.ts` WAS in the changed-file list it was shown. A third of that build's $9.65 was spent after its first finish attempt | The claim extractor's token class includes `.`, so a summary ending a sentence with a filename captures the full stop into the path: the claim becomes `src/platform/branding.ts.`, which matches no changed file. The identical sentence without the trailing period passes. Repeated over-claiming AUTO-HALTS a cycle, so this could kill a build over punctuation — and it rejects most confidently exactly when the model writes a well-formed sentence | **Machine check:** trailing sentence punctuation (`.,;:!?`) is stripped from every captured claim before matching — no filename ends in it, so stripping is unconditionally safe — and a genuine over-claim is now reported without the punctuation glued on. Reproduced against the real function with the exact two summaries from build 129, then covered by a regression test that also holds the mid-sentence and still-rejected cases. The general lesson: a checker that rejects a build's honest report is more expensive than the dishonesty it was written to catch, because the model cannot fix what is not wrong — it can only retry |
| 108 | Every project ever created | Creating a project dropped the operator on a page with fifteen cards and no order, so the common first move was to type one sentence into the prompt box and hope. Every part of a good first run already existed — the first-admin door, the asset library, the app's own self-description, the design chat, the approval gate — and nothing put them in a sequence or said any of them were there | That one sentence is the highest-leverage input in the pipeline: the mockup is generated from it, the inventory extracted from the mockup, the first build's instruction composed from the inventory, and BOTH design measurements taken against the mockup. All of it was calibrated to a document written by someone who was never asked who the app is for. The cost is measurable — on project 46, the template instruction "implement every screen, field and action the inventory defines" took 110 turns and $9.65, while a brief that said what to build with numbers in it took 39 turns, $2.53, and closed on its first finish attempt | **Template + rule:** a six-step guided setup (project · your account · logo · about · design · approve), on by default with a `classic` admin toggle that restores the old behaviour exactly. Three design constraints did the real work: nothing BLOCKS (skipping every step contributes empty strings downstream and pushes nothing to the app — asserted directly, because the promise is worthless unspoken); every step DERIVES its done-state from data that already exists for other reasons, so there is no counter to desync and closing the tab mid-setup loses nothing; and the three "about" answers land on the PROJECT ROW rather than in the app, because appContext lives in the generated app and "who is this for" is answered in the first thirty seconds, while the container is still provisioning — a reconciler pushes it through the app's own admin endpoint later, signed in as the platform's existing `@fixture.invalid` admin so there is no second credential. The answers have two destinations: the mockup brief and the first build's instruction. `copy` was in the sketch and came out — before there are screens there is nothing for copy to attach to. The general lesson: when the product's most important input is a blank box, the product has already decided the answer will be bad |
| 109 | Project 44 (N9), and every base-app upgrade ever run | A build's gates went red on a file it had not touched: `src/platform/schema.ts(1,1): error TS1490: File appears to be binary`, and `platform-intact` reporting that the file "no longer exports legalPages / assets / apiKeys". The file was **FOUR BYTES** — `8a 6a 68 ae` — against 5541 in git. The build spent turns diagnosing it and restoring it from HEAD, and its finish was then rejected for naming a file its own diff did not contain | Those four bytes are exactly `printf 'import' \| base64 -d`. The content had been base64-decoded TWICE: `base-app-upgrade.js`'s stdin helper decoded the payload on the HOST and piped plain text into `incus exec`, where the in-container script ran `base64 -d > "$d"` again — consuming the leading run of base64-legal characters ("import"), writing those four bytes, and erroring on the first space. The redirect had already truncated the file, so a FAILED upgrade left a load-bearing platform file destroyed. `concept.js` and `audit.js` always did it correctly (`{ input: b64(content) }`); the broken helper was the one three later writers were copied from — design-review, design-findings and design-promote all inherited it, so the corruption surface was four files, one of them the approved design | **Machine check:** all four now pass the ENCODED payload on stdin and let the container decode once. The test writes a real file through a stub `incus` that runs the script and rewrites `/srv/app` into a fake root — the first version of that stub let absolute paths escape, so the test passed by reading the file it had seeded itself, which is precisely the failure mode a shape-assertion has. Two scanners cover the class across every mock2 module (no host-side decode feeding a script that also decodes; every stdin writer supplies `input:`), and re-breaking one writer fails three of the four tests. The general lesson: a corrupting write that reports failure is worse than one that throws, because the caller retries and the damage is already on disk — and "the helper I copied from" is how one bug becomes four |
| 110 | Project 44 (N9), builds 132–134 | The operator dropped a pin on the Saved/Delete row and wrote "look at ALL elements above the text field, there is so much unused space". The build tightened three margins on that one row. The thing they were actually looking at — a nav bar wrapping onto two lines directly above it — was untouched, so they asked again in plain words in a second build, and a third | Two causes, both in the platform's own wording. The instruction ended **"Apply exactly these changes at the marked spots; change nothing else"**, which fences the build to the resolved selector — right for "make this button blue", wrong for every complaint about space, which is about a REGION and not a node. And the build had been told never to restyle the shell, so the wrapping header was doubly out of reach even though an override in the app's own stylesheet was always legitimate. A pin is where a finger landed; it was being read as a scope contract | **Template:** one shared `ANNOTATION_CLOSING` (both composers had their own copy of the fencing sentence — fixing one would have left the other shipping the half-fix). It says the selector is where they pointed and **the NOTE wins** when the two disagree; that a note about space/layout/density/alignment/order means the pinned element, its siblings, its container and what is stacked above and below it, naming "tightening the one node you were handed is not the fix, it is the appearance of one"; that the app's own CSS MAY override shell spacing while the platform-owned files stay untouched; and that scope is the SCREENS and PROBLEMS named, not the DOM nodes the pins happened to hit. The header line changed too — "the pins mark the exact spots" is the same claim in miniature and it is the one read first. The general lesson: a scope fence written for the easy case silently converts every hard case into a half-fix, and the operator pays for it one build at a time |
| 111 | The Design help interview, one day old | The eight-question design interview from row 104 was right about the diagnosis and wrong as a product. The operator's verdict: "the idea is great but the implementation is cumbersome, 8 questions feels like too many at that time (even if it's the right amount), there is no way to skip the rest of the questions, on mobile it feels like a lot to read and respond to" | It asked the operator to do the designer's work. Every question was a good question, and the person answering them still had to hold eight answers in their head, on a phone, before anything happened — and the escape hatch skipped ONE question, never the rest. Worse, it fired at the moment of least patience: someone looking at a screen that feels wrong wants to see it fixed, not to be interviewed about it | **Template (replacing 104):** **Design options**. The same complaint now triggers one flagship read of the LIVE screen — screenshots at both widths, the deterministic density measurements, the approved design's own class names — which returns a diagnosis with a NUMBER in it and two or three named layouts, ordered smallest change first. Each option is posted as its own assistant message, which already carries the "Build this as a Quick update" chip, so every option gets a working button and the feature adds no new UI at all. Three properties are enforced rather than hoped for: it must SEE the screen (options invented from the sentence alone are the generic advice the operator could have written); the options differ in KIND via a declared strategy, with duplicates dropped by the parser, because "tighter / much tighter / very tight" is one option pretending to be three; and NOTHING is applied. The whole cost of a wrong guess was project 44's three builds — one of which gave the reclaimed space to an empty textarea, which looks exactly like the wasted space being complained about, so the prompt forbids that move by name. The general lesson: when a user cannot name the fix, the answer is to name it FOR them and let them choose — not to make them qualified to name it |
| 112 | Every build that ever ran a UI check with a login | "No build to date has been able to login and assess the screens." The checks a build writes into `state/ui-checks.json` declare fixture users — an address and a password the MODEL invents — and **nothing had ever created those rows**. Every check that signed in as one was rejected at the form, bounced to the gate, and timed out on an element that only exists behind it: project 44 build 133 lost three checks that way on a build that had deployed and worked; project 46 build 129 lost five of seven, at $9.65 | The platform seeded fixtures only for the design REVIEW, and `withPlatformLogin` deliberately stands aside when the spec has a login block of its own — a build that said how to sign in has made a choice the platform must not override. So the one case where the platform's credentials were withheld was exactly the case where nothing supplied any. The tell was visible in every affected run and nobody read it: the platform's OWN baseline checks passed in the same build, because those use accounts the platform creates | **Template + machine check:** `ensureScreenAccounts` seeds the declared users immediately before the checks run, and an **App access → Screen accounts** action creates the whole set (admin reviewer, least-privilege viewer, every declared user) on demand and shows the credentials once, so a person can sign in and see exactly what the checks see. Two properties carry the security argument. Only `@fixture.invalid` is minted — a spec naming a real address is SKIPPED and reported, because that account is the operator's, it would count as "a real user exists", and it would take their first-admin slot. And the seed reads the spec AS WRITTEN: `withPlatformLogin` adds the reviewer under a role literally called `platform`, which is not an admin role name, so seeding the transformed spec would give the reviewer the LEAST privileged role in the table and quietly break every admin screen it exists to reach — asserted by a ratchet that checks the ordering in `smoke.js` AND demonstrates the demotion on the transformed spec. The operator button is not `ensureReviewAccount`: that one stops on "the reviewer already signs in", so on every project built before the viewer fixture existed it would report success and create nothing. The general lesson: a test fixture the product asks a model to name is a fixture the product has to create — declaring a credential is not provisioning one |
| 113 | Every design review since the automation context was factored out | The operator's chat showed **"Design review (after build) did not run — browser error: AUTOMATION_CONTEXT is not defined"**, twice, on a build that had otherwise gone fine. The design review — the whole post-build critique loop, the findings ledger, the promotion invitations — had been dead, and the message reads like the app's fault | `design-review.js` opens its own browser contexts with `newContext({ ...AUTOMATION_CONTEXT, viewport: MOBILE })`. That constant is declared at module scope in `ui-checks.js` and was never `export`ed; design-review.js never imported it. A free variable is a ReferenceError the moment the line runs, and that line needs a real browser and a real container, so the module imported cleanly and every test passed. Nothing failed loudly because the review is deliberately never a gate: it is logged and swallowed. Design options, shipped the same day, calls the same `captureAppScreens` — it would have failed identically on its first use | **Machine check:** the constant is exported and imported (one definition — two copies of "why service workers are blocked" is how they diverge), and a ratchet scans every mock2 module for the defect CLASS: a SCREAMING_SNAKE constant declared at module scope in one module and used in another that never binds it. Two things had to be right before it caught anything, and neither was obvious — `...SPREAD` reads as `obj.property` to a "not preceded by a dot" lookbehind, and an object LITERAL is written exactly like a destructuring pattern, so harvesting every `{…}` as a binding made the missing name look declared. The first two versions of the scanner reported the broken tree clean, which is why the test now carries an executable proof that it fires on the real shape and not on the fix. Strings, template literals, comments and regex literals are blanked first: these modules emit shell and prompts by the screenful and name constants in prose, and without that step the scan reported twenty files, nineteen of them fiction. A sibling scan confirmed no mock2 import names a symbol its target does not export. The general lesson: a step that is "never a gate" is a step whose failures nobody has to act on — so it needs a check that runs when it does not |
| 114 | Project 44 (N8), the screen accounts one day old | The App access panel showed the accounts it had just created, and one of them read `n8-admin@fixture.invalid` / **`n8-admin-password-123`**. The operator: "please use dynamic secure passwords for all (the current admin one is insecure)" | The platform seeded the spec-declared accounts with the credentials the BUILD MODEL invented, because honouring the spec looked like the respectful thing to do. It is not: the model picks a password the way it picks a placeholder, and the platform already holds a 32-character generated secret per project. The addresses were just as arbitrary — whatever string the model happened to write | **Template:** the spec's ROLES are honoured (that is the thing the build actually decided) and its IDENTITY is not. Each declared role gets `{role}-{project}@fixture.invalid` and the project's own stored secret, and `withScreenLogins` puts those back into the in-memory spec so the checks sign in as the accounts that now exist — nothing is rewritten on disk, so the diff stays clean. A spec naming a REAL address is now SUBSTITUTED rather than skipped: strictly better, because the check runs and no real-domain account is created either way. The requested `{role}@{project}.com` was NOT built, and the reason is the whole point: the auth component's guard is `email NOT LIKE '%@fixture.invalid'`, an exact suffix match, so `admin@n8.com` would count as a real user and take the operator's first-admin slot — the failure that locked an operator out once already (rows 93/94). `admin@n8.fixture.invalid` fails identically, and widening the guard inside a versioned component would reproduce the regression on every project still running the old one. So the role and the project moved into the LOCAL part, where they are just as readable and cost nothing. The general lesson: when a request would breach a boundary, the answer is the readable half of it, not the boundary |
| 115 | The screen check, and Design options one day later | "Please show the screen check, as it's doing work, in the preview sections. Please also fix as the screen check, I had to refresh the page for it show up." | One missing fact behind both halves. The chat polls while a BUILD cycle is live, an ask is streaming, or a rule question is open; a screen check is none of those — it is a browser driving the app for two minutes with no cycle and no ask job. So nothing refreshed, and nothing anywhere said work was happening: a toast said "running", then silence, so a slow capture and a dead one looked identical. Design options, shipped the same week, drives the same browser and had the same hole | **Template + machine check:** one in-memory progress record per project (`screen-job.js`), same shape and lifetime as the existing ask job, exposed both on the chat payload — so `shouldPoll` has a term for it and the findings arrive on their own — and on a light endpoint of its own, so the PREVIEW can watch a capture without pulling the whole chat every few seconds. The banner goes over the preview because that is the surface which is a picture of the app: the thing being looked at is the thing on screen. Expiry is checked on READ rather than by a timer, so a backend restart mid-capture stops reporting by itself; a spinner over work that is not happening is worse than no spinner. Two ratchets: the chat payload and `shouldPoll` are asserted together (the backend can report progress perfectly and the chat still never refresh), and BOTH browser-driving runs must start, finish, and finish-on-failure their record — a progress record on only one of them is the same complaint again, one feature later. Long chat notes also got Copy/Save, because the review that says what is wrong with the app had no way out of the chat at all. The general lesson: work with no progress record is work the product cannot admit it is doing, and "it needs a refresh" is that missing record wearing a different hat |
| 116 | The screen-check polling fix, one release old | "I still had to refresh instead of the findings automatically populating the chat." The same report, on a release whose commit message said it was fixed | The fix was CIRCULAR. It read the progress record off the CHAT payload and added it to `shouldPoll` — but the chat payload only refreshes when that poll is already running, and the poll only runs when the job is visible. A run that starts after mount is never seen, so it is never polled for, so it is never seen. The banner over the preview worked the whole time, on its own independent poll, which is exactly why it looked fixed | **Machine check + template:** the chat watches the job on its OWN heartbeat — a cheap read of an in-memory record, independent of the chat poll, quick while something is happening and slow when nothing is. It works no matter who started the run (the button, the auto-review after a build, another tab), and seeing an active job is what turns the fast chat poll on. A second effect loads once more on the active→idle EDGE, because the findings message is inserted just before the job closes: the poll that observes 'done' is the one that must fetch it. The general lesson: a self-refreshing thing cannot be its own trigger, and "it polls when it knows" is a sentence that should always prompt the question "and how does it find out the first time" |
| 117 | Every screen check ever run | "I want to see the screen changes reflected in the preview area (I want to see it being driven, and for that period to be locked from navigating until it finishes)." The capture runs headless in the BACKEND process, so the operator's preview knew nothing about it and showed a spinner for two minutes | Driving the preview iframe along the same route list was the obvious fix and is the wrong one: a second browser, a different session, possibly different data — a re-enactment of the capture rather than the capture. What the operator asked to see is what the review is about to be written from | **Template:** `captureAppScreens` takes an `onShot` callback and each frame goes straight to the progress record, so the preview shows the ACTUAL JPEG the browser just took, captioned with its path and width. The bytes deliberately do NOT ride the status payload — that is polled every couple of seconds and a 200KB image in it would be paid for on every tick whether or not the picture had changed; the status carries a sequence number and the frame has its own endpoint, refetched only when that number moves. Only the latest frame is kept: this is a viewfinder, not a filmstrip, and the shots are already on their way to the chat where they persist. The overlay covering the frame IS the lock, but the toolbar sits above it, so reload / Desktop / Mobile / Annotate are disabled for the same period — a ratchet counts them, because an overlay that leaves the controls live is a lock in appearance only. `onShot` is wrapped in a try/catch that swallows: a viewfinder that throws would cost the review it was decorating. The general lesson: when someone asks to watch a process, show them the process, not a reconstruction of it |
| 118 | Every design review's findings | Seven numbered defects, each with the fix already written next to it, and the only way to act on any of them was to read the list, decide which mattered, and re-type an instruction into the composer by hand | Every other system note in the chat is STATUS — the base app deployed, three screen accounts were created — and there is nothing to do with them but read them. The findings note is the one that is a TO-DO LIST, and it was rendered identically to the rest | **Template + machine check:** the note carries **Fix these** (distil the findings into a prompt and run it as a Quick update) and **Fix + add a note** (the same, plus whatever the operator knows that the review does not — which screen matters, what to leave alone, a constraint no screenshot can show). The operator's note is appended AFTER the distilled instruction and labelled "this wins where it disagrees with the above", because a note that cannot override the findings is not worth typing. The chat tells a findings note from a status note by the message's opening words, so a ratchet reads the frontend's literal regex out of the source, runs it against a REAL generated message, and asserts it does not match ordinary status notes — the two files cannot drift without a test going red. The invitation is in the message text as well as in the UI, because the message is also what gets copied and saved, and it is suppressed on a clean review so nothing offers a build with nothing to do. The general lesson: a list of defects is a to-do list whatever it is rendered as, and rendering it as prose makes the reader do the transcription |
| 119 | The Fix buttons, the day they shipped | Pressing **Fix these** answered **"Could not turn the message into a prompt — Only Ask answers can be turned into a build prompt"**. The feature did not work at all, on any press | It reused `distill-prompt`, which is explicitly guarded to genuine Ask answers (assistant rows with no cycle) because system notes and build summaries are status, not asks. A findings note is a system note. The guard was right and the reuse was wrong — and it was wrong on a second count nobody would have noticed: the distiller reads the WHOLE message, so it would have discarded any choice the operator made about which findings to fix | **Template + machine check:** fixing findings does not go through the distiller at all. The dialog composes the instruction from the findings that SURVIVED the ticking, carrying the review's own fix wording verbatim — it names variables and breakpoints, and rewording it into a summary is how a precise instruction becomes a vague one. A ratchet asserts `sendFix` never calls `mock2DistillPrompt`. The general lesson: reusing a path with a guard on it means inheriting the guard, and a guard that fires on your new caller is telling you the paths are different |
| 120 | Every design review's chat message | Five kilobytes of `whitespace-pre-wrap` collapsed behind "Show all (4.6k chars)": severity, screen, problem and fix run together on every line, six thumbnails, one button. Seven separate decisions rendered as one paragraph | The review already produces STRUCTURE — severity, screens, issue, fix, plus axe rules and adherence codes — and `reviewChatMessage` flattened all of it into prose because prose is what a chat message is. Nothing downstream could see the findings as findings, so the operator was the parser | **Template + machine check:** the message is parsed back into the list it is (`parseFindings`), rendered as a row per finding with severity and screen as chips, and fed to a Fix dialog with a tick per finding, a note field and image attachment. Parsing rather than carrying a second structured copy, because the body is the durable record — it is what is stored, copied, saved and re-read — and a parallel copy is a second thing to keep in sync. It is reading our own generator, not scraping somebody else's output, and the ratchet runs the parser against a REAL generated message for every case: a hand-typed fixture would keep passing after the format moved, which is the one bug it exists to prevent. Two things the tests caught that reading would not: the button prompt was being parsed as a finding after its own label gained an ellipsis, and the adherence line has a SECOND form (`**Visual drift** — …`) that only appears when drift is severe, so matching the calm one sent the alarming one to the bottom of the card as loose text. The general lesson: when the product flattens its own structure into prose, it has made the reader do the parsing — and then it cannot offer them a single button |
| 121 | The chat's scroll, on every findings message | The findings landed and the view sat above them: "please auto scroll the chat to the bottom" | Top-anchoring is deliberate and correct — it exists so a long Ask ANSWER reads from its first line — and it had been applied to every message kind. A findings note is not prose: it ends in six thumbnails and the one button the whole message is asking you to press, and anchoring to its top put every one of them below the fold | **Template:** top-anchoring is now scoped to what it was written for (`assistant` messages). Everything else lands at the bottom, and a screen check that finishes also re-arms the follow, since a capture takes two minutes and the operator has very likely scrolled around while waiting. The general lesson: a scroll rule justified by one message type will be wrong for the others, and the justification is where to look for the scope |
| 122 | Every design review ever run | Every finding the review has produced is about CHROME — footer links, the theme toggle, native select boxes, contrast. Not one is about a screen being hard to use, because it signs in as the capture account and screenshots an app holding one note called "test" | An empty app cannot be designed. The evidence is two notes apps built from a byte-identical starting instruction: the one that came out well had a list showing a PINNED note with 3 to-dos beside one from 40 minutes ago and one from a week ago, and a to-do carrying a MUTED reminder. Somebody had to decide what pinned looks like next to unpinned, and what "1w ago" looks like next to "40m ago" — decisions that only get made when those cases exist. The other app's home screen was 800px of nothing, and that empty middle was never a style choice, it was an unmade decision | **Template + machine check:** a demo-content seeder that fills the app through its OWN API, signed in as the `@fixture.invalid` capture account and nothing else ("not the super admin or any users"). Going through the API rather than writing rows keeps validation, defaults, derived fields and audit entries the app's — the same principle as the first-admin bootstrap — and it exercises the real write path, so a broken create endpoint surfaces here rather than in a screenshot two builds later. The prompt asks for the variety that FORCES decisions: one very long title and one two-word one, one with nine children and several with none, both sides of every flag, and a spread in time so relative timestamps show their range. Every fence is a thing the seeder must be unable to do rather than a thing the model is unlikely to ask for, because it runs unattended against a live app: no DELETE, no path that leaves the origin, no endpoint outside the route list grepped from the app's own source, and an unresolvable `$1.id` drops the CALL rather than sending the literal — a row carrying "$1.id" where an id belongs looks seeded and is not. The general lesson: a reviewer that can only ever see an empty screen will only ever review the frame around it |
| 123 | Every vague quick update, and the classifier written to catch them | The operator asked for a classifier that pushes back on vague requests. The obvious implementation — flag the short ones — would have been actively harmful, and their own build history proves it | The good project's median instruction was 174 characters and TWO of its 24 builds were literally "Please fix" and "Please update". Both succeeded, because the build before them was "Please complete the biometric login" — the conversation carried the referent. The worse project's median was 865 characters and its vaguest asks were its longest. A length heuristic flags the project that is going well and passes the one that is not | **Template + machine check:** the test is COULD YOU TELL AFTERWARDS WHETHER IT WAS DONE, and the existing quick-lane pre-pass was already computing it as `brief.acceptance` ("concrete checks that would prove it works") and throwing it away — so the classifier costs no extra model call to decide. A CONTINUATION is never challenged: short, bare, and with a previous turn or a just-failed build to refer to. An image is specificity — they showed instead of telling. No pre-pass means fail QUIET, because the cost of a missed clarification is one ordinary build and the cost of a false one is a decision the operator did not need, every time, until they stop reading the card. The look is narrowed to the pages the request NAMES (a question about /admin must not cost a capture of /login, /profile and home), and naming no page at all is itself the first thing to ask about. It offers 2–3 PRESSED options rather than questions — the 8-question interview removed four days earlier is the thing this must not become — always keeps "Build it anyway" first-class, and fires once per request. Ratchets hold the ordering (it must be the FIRST card, not the third — decomposing a request nobody can check yet is answering a question before it was asked) and the fail-open catch (a clarifier that can break a build by failing is a gate wearing a helper's clothes). The general lesson: when you are about to build a filter, find the data that says what it would have filtered |
| 124 | Project 44 (N9), and every project since the baseline checks shipped | "I was able to get some changes done in the top nav bar but it seemed to be blocked during larger changes (say if asking to hide it or move it to the side)." Padding tweaks landed; hiding the nav or moving it to a sidebar did not | Nothing refused the edit — `public/app-shell.html` is app-owned and a build may rewrite it. What blocked it was the platform's OWN baseline check, which asserted `expect_visible: 'header'`, `expect_visible: '.theme-toggle'` and `expect_visible: '[data-legal-footer]'` on the default screen. A padding change leaves the header visible and passes; hiding it, relocating it, or folding its controls into a menu fails, so the build went red for doing exactly what it was asked. The check was asserting SHAPE where it meant to assert a GUARANTEE: what the platform promises is that the theme control, the legal pages and the admin route stay REACHABLE, and "there is a visible header" was one implementation of that promise which the check had frozen | **Template + machine check:** a shell contract at `state/shell.json` (`nav: top\|side\|hidden`, `navSelector`, `menuOpener`) that the baseline checks READ. A sidebar is a declared layout; a control behind a menu is fine because the check clicks `menuOpener` before asserting — a check that cannot open a menu is a check that has banned menus, which is most of what "move it to the side" means on a phone. What no contract can declare away: the theme control, the legal footer and the admin route must each still be reachable in at most one interaction, and an app that blanks those selectors gets the defaults back. An app that declares nothing produces byte-identical steps to what shipped before, asserted directly, so this is a fix rather than a migration. The other half was the missing instruction: a build told "never edit platform-owned files" and then failed by a check it could not see had no legitimate path at all, so it either fought the gate or gave up — both happened. The runner prompt now names the file and says it must be written in the SAME change. The general lesson: a check that pins the implementation of a guarantee will eventually forbid the feature it was protecting |
| 125 | Project 44, and one finish in seven across every build history | The build finished with **"…removed the To-dos inner scrollbar…"**, an acceptance sentence promising "no scrollbar beside the Note/To-dos content", and `acceptance_ids: ["note-create-and-edit", "notes-list-loads"]` — two checks that assert nothing whatsoever about a scrollbar. The scrollbar was still on the screen. Nothing contradicted it, and the operator found it by looking | The gates catch horizontal overflow, dead controls and design drift; none of them catch "the thing you said you removed is still there". This is not a rare shape: across 86 real finish payloads in six histories, 12 summaries carry a verb-shaped removal claim and **eight of the twelve declared no acceptance ids at all**. The build was not lying — it had set `#panel-todos { overflow: visible }` and believed that was the scrollbar in question. It had no honest way to prove it either way, because a scrollbar is not an element and `expect_absent` cannot reach one | **Template + machine check:** `finish(removals: [{what, check_id}])`, where the named check must EXIST and must contain a step that could fail while the thing is still present (`expect_absent`, `expect_no_scroll`, `expect_text`) — a removal "verified" by `expect_visible` passes either way. Those ids are then forced into the smoke run, because confirming a check exists and never running it moves the unverified claim one step later rather than closing it. It rejects at most ONCE (repeated rejection auto-halts a cycle — row 107); the second finish ships and tells the OPERATOR instead, since a claim nobody checked is exactly what they would otherwise discover by looking a day later. The detector is deliberately conservative and was tuned against the real corpus, not imagined: it demands nothing for code-only removals ("deleted the dead unwrapped-atob public/webauthn.js" — nothing to see in a browser) or for style tweaks ("dropped the page's large bottom padding" — no element vanished, so any check written for it could not fail). Three passes were needed to get the grammar right, and each was a wrong answer on real data: "the double-tap helper PARAGRAPH" is a paragraph, not a helper function; "the PAGE's large bottom padding" is about padding despite containing "page", which is what forced the head-noun rule; and "the transition ON THE CARD" is about the transition, so a trailing prepositional phrase had to be stripped before the head was taken, or a style tweak started demanding a UI check. The new `expect_no_scroll` step is what makes the demand fair, and it produced the two defects worth recording, BOTH found by measuring a real browser rather than by reading the code. First: `scrollHeight - clientHeight` alone reports 120 vs 60 on an `overflow: visible` box that shows no scrollbar — which is *literally the fix the motivating build applied*, so the check would have told a build that had correctly removed the scrollbar that it was still there. Second, and worse: the root element's overflow propagates to the VIEWPORT, so `body`, `html` and `:root` all report `overflow-y: visible` on a page that `window.scrollTo(0, 500)` really moves 500px — meaning `{ "expect_no_scroll": "body" }` was accepted as falsifying evidence while being incapable of ever failing, the precise thing this feature rejects, smuggled in through its own new step. Both are now measured against `document.scrollingElement` with the propagated overflow, and the ground truth in the verification harness is REAL WHEEL INPUT, not `scrollTop` — an `overflow:hidden` box is programmatically scrollable while showing no scrollbar and ignoring the wheel, so the obvious probe called three correct verdicts wrong. The general lesson: a claim the product cannot falsify is a claim it should not accept — and before demanding proof of something, check that the proof can be written at all |
| 126 | Every build on an app with no first administrator | Project 46 build 129 reported 5 of 7 checks as `locator.waitFor: Timeout 5000ms exceeded`, including the platform's own baseline. The build had done nothing wrong, and the operator went looking through a diff that was fine | The first administrator belongs to the OPERATOR, so the auth guard is `email NOT LIKE '%@fixture.invalid'` and the seeded fixtures deliberately do not satisfy it. Until a real admin exists, `/login` renders the create-administrator form and HIDES the sign-in form — so a check declaring a role has no form to fill and a baseline check asserting a sign-in selector times out on an element that is legitimately absent. Row 112 fixed the "the user does not exist" half by seeding the rows straight into Postgres; this is the other half, and the two look identical in the report. The gate could not tell "failed" from "not yet possible", and only one of those is the build's fault | **Template + machine check:** the smoke gate reclassifies session checks as NOT POSSIBLE when the app itself answers `canCreateSuperadmin: true`, and says so in one sentence naming the cause and the way out. Three properties keep it from becoming an excuse generator, each with a test: it fires only on a POSITIVELY confirmed answer (a non-200, an unreachable app, a malformed body and a missing field all leave the report exactly as it was — a probe that fails must never turn a red build green); it only touches checks that NEED A SESSION, so an anonymous check that fails still fails; and one genuinely failed check keeps the whole gate red, because first-run explains part of a report and never all of it by fiat. The probe is LAZY — run only when something already failed — so the green path pays nothing, and it is one curl rather than the access panel's full psql roster. The pass it produces carries `notYetPossible` out with it: the gate now goes green on a first-run app, and a SILENT green would be worse than the red it replaces. The general lesson: a report that cannot distinguish "broken" from "not yet possible" will spend the operator's attention on the wrong one every time |
| 127 | Six features shipped in a week, none observed | Asked "did demo content run on this build?", there was no way to find out but to scroll a chat hunting for a note that may never have been written. Every one of the six was unit-tested; not one had been seen working on a live build | A feature that DECLINES to act writes nothing, so silence meant both "it ran and correctly found nothing to do" and "it never ran, is switched off, or was never wired" — and those are precisely the two answers that matter when validating something new. Reading the chat notes could not close the gap either: they are written for an operator mid-build, interleaved with everything else, phrased differently by each feature, and only emitted on the interesting branch. Wiring this up immediately found one: **demo content is called from exactly one route — the operator's button — and never runs during a build at all**, which is why every design review's findings were still about chrome | **Template + machine check:** one ledger per build, one line per declared feature, INCLUDING the ones that did nothing and the ones never reached. The skip rows are the entire point; a `fired` row was always discoverable somehow, "it ran and decided not to, because X" was not discoverable at all. Keyed by PROJECT rather than by cycle, because the clarifier and the pre-pass run before a cycle exists and a cycle key would silently drop exactly the two features hardest to observe. A feature must be declared in a registry to be recorded, so a typo is an absence rather than a plausible-looking row, and the registry doubles as the first written answer to "what is the platform actually doing to my build". Last-write-wins EXCEPT that a `failed` is never overwritten by a later success — a feature that threw and then partially recovered is a thing to look at, not to forget. Ratchets assert the SKIP branches are wired (recording only when a feature acts rebuilds the exact blind spot) and that the ledger is written where the build reports and cannot throw. The general lesson: instrumentation that only fires on the interesting branch cannot answer the question you built it for |
| 128 | Every project since the audit shipped | The stage indicator draws Concept → **Define** → Build → Run. Define really runs: the audit raises rule questions, the editor taps an answer, and it is committed to `state/rules.md` with a hash-chained change record — sign-off #2, one of the two human approvals that gate all code. From the moment it was given, the only way to read it back was a terminal into the container | Not a missing feature — a finished feature with no read side. `conceptStageInfo` also advances concept → build on design approval and never reports 'define', so the indicator drew a step whose one artefact was invisible and whose completion it could not represent. I initially diagnosed this as "Define is not reachable" and was wrong; it is reachable, inside the audited Full build lane, and unreadable | **Template + machine check:** a Rules panel, and it serves BOTH sources. `rules.md` is written only by the audited Full build lane, so the majority of projects — driven from the chat's Quick update — have an empty one while still being governed by `CRUD_RULES_PACK`, the floor injected into every quick/MVP build precisely because the interview was skipped. Serving only the confirmed set would tell most projects "you have no rules" while eight are being enforced, which is the more dangerous direction of wrong. The two are labelled and never merged: a baseline rule is a platform default, a confirmed rule is a person signing off on their domain, and blurring them makes the sign-off look like a setting. The baseline is PARSED from the pack's own text rather than re-typed, so there is one definition — and that parse carried the bug worth recording: with the `m` flag `$` matches at every line end, so a lazy body-match stopped at the first newline and every rule lost all but its opening clause, silently, because a truncated sentence still reads like a rule. Three more things refuse to lie: an unreachable container reports "could not be read", not "no rules"; the empty message distinguishes "the audit ran and you answered nothing" from "the audit never ran"; and the stage indicator only ever GAINS detail (with no count supplied it is byte-identical, and it never moves a project backwards). The general lesson: a workflow step whose artefact you cannot read is a step the product cannot prove it took |
| 129 | Project 47, three cycles and $10.28, and the shell contract five days old | Three build cycles, identical outcome each time. Cycles 2 and 3 both concluded "no product-code change was needed", shipped an empty diff, and failed on the same two platform baseline checks: `platform-baseline-admin-reachable` on `header.topbar`, and `platform-baseline-signin-legal` on `[data-legal-footer] .legal-link` | THREE SEPARATE DEFECTS, one of them mine. (1) `shellAdminSteps` applied the app's OWN `navSelector` to `/admin` — the PLATFORM's console. The build replaced the base header with per-screen topbars, correctly declared `{"nav":"top","navSelector":"header.topbar"}`, reasoned its way to the right decision about the console ("that entire surface already exists at /admin … so I correctly link rather than rebuild") and was failed for it. The function's own comment said the guarantee was "that an administrator can GET THERE and use it, not that a `<header>` is on the page" and the next line asserted the header anyway. Invisible until an app declared something specific, because the default `header` matches the platform console by coincidence — so the feature's whole purpose was also its blind spot. (2) `expect_visible [data-legal-footer] .legal-link: Timeout 5000ms exceeded` withheld the one fact that mattered: the slot WAS present and empty, because `platform.js` mounts those links at runtime and the build's rewrite of `login.html` had stopped it running. (3) Nothing said "every failure here is the platform's own check", so re-running was always the obvious next move | **Three fixes + machine checks.** The admin route no longer consults the app's layout declaration at all: an element INSIDE the console proves the console rendered, which is the whole of the guarantee, and the test asserts it for every layout rather than only the one that broke. A failing `expect_visible` now BISECTS its own selector — probing each prefix and reporting the deepest one present — so "the container is missing" and "the container is empty" stop reading identically; the splitter tracks bracket/quote depth (`split(' ')` would cut `[data-x="a b"]` in half and probe two invalid selectors) and never emits a prefix ending in a combinator (`div >` matches nothing for reasons that have nothing to do with the page), while still emitting the left side so `div` remains probeable. Verified in a real browser against both shapes of the project-47 markup. Named causes are a bonus on top of the bisection, not the mechanism — a table of known baselines would have fixed one report and nothing else. And a smoke failure whose every entry is a baseline now says so, says whether the build changed any product code, and carries each failure's detail verbatim; the "product code" filter excludes `state/` and the build-id/sw.js stamps, because cycles 2 and 3 touched only those and counting them would make the futility claim never fire on the exact shape that motivated it. Two of my own ratchets broke on this change by pinning a literal (an exact steps array, an exact return tuple) rather than the property, and both were rewritten — a test that fails for a change it does not care about spends the same attention as the bug. The general lesson: when a check asserts a guarantee, make sure it asserts it against the thing that owns the guarantee — and when it fails, the report has to distinguish what the reader is supposed to do next |
| 130 | Project 47, and every gate added since project 34 | "I feel like the platform itself is getting better / more user friendly / nice features; but the builds it is producing are getting worse (even with good guide direction)." The notes app shipped a button reading **"Edit note title/body"**, a second redundant path to /admin, and screens the operator described as looking like email | THE GATES WERE WRITING THE UI, and project 47's own log documents the whole chain. Action parity's drift grep required EVERY significant word of a contract label on ONE LINE of source — for "Edit note title/body" that is edit AND note AND title AND body together, which no designed control ever satisfies. The build had already built `More actions → Edit`, was told the action appeared NOWHERE, and reasoned: *"I have it via More actions → Edit, but the checker likely wants an explicit id/label. Let me add explicit affordances"* — then reported *"Both contract actions now have visible, labelled controls … (plus the gear icon with matching title)"*. So the gate did not catch a missing feature; it dictated two button labels and added a redundant control. It also accepted a `hidden` element as proof, which the build duly shipped (`<p id="admin-settings-hint" hidden>`) — simultaneously too strict about WORDING and too weak about VISIBILITY, both errors pushing toward markup written for the detector. And the reviewer that should have caught the result could not see it: `MAX_PATHS = 4` ROUTES, while the generated apps are SPAs whose note detail and note editor both live inside `/`, so those two screens had never been photographed by any review on any build | **Two fixes + a written audit (`docs/gate-audit.md`), and the audit is the durable part.** Parity now matches the action's two-word CORE (the verb and what it acts on — what a real control can carry), rejects and NAMES hidden-only matches, and its rejection message says the contract is a list of CAPABILITIES rather than button copy, names `More actions → Edit` as passing, and forbids printing the contract wording. The review now discovers `section[data-screen]` panels — the mockup's OWN convention — activates each the way the app does, shoots it, and restores the route so the desktop shot matches the mobile one; verified in a real browser against project 47's exact markup shape. The audit asks of all fifteen gates the question none of them was ever asked: *what is the cheapest way to pass this, and what does the app look like when a build does that?* Its finding is structural and worse than any single gate: **every gate is ADDITIVE** — each says add a class, a control, a badge, a check, an id — and **not one can ever say "this screen has too much on it"**. An optimiser under fifteen additive constraints and zero subtractive ones produces exactly the dense, form-like app that was reported. Two further gates fail the same test on inspection: the coverage gate is satisfiable by adding a path glob to an existing check (project 47 did this and said so — "no product-code change was needed"), and `acceptance_ids` rewards declaring FEWER ids. The general lesson: a gate is a specification of the product whether or not anyone wrote it as one, and fifteen local checks nobody read together are a design nobody made |
| 131 | ProxyPilot, classic build view | The classic view could not scroll — everything below the fold was clipped, so a blocked build's status panel (the ONE surface that explains a block and offers Resume) was unreachable; in Flightdeck the same blocked state was invisible altogether | Two halves. The classic view was wrapped in a plain `space-y-2` block inside the Chat tab's `overflow-hidden` flex parent — the #33 shape again (a scroll area whose ancestor chain is missing `min-h-0 flex`), this time one wrapper ABOVE the component that had it right. And Flightdeck has no Build panel at all, so `awaiting_admin` only manifested as the composer quietly relabeling itself | **Rule:** every wrapper between an `overflow-hidden` flex parent and a scrolling child is a `min-h-0 flex` column (the #33 lesson now applied to intermediate wrappers, not just the scroll element); a build state that stops the world must be VISIBLE on every surface the operator drives from — Flightdeck's top bar carries a Blocked chip and the chat carries the reason + how to resume |
| 132 | ProxyPilot, guided setup panel | The setup panel's X appeared to do nothing — and once a dismiss did land there was no way to ever see the checklist again | The dismiss handler awaited the server round-trip before hiding, and that save also attempts to push app context into the project container (up to ~30s of best-effort container work) — a UI toggle serialized behind slow I/O. Dismissal was also the only state: no surface showed a dismissed checklist, so hiding it was irreversible in practice | **Rule + template:** a hide/show toggle updates the UI optimistically and persists behind itself (rolling back on failure); anything dismissible needs a durable home — the setup checklist now lives on Details → Overview permanently, with the un-dismiss control next to it |
| 133 | ProxyPilot, Flightdeck chat header | The Build chat header overflowed its ~380px pane on DESKTOP (labels + count squeezed into ellipsis soup), and the workspace toggle read "Dev" in both positions — a switch whose label never said what the other side was | The header buttons hid their labels below `sm:` only, but the Flightdeck chat pane is ~380px wide at every viewport — breakpoint-gated compaction keyed on the SCREEN when the constraint was the PANE. The toggle stamped one static word on a two-state control | **Template:** the header buttons are icon-only at every width (title/aria-label carry the words — the mobile presentation was already correct, it just wasn't applied); the toggle names the state it is IN ("Chat"/"Code"), so either position tells you what the other one is |
| 134 | ProxyPilot, blocked-build banner (#131's fix) | The banner added to surface a blocked build itself trapped the operator: a paragraphs-long halt reason rendered unbounded inside the chat card's fixed flex column, filling the phone screen and pushing the composer — the Resume control — off it, with nothing scrollable ("blocked is stuck/can't scroll so I am unable to do anything about it") | Server-authored text of unbounded length was dropped into a `shrink-0` block inside an `overflow-hidden` flex layout; the fix for "the blocker is invisible" never asked how big the blocker's own explanation could get | **Rule + template:** any server-authored text rendered into a fixed-height layout gets its own scroll box (`max-h-* overflow-y-auto`); a banner that names a required action carries that action as its own button — the way out must never depend on scrolling past the thing that is blocking you |
| 135 | Project 47 request 141 (resumes) | Resuming a build halted with "work complete but finish rejected" burned ~$1 per press and re-blocked, forever: the resume replayed the ORIGINAL instruction against a tree where the work was already committed, the new cycle verified everything present and changed nothing — and then had no honest exit, because a finish summary naming the files it VERIFIED tripped the over-claim rule (empty diff ⇒ every named file is "not changed this cycle"), leaving halt as the only truthful call | The completion contract assumed every cycle changes something; "the requested work already exists" had no legitimate ending, and neither the finish nor the halt tool description acknowledged the state | **Rule + machine check:** `verificationOnlyFinish` (pure, tested) — a finish with an empty product-code diff whose summary explicitly says the work is already implemented passes over-claim (the named files are the evidence trail, not an authorship claim); the empty-diff rejection message teaches the phrasing; both tool descriptions route "already done" to finish, not halt; a source-contract test pins the wiring |
| 136 | ProxyPilot, Design options | The feature was reported as "only on mobile" — it rendered on desktop too, but as a ghost-variant button alone at the bottom of the Flightdeck chat pane, where it read as inert footer text; operators never found it | A borderless, muted button placed after the content it relates to has no affordance on a wide layout; "it renders" is not "it is discoverable" | **Rule + template:** chat-lane actions that START something are outline-variant minimum, never ghost; Design options now opens an explicit screen picker (all screens — uncapped, every route and every data-screen panel view — or a chosen set), and the preview Annotate pins carry a per-pin Fix/Options toggle so the feature is reachable from where the looking actually happens |
| 137 | Project 47 requests 140/171 | One genuinely broken check became a WALL: after notes-todo-add first failed, a pure-CSS layout change deployed fine and was marked failed by that same unrelated check — and so would every later build, whatever its diff. $10.29 sat on an open request whose app was live and working | The smoke gate had no memory: it could not tell "your change broke this" from "this was already red before you started" (the baseline-only path existed for PLATFORM checks, but an app-owned red failed everything) | **Machine check:** `preexistingSmokeVerdict` (pure, tested) — a failed smoke run ships-with-warning when every failing check was already red before the cycle (parsed from the latest prior smoke-failed cycle) or is a platform baseline; the ORIGIN cycle still fails (no prior record names it), a cycle that declared the red check as its own acceptance fails on it, and an unreachable app or failed db connector is never pre-existing. The still-red set rides the chat, a dedupe flag, and the event log |
| 138 | Project 47 cycle 587 | With the finish-guard live, the model STILL lost a full cycle ($1.17) to serialization flakiness: three finish calls in a row arrived with the parameters glued into `summary`, and re-issuing was a coin flip (the next cycle's call was clean) | The only remedy on offer was "re-call finish" — through the exact channel that was garbling the call | **Template + machine check:** the file fallback — the malformed rejection and the escalated diagnostic teach writing the same fields to `state/finish.json` (the file tools demonstrably work; the whole build was made with them); the runner hydrates a malformed/incomplete finish from the file and consumes it before the diff read, so it can never leak into a checkpoint. `parseFinishFile` is pure and tested |
| 139 | Project 48 (initial build) | The operator's phone said "Build failed" about a build that had DEPLOYED and concluded as pending-operator-verification; the UI showed no failure, no button, and the queued design-fix build sat "Up next" forever | Three independent gaps behind one experience: buildCycleNotification had no pending_verification case so the outcome fell through to the "Build failed" wording; the verification checklist rendered only in the classic Build panel (Flightdeck/phone had no way to even SEE the state, let alone confirm it); and build-queue.js's "the close hook re-drains" was a comment with no code — nothing re-drained the queue when a cycle concluded | **Rule + machine check:** a pending_verification notification is a calm info-level completion ("not failed, waiting for you" — tested); VerificationChecklist mounts in the Flightdeck chat (self-hiding, scroll-boxed per row 134); `queueMayAdvancePast` (pure, tested) gates a REAL close-hook drain in scheduleJobCleanup plus a self-healing drain on the latest-cycle poll, so a wedged queue clears the moment anyone looks at the project |
| 140 | Project 49+ (operator wedge) | An API hiccup mid-build left the platform STUCK: the queue said "Building now:" over an idle chat, no Interrupt (no active cycle client-side), no error, nothing to click — the operator's only option was to wait or file a report | Three gaps stacked: the model-call abort deadline scales with requested output (~106 min at a 128k budget) so a silently-dropped stream connection just sat there; a build start that died between marking the queue row 'started' and the cycle surviving left an orphan row nothing ever reconciled; and no surface offered recovery because no component knew the build was dead | **Machine check + template:** a stream idle watchdog in model-client (5 min without bytes aborts, surfaced as a stall, not a timeout-by-deadline); `buildStallVerdict` (pure, tested) drives a 60s stall sweep that stops silent running cycles (interrupted + lock released + chat note) and requeues orphaned started rows once (`requeueOrphanedStartedBuilds`); the chat shows a stall banner after ~3 min of event silence with one-click "Restart build" (POST /build-restart force-stops and resumes from the last checkpoint; 409 if the build proves alive) |
| 141 | Project 49 (queue wedge, round 2) | With the stall watchdog live, "Building now:" STILL sat over an idle chat — and Restart honestly answered "nothing to restart", because nothing was wrong by its rules: the build had finished and deployed as pending-operator-verification | The queue settle trusted only the REQUEST's status, and a pending-verification finish keeps the request open BY DESIGN (a failed live check resumes it as a new segment) — so the 'started' row never settled, the drain reported busy forever, the orphan check counted awaiting_user as "live", and the poll self-heal was gated on "no started row". Four guards, all reading the one signal that deliberately does not move | **Machine check:** `queueRowConcluded` (pure, tested) — a queue slot settles on CYCLE evidence (latest segment not active AND queueMayAdvancePast), not request status; settleStartedBuilds applies it, and the poll self-heal drains whenever any queue row sits behind a passable cycle |
| 142 | ProxyPilot, Flightdeck chat (rows 134/139 repeat) | The stall banner + build queue + verification checklist stacked BELOW the composer overflowed the fixed phone column with nothing scrollable — the banner's own Restart button was half off-screen, and the checklist that explained the "stuck" build was entirely off-screen | The same class as rows 134/139, reintroduced by the fix for row 140: each new piece of state was appended to a `shrink-0` footer inside an `overflow-hidden` flex column, and each looked small alone; nobody summed the stack | **Template:** live "now" state (build queue, stall banner, verification checklist) renders INSIDE the chat scroll container (ChatMessageList `footer` slot), never below the composer — the composer column may contain only the composer and its bounded action rows |
| 143 | ProxyPilot, Flightdeck chat (row 142, violated same-day) | Row 142's rule ("live state renders inside the chat scroll, never below the composer") was broken by the very next features: the Redo and extra-effort cards were added to the composer column, and on a phone the boost card's own Arm buttons rendered below the fold with no scroll — the operator could open the card but not use it | A rule recorded as prose guards nothing: each new card was added where the previous cards visually lived, and the column has no overflow so every addition silently gambles on total height | **Template (structural):** ALL conditional content — banners AND interactive cards (split/clarify/suggest/design-options/redo/boost) — now renders in the ChatMessageList `footer` slot inside the scroll container; the composer column contains only the textarea, attachments, action row, and a one-line status note, and a card that opens scrolls itself into view |
| 144 | ProxyPilot, Flightdeck chat (blank page) | Row 143's restructure BLANKED the whole dashboard: the new scroll-into-view effect read `optionsPicker` ~350 lines before its declaration — a temporal-dead-zone ReferenceError on every BuildChat render, and React unmounts the tree | Vite builds don't evaluate: a use-before-declare inside a component body compiles clean and only throws at render; the frontend has no linter, so nothing static stood between the green build and the crash | **Rule + template:** any derived value (like `anyCardOpen`) is declared IMMEDIATELY AFTER the last state it reads, never in an earlier "effects" region; state that multiple regions read is declared in the component's top state block. Follow-up parked in docs/known-issues.md: add ESLint (no-use-before-define) as the machine check |
