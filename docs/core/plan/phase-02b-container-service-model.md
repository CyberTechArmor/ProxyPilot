<!-- New phase added after Phase 2 to support container-as-service modeling -->
<!-- Index: docs/core/plan/README.md -->

## Phase 2b: Container-as-Service Data Model + Multiple HTTP Routes

**Goal:** Re-shape the `services` schema so a service represents one logical workload (typically an LXC or Docker container) that can expose **multiple HTTP routes** at once. Today the operator has to create one ProxyPilot row per (domain, path_prefix) → port mapping, even when those mappings all point at different ports of the same container. After this phase, a single service holds one or more `service_http_routes` rows, the wizard supports an LXC container picker that auto-populates the target IP, and the merged Caddy generator extends to take routes (not services) as its input. This is the prerequisite for Phase 2c, which will add non-HTTP (TCP/UDP/TLS-SNI) port forwards via the `caddy-l4` plugin.

**Scope note:** Frontend + backend only. No new system dependencies, no nftables, no Postgres, no Infisical, no Caddy plugin install. The Caddy reload pipeline from Phase 2 stays. The data model is reshaped in place via an idempotent migration; existing Phase 2 services migrate transparently with zero data loss. nftables / kernel-space port forwards are explicitly out of scope and remain Phase 10 territory.

**Files to edit:**
```
admin/backend/src/db.js                          # New service_http_routes table; idempotent migration that splits existing services rows into (services, service_http_routes) pairs
admin/backend/src/routes/services.js             # Refactor every endpoint that touches (domain, path_prefix, target, port) to operate on routes; add per-service routes CRUD; extend buildDomainCaddyConfig to take a list of (service, route) pairs; LXC IP discovery on create + Refresh action
admin/backend/src/routes/lxc.js                  # Add a `GET /api/lxc/containers/with-ip` helper that returns name + IP + status, used by the Add Service wizard's LXC dropdown
admin/frontend/src/pages/Dashboard.jsx           # Add Service wizard becomes Pick kind → if container, pick LXC → add 1+ routes; service detail page gains a Routes section with add/edit/remove inline; existing Phase 2 banner/header/warning logic generalizes to "routes that share a domain"
admin/frontend/src/lib/api.js                    # Add createRoute, updateRoute, deleteRoute, getLxcContainersWithIp; document that services now nest routes
```

**Deliverables:**

- **Schema migration (`db.js`):**
  - Add `kind` column to `services`: `TEXT NOT NULL CHECK(kind IN ('static_site', 'container_service'))`. Default `'container_service'` for any pre-Phase-2b row whose `type` is not `'static'`; default `'static_site'` when `type='static'`. Backfill in the same migration.
  - Add `runtime` column to `services`: `TEXT CHECK(runtime IN ('lxc', 'docker', NULL))`. Backfill from the existing `type` column: `type='docker'` → `runtime='docker'`; the existing `'proxy'` type and any LXC-pointed services default to `NULL` and the operator can set them later via the wizard.
  - Add `target_ip` and `lxc_container_name` columns to `services`. Backfill `target_ip` from the existing `target` column for non-static services. `lxc_container_name` stays NULL until an operator explicitly picks an LXC container in the wizard (cannot be backfilled — the existing `target` is just an IP, not a container name).
  - New table `service_http_routes (id TEXT PRIMARY KEY, service_id TEXT NOT NULL, domain TEXT NOT NULL, path_prefix TEXT NOT NULL DEFAULT '/', target_port INTEGER, websocket_enabled INTEGER DEFAULT 0, ssl_enabled INTEGER DEFAULT 1, force_https INTEGER DEFAULT 1, max_upload_size TEXT DEFAULT '1G', created_at TEXT DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (service_id) REFERENCES services(id), UNIQUE(domain, path_prefix))`. The `(domain, path_prefix)` UNIQUE constraint moves from `services` to this new table.
  - Idempotent split migration: detect via `sqlite_master.sql` whether `service_http_routes` exists. If not, create it and INSERT one row per existing service carrying its `(domain, path_prefix, port AS target_port, websocket_enabled, ssl_enabled, force_https, max_upload_size)`. Then drop the now-redundant columns from `services` (`domain`, `path_prefix`, `port`, `websocket_enabled`, `ssl_enabled`, `force_https`, `max_upload_size`) via the same table-rebuild pattern Phase 2's `migrateServicesUniqueConstraint` used. Running twice is a no-op.
  - SSL flags now live on routes, not services. The Phase 2 all-or-nothing SSL check generalizes to: "all routes whose `domain` matches must agree on `ssl_enabled` and `force_https`". The merged site address still has one stance per domain, just sourced from any route on that domain.

- **Per-domain Caddy generator (`services.js`):**
  - Refactor `buildDomainCaddyConfig(servicesList, domain)` to `buildDomainCaddyConfig(routesList, domain)` where each entry in `routesList` is the joined `(service, route)` pair. The pure function still emits one merged site block, sorted by `length(path_prefix) DESC`, with `handle_path /prefix*` blocks per prefixed route and one `handle { ... }` for the root-scoped route. Each route's `target_port` combines with its parent service's `target_ip` to form the `reverse_proxy` target.
  - `regenerateDomainCaddyConfig(db, domain)` now SELECTs from `service_http_routes` JOINed to `services` (excluding `is_admin = 1` services) and hands the joined rows to `buildDomainCaddyConfig`. All nine Phase 2 call sites continue to call this helper — only the SQL inside it changes.
  - Static-site routes (`kind='static_site'`) still emit `root * <local path>` + `file_server` + `try_files`, with the path coming from the parent service's `data_dir` instead of `target_ip` + `target_port`.
  - Per-route validation: refuse to emit if any sibling routes for the same domain disagree on `ssl_enabled` or `force_https` (defense-in-depth, matches Phase 2's posture).

- **Routes CRUD (`services.js`, new endpoints):**
  - `GET /api/services/:id/routes` — list every route for one service.
  - `POST /api/services/:id/routes` — add a route. Validates `(domain, path_prefix)` uniqueness, validates SSL consistency against any pre-existing routes on the same domain (across all services), regenerates the merged Caddy config for the affected domain, validates + reloads, rolls back on failure (same backup-then-revert pattern as Phase 2's update endpoint).
  - `PUT /api/services/:id/routes/:routeId` — edit a route. If `domain` changes, regenerate both old and new domain's merged files.
  - `DELETE /api/services/:id/routes/:routeId` — remove one route. Regenerates the merged file for the route's domain (which may shrink or unlink). The parent service stays even if its last route is removed — the operator can add routes back without recreating the service. The existing service-level DELETE still cascades to all routes.

- **LXC integration (`lxc.js` + `services.js`):**
  - Add `GET /api/lxc/containers/with-ip` returning `[{name, status, ipv4, ipv6}]` for every Incus container the dashboard already manages. The wizard's LXC dropdown calls this on open.
  - On service create, when the operator picks an LXC container, ProxyPilot reads the IP from the dropdown selection and stores it in `services.target_ip` + `services.lxc_container_name`. **No live re-resolution at config-generation time** — the cached IP is used.
  - New endpoint `POST /api/services/:id/refresh-ip` — re-queries Incus for the cached `lxc_container_name`, updates `services.target_ip` if it changed, regenerates the merged Caddy config for every domain that has routes for this service, reloads. Audit-logged.
  - The phase spec recommends operators give LXC containers static IPs via Incus profiles to avoid having to use Refresh IP after every container restart. Document in the dashboard.

- **Frontend wizard (`Dashboard.jsx`):**
  - Step 0 (kind picker): Static Site / Container Service. The existing Static Site / Proxy Container / Docker Compose / LXC Container tile grid collapses into the two top-level kinds, with runtime (LXC vs Docker) being a sub-choice inside Container Service.
  - Step 1 (container picker, only for container_service kind): if `runtime='lxc'`, dropdown of LXC containers from `/api/lxc/containers/with-ip`; if `runtime='docker'`, the existing Docker container/port picker. The selected container's IP populates `target_ip` and is shown read-only.
  - Step 2 (routes builder): list of `(domain, path_prefix, target_port)` rows the operator can add/remove inline. Minimum one route. Each row has its own SSL toggle. The Phase 2 banner (existing prefixes for typed domain) and the client-side collision guard generalize to operate against all routes across all services, not just the in-progress one.
  - Service detail page gains a **Routes** section: card with all routes, add-route button, edit/remove icons per row.
  - All new UI uses Phase 1's mobile-first patterns (full-screen wizard at `<sm`, grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` everywhere, 44×44px primary action buttons).

- **Audit log:** existing `SERVICE_CREATED` / `SERVICE_UPDATED` / `SERVICE_DELETED` payloads add a `routes` array. New actions: `ROUTE_CREATED`, `ROUTE_UPDATED`, `ROUTE_DELETED`, `LXC_IP_REFRESHED`. Every action carries the parent `service_id` and the affected route's full `(domain, path_prefix)` tuple.

- **Export/import:** `serviceExport` nests routes under each service. The import endpoint accepts both the new nested format AND the Phase 2 flat format (one route per service auto-derived from the legacy `(domain, path_prefix, port)` fields) so existing exports keep importing cleanly.

- **api.js:** Add `getLxcContainersWithIp`, `getServiceRoutes(serviceId)`, `createRoute(serviceId, route)`, `updateRoute(serviceId, routeId, route)`, `deleteRoute(serviceId, routeId)`, `refreshLxcIp(serviceId)`. Update the existing comment to note that services now nest routes and Phase 2's per-domain caching remains absent.

**Spec references:** Phase 2's `phase-02-path-prefix-multi-service.md` (the merged Caddy generator this phase extends), Phase 1's `admin/frontend/MOBILE_FIRST.md` (the wizard's mobile patterns). The existing `admin/backend/src/routes/lxc.js` code for the Incus container query path. No core infrastructure prompts apply yet.

**Verification:**
- [ ] Fresh install: `services` table has `kind`, `runtime`, `target_ip`, `lxc_container_name` columns; `service_http_routes` table exists with the `UNIQUE(domain, path_prefix)` constraint; the old `domain`/`path_prefix`/`port`/`ssl_enabled`/`force_https`/`websocket_enabled`/`max_upload_size` columns are gone from `services`.
- [ ] Existing-install migration: a seeded Phase 2 DB with two services on `mul.test` (`/` static + `/api` docker) migrates without data loss — both rows now appear in `service_http_routes` with their original prefixes, both belong to a `services` row whose `kind` and `runtime` were inferred from the legacy `type`.
- [ ] Migration is idempotent: running `initDatabase()` a second time logs nothing and changes nothing.
- [ ] Create a `container_service` with `runtime='lxc'`, pick an LXC container from the dropdown, add two routes (`example.com/` to port 8000 + `example.com/api` to port 8001) — both succeed, the merged Caddy file at `/etc/caddy/sites/example.com` contains both `handle_path /api*` and root `handle { reverse_proxy <lxc_ip>:8000 }` blocks, both proxying to the same `target_ip`.
- [ ] Add a third route at `example.com/api/v2` pointing at port 8002 — the merged file now has `handle_path /api/v2*` before `handle_path /api*` (length-DESC ordering preserved from Phase 2).
- [ ] Edit the `/api` route's port to 8003 — the merged file's `handle_path /api*` body updates, the others are unchanged.
- [ ] Delete the `/api/v2` route — only that handle_path block is removed, the other two remain. The parent service still exists with two routes.
- [ ] Delete the parent service — every route under it is removed in one cascade, the merged file collapses or unlinks accordingly.
- [ ] SSL-consistency: attempt to add a route on an existing domain with a mismatched `ssl_enabled` — rejected with the same all-or-nothing error message Phase 2 surfaces, but now naming the conflicting route(s) instead of the conflicting service.
- [ ] LXC IP refresh: stop the picked LXC container in Incus, change its assigned IP, restart, click Refresh IP on the service detail page — `target_ip` updates and the merged Caddy config rewrites.
- [ ] Export the new-shape DB, wipe, import → all services + routes restore.
- [ ] Import a Phase-2-format export file → each Phase 2 service becomes one new service with one route, no data loss.
- [ ] Audit log: every CRUD action on a route has a corresponding `ROUTE_*` row in `audit_log` carrying the parent `service_id` and the route's `(domain, path_prefix)`.
- [ ] Dashboard service list groups routes by their parent service in the favorite-sort view (Phase 2's domain grouping generalizes to service grouping).
- [ ] **Mobile: 360px Add Service wizard with multi-route step** — typing a new domain, adding two routes with different ports, picking an LXC container, all without horizontal scroll at 360×640.
- [ ] **Mobile: 360px Service detail Routes section** — add/edit/remove a route from the detail page on a 360px viewport without horizontal scroll, all touch targets ≥44×44px.
- [ ] **Mobile: 360px Delete confirmation generalizes** — deleting a route still surfaces the "this will leave N other routes on this domain" warning when applicable.
- [ ] **Mobile: 1280px desktop regression** — none of the Phase 1 baseline layouts regress.
- [ ] Caddy `adapt` validates every generated merged config — same defense-in-depth check as Phase 2.

**Commit:** `phase-02b: container-as-service model with multiple HTTP routes per service`

---

## Function-by-Function Checklist

> Ordered backend-first: schema migration → Caddy generator refactor → routes CRUD → service-endpoint refactor → LXC integration → audit-log payloads → api.js → frontend wizard → frontend service detail → mobile verification. One checkbox = one commit. Every item must be verified in `npm run dev` (headless puppeteer at 360×640 AND 1280×800 for frontend items, Express in-process integration test for backend items) before it is ticked off. `_Verified:_` notes mirror Phase 2 style: describe what you actually ran + what you inspected.
>
> **Runtime-safety invariant:** every intermediate commit must leave the tree in a runtime-correct state. Schema changes are **additive first** — new columns and the new `service_http_routes` table land before any code is switched over to read from them, and the legacy route-owned columns on `services` (`domain`, `path_prefix`, `port`, `ssl_enabled`, `force_https`, `websocket_enabled`, `max_upload_size`) are only dropped **after** every endpoint has been refactored to read and write from the new shape. See D.14 ("Drop legacy route columns") for the final drop step.
>
> **Manageable segments:** every Claude Code session picking this up must break its work into small, bounded tool calls — one file/section at a time — so the tool harness does not hang on oversized edits, reads, or generations. Each checklist item is already scoped to one commit; within a single item, further split reads/edits/tests into chunks of ≤200 lines per tool call, run long operations (`npm run dev`, puppeteer scripts, migrations against seeded DBs) in the background with a monitoring loop rather than blocking, and checkpoint progress with `TodoWrite` between segments. If a segment hangs, cancel it and split further rather than retrying the same oversized call.

### A. Backend — Schema migration (runs first; unblocks everything else)

- [x] Add new Phase 2b columns to fresh-install `services` CREATE TABLE — ADDITIVE only (admin/backend/src/db.js:115)
      — Added `kind TEXT NOT NULL DEFAULT 'container_service' CHECK(kind IN ('static_site', 'container_service'))`, `runtime TEXT CHECK(runtime IN ('lxc', 'docker') OR runtime IS NULL)`, `target_ip TEXT`, `lxc_container_name TEXT` to the fresh-install `CREATE TABLE IF NOT EXISTS services`. Legacy route columns (`domain`, `path_prefix`, `port`, `ssl_enabled`, `force_https`, `websocket_enabled`, `max_upload_size`) and `UNIQUE(domain, path_prefix)` are retained. Matching `ALTER TABLE services ADD COLUMN ...` try/catch blocks land AFTER `migrateServicesUniqueConstraint(db)` because the Phase 2 rebuild has a hardcoded canonical column set — new columns added before it would be dropped by the rebuild. **Verified:** smoke test covered three scenarios — (1) fresh DB init produces a table whose `PRAGMA table_info(services)` lists all 4 new Phase 2b columns alongside every legacy route column and UNIQUE(domain, path_prefix) is intact; (2) seeded a pre-Phase-2 DB with the old `UNIQUE(domain)` shape + two rows on `mul.test` and `api.test`, ran initDatabase() → the Phase 2 rebuild logged "Migrating services table: UNIQUE(domain) -> UNIQUE(domain, path_prefix)" and the Phase 2b ALTER TABLEs then added `kind` (defaulted to `container_service`), `runtime`/`target_ip`/`lxc_container_name` (all NULL), both seeded rows survived with original data; (3) running initDatabase() a second time logged only "Database initialized" (no re-migration, no column changes, rows unchanged).

- [x] Add fresh-install `service_http_routes` CREATE TABLE (admin/backend/src/db.js, new block just below the services CREATE TABLE)
      — Added `CREATE TABLE IF NOT EXISTS service_http_routes (id TEXT PRIMARY KEY, service_id TEXT NOT NULL, domain TEXT NOT NULL, path_prefix TEXT NOT NULL DEFAULT '/', target_port INTEGER, websocket_enabled INTEGER DEFAULT 0, ssl_enabled INTEGER DEFAULT 1, force_https INTEGER DEFAULT 1, max_upload_size TEXT DEFAULT '1G', created_at TEXT DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (service_id) REFERENCES services(id) ON DELETE CASCADE, UNIQUE(domain, path_prefix))` plus a helper `CREATE INDEX IF NOT EXISTS idx_routes_service ON service_http_routes(service_id)`. Placed directly after the Phase 2b `services` ALTER TABLE blocks so a single `initDatabase()` pass lands both the parent-table columns and the sibling routes table. Note: SQLite FKs are not enforced in this app (no `PRAGMA foreign_keys = ON`); the FK clause is documentary + used by any future install that opts into enforcement. **Verified:** smoke test covered five scenarios — (1) fresh DB init creates the table with all 10 required columns, the `idx_routes_service` index, and the auto UNIQUE index on `(domain, path_prefix)`; (2) inserting two sibling rows `(web.test, /)` + `(web.test, /api)` both succeed; (3) inserting a third row with a duplicate `(web.test, /)` tuple is rejected with `UNIQUE constraint failed`; (4) seeded a pre-A.2 DB (post-A.1 services shape, no routes table) with one services row, ran `initDatabase()` → the routes table was created, the seeded services row survived intact, routes table empty (A.3 will backfill); (5) running `initDatabase()` a second time is a no-op.

- [x] `migrateServicesToRoutes(db)` helper — ADDITIVE data backfill only (admin/backend/src/db.js, new export)
      — Idempotent data-only migration exported from `db.js`. The helper's idempotency comes from two column-existence guards (post-D.14 detection via `PRAGMA table_info` for `domain`/`path_prefix`/`port`; pre-A.1 detection for `kind`/`runtime`/`target_ip`/`lxc_container_name`) plus per-statement WHERE clauses that only match rows still in the unpopulated state. Inside a `db.transaction`: (1) `INSERT INTO service_http_routes (id, service_id, domain, path_prefix, target_port, websocket_enabled, ssl_enabled, force_https, max_upload_size) SELECT lower(hex(randomblob(16))), id, domain, COALESCE(path_prefix, '/'), port, COALESCE(websocket_enabled, 0), COALESCE(ssl_enabled, 1), COALESCE(force_https, 1), COALESCE(max_upload_size, '1G') FROM services WHERE is_admin = 0 AND domain IS NOT NULL AND id NOT IN (SELECT service_id FROM service_http_routes)`; (2) `UPDATE services SET kind = 'static_site' WHERE is_admin = 0 AND type = 'static' AND kind != 'static_site'` (A.1's ALTER TABLE already set `kind` to the `container_service` default on every existing row, so only static rows need flipping); (3) `UPDATE services SET runtime = 'docker' WHERE is_admin = 0 AND runtime IS NULL AND type = 'docker'`; (4) `UPDATE services SET target_ip = target WHERE is_admin = 0 AND target_ip IS NULL AND target IS NOT NULL AND type != 'static'`. Legacy route columns on `services` are NOT dropped here — D.14 owns that. Logs `Backfilled services → service_http_routes (N routes)` only when N > 0, so idempotent re-runs are silent. **Verified:** smoke test covered five scenarios — (1) seeded a post-A.1/A.2 DB with three services (`svc-static` static on `mul.test/`, `svc-docker` docker on `mul.test/api` port 3000 with websocket + 500M upload, `svc-admin` proxy admin on `admin.test/`) and ran the helper; the log reported `Backfilled services → service_http_routes (2 routes)`, the routes table contained exactly two rows with correct `(domain, path_prefix, target_port, websocket_enabled, max_upload_size)` tuples, `svc-static.kind` flipped to `static_site` (runtime + target_ip stayed NULL), `svc-docker.kind=container_service` + `runtime='docker'` + `target_ip='127.0.0.1'`, and the admin row was untouched beyond A.1's defaults; (2) a second call on the same DB produced zero row changes and no log output (captured via a console.log stub); (3) backfill on an empty services table inserted zero routes; (4) post-D.14 simulation (services table without the legacy route columns) returned early without touching anything; (5) pre-A.1 simulation (services table without Phase 2b columns) returned early without crashing.

- [x] Wire `migrateServicesToRoutes(db)` into `initDatabase()` (admin/backend/src/db.js:260)
      — Call lands inside `initDatabase()` directly after the Phase 2b `service_http_routes` CREATE TABLE + `idx_routes_service` CREATE INDEX blocks, so the backfill has both (a) the Phase 2b columns on `services` (added by A.1 at the top of initDatabase) and (b) the empty routes table to INSERT into. Runs on every boot but is silent unless something was actually backfilled. **Verified:** smoke test covered three scenarios — (1) seeded a pre-Phase-2 DB (old `UNIQUE(domain)` shape + two rows on `mul.test` static and `api.test` docker port 3000) and called `initDatabase()` once; captured logs showed the full pipeline ran in order (`Migrating services table: UNIQUE(domain) -> UNIQUE(domain, path_prefix)` → `Services table migration complete` → `Backfilled services → service_http_routes (2 routes)` → `Database initialized`), both seeded rows survived with correct Phase 2b fields, and two routes landed in `service_http_routes`; (2) a second `initDatabase()` call on the same DB logged only `Database initialized` (no Phase 2 rebuild, no backfill) and produced zero row changes; (3) a fresh DB `initDatabase()` created both tables with the correct columns, left `service_http_routes` empty, and did not emit the backfill log.

### B. Backend — Per-domain Caddy generator refactor

- [x] Refactor `generateServiceHandlerBody(entry, indent)` to accept both legacy and Phase 2b entry shapes (admin/backend/src/routes/services.js:3296)
      — Pure function that now normalizes field names from either the legacy Phase 2 shape (`type`, `target`, `port`, `rootDir`/`root_dir`) or the Phase 2b joined shape (`kind`, `targetIp`/`target_ip`, `targetPort`/`target_port`, `rootDir`/`root_dir`). `kind` takes precedence over legacy `type` when both are present (Section D endpoints will emit `kind`; pre-Section-D call sites still pass `type`). Branch selection maps `kind='static_site'` and legacy `type='static'` to the `root * / file_server / try_files` static output; `kind='container_service'` and legacy `type='docker'`/`type='proxy'` to the `reverse_proxy <target>:<port>` output. The `indent` parameter is preserved unchanged. This backward-compat step is the Option 1 "dual reader" strategy: every existing Phase 2 call site keeps working against the legacy shape, and new Section D call sites can start passing the Phase 2b shape without any coordination. **Verified:** inline snapshot test covered 10 cases byte-for-byte — (1) legacy static with `rootDir` → three-line static block; (2) legacy docker with `target=127.0.0.1`, `port=3000` → `reverse_proxy 127.0.0.1:3000`; (3) legacy proxy → same reverse_proxy line with port 3001; (4) Phase 2b container_service camelCase (`kind='container_service'`, `targetIp=10.0.0.5`, `targetPort=8000`) → `reverse_proxy 10.0.0.5:8000`; (5) Phase 2b container_service DB snake_case with coexisting legacy `type='docker'` (the A.3 backfill state) → `reverse_proxy 172.17.0.2:9000`; (6) Phase 2b `kind='static_site'` without legacy type → three-line static block from `rootDir`; (7) `kind='container_service'` with `target_ip=null` falls back to `127.0.0.1:<port>`; (8) indent parameter of 8 spaces (handle_path body depth) respected; (9) legacy docker snake_case with `root_dir=null` unchanged; (10) legacy static snake_case via `root_dir` still produces the static block.

- [x] Refactor `buildDomainCaddyConfig(entriesList, domain)` to accept both legacy and Phase 2b entry shapes (admin/backend/src/routes/services.js:3417)
      — Renamed the argument from `servicesList` to `entriesList` and extended the internal `normalized` map to read `target` from either `targetIp`/`target_ip` (Phase 2b) or legacy `target`, `port` from either `targetPort`/`target_port` (Phase 2b) or legacy `port`, and to pass `kind` through alongside `type` so the downstream `generateServiceHandlerBody` can branch on either. The length-DESC sort, duplicate-tuple rejection, site-address + SSL-stance selection, `request_body max_size` merge, and header/log blocks are unchanged. The header-comment label prefers `kind` over `type` when both exist, so the comment stays informative post-D.14 when legacy `type` is gone. **Verified:** inline snapshot test covered 6 cases — (1) two legacy Phase 2 rows on `mul.test` (`/` static + `/api` docker port 3000 with 500M upload) produce the merged site block with both handlers, length-DESC order, `max_size 1GB` (max across rows), root wrapped in `handle { ... }`, /api in `handle_path /api* { ... }`; (2) the same end state expressed as Phase 2b joined entries (`kind='static_site'` with `root_dir`, `kind='container_service'` with `target_ip='127.0.0.1'` + `target_port=3000`) produces a site block that matches Case 1 byte-for-byte modulo the header-comment label line and the generated timestamp; (3) a mixed input with one legacy row at `/admin` + one Phase 2b joined row at `/` (simulating the A.3 backfill state where both shapes coexist) renders both entries correctly with length-DESC ordering; (4) wildcard domain `*.test.com` still falls back to `http://*.test.com {`; (5) two entries with duplicate `(domain, path_prefix)` tuples throw `Duplicate (domain, path_prefix) tuple detected`; (6) empty and null lists both return `null`.

- [x] Refactor `regenerateDomainCaddyConfig(db, domain)` to dual-source read (admin/backend/src/routes/services.js:3590)
      — Per the Option 1 dual-reader strategy, the helper now runs BOTH queries: (1) a Phase 2b JOIN of `service_http_routes` to `services` pulling route-owned fields from `r.*` and service-owned fields from `s.*`; (2) the unchanged legacy `SELECT ... FROM services WHERE domain = ?` query wrapped in a try/catch so it tolerates post-D.14 installs where the legacy columns no longer exist. After reading both, the helper dedupes by `(service_id, path_prefix)` with the routes row winning — this is the post-D.14 source of truth, and the dedupe is what makes the A.3 backfill state (every non-admin service present in both sources) render correctly without double handlers. The combined list flows through `buildDomainCaddyConfig` (which already accepts either shape after B.2). Admin services are filtered out of both queries. Post-D.14 the legacy SELECT will either return zero rows (`is_admin = 0` filter) or throw (columns dropped) — the try/catch collapses the helper to a routes-only read without further code changes. **Verified:** integration test with a real temp sqlite DB + temp `CADDY_SITES_DIR` covered 6 scenarios — (1) legacy-only domain (seeded one service with legacy `domain`/`port` + no route rows) renders one `reverse_proxy 127.0.0.1:3000` handler from the legacy source; (2) pre-A.3 fresh-install state (legacy static service row with `root_dir`, no route row) renders the static block from the legacy source; (3) **A.3 backfill state** (same service exists in both sources with matching `(service_id='s3', path_prefix='/api')` tuple) — merged file contains exactly ONE `handle_path /api*` block and exactly ONE `reverse_proxy 127.0.0.1:3000` line, proving dedupe prefers the routes row; (4) mixed domain where service A has a route-only entry on `/` and service B has a legacy-only entry on `/api` — merged file contains both with correct reverse_proxy lines (`10.0.0.5:8000` for A, `127.0.0.1:4000` for B); (5) empty domain unlinks a stale merged file; (6) admin service present in both sources is filtered out — no merged file created on the admin domain.

- [x] Cross-route SSL consistency check helper `assertRoutesShareSslStance(db, domain, candidateRoute, excludeRouteId)` (admin/backend/src/routes/services.js:3709)
      — Exported helper following the B.3 dual-source pattern: scans BOTH the `service_http_routes` JOIN `services` query AND the legacy `services` query (wrapped in try/catch for post-D.14 compatibility), dedupes legacy sibling entries whose `(service_id, path_prefix)` tuple is already covered by the routes query. For each surviving sibling, compares `ssl_enabled` and `force_https` against the candidate and throws an `Error` with `code === 'ROUTE_SSL_CONFLICT'` whose message names the conflicting sibling — routes-sourced siblings are labeled as `"<service_name>" route /<path_prefix>`, legacy-sourced siblings as `"<service_name>" (legacy path /<path_prefix>)`. Admin services are filtered from both sources. `excludeRouteId` is used in the routes query so update flows do not false-conflict against the row being updated. **Verified:** 10-case integration test on a real sqlite DB — (1) no siblings passes; (2) legacy sibling with matching stance passes; (3) legacy sibling with different `ssl_enabled` throws with message including `"LegacySsl"`; (4) legacy sibling with different `force_https` throws; (5) route sibling with matching stance passes; (6) route sibling with different stance throws with message including `"ParentTwo" route /api`; (7) A.3 backfill state (same row in both sources with matching stance) passes without a false self-conflict (dedupe prevents it); (8) A.3 backfill state where the routes-side stance was updated and differs from the legacy-side stance → routes wins via dedupe, candidate matching the routes stance passes; (9) `excludeRouteId` lets an update flow pass a mismatched candidate against the row being updated itself; (10) an admin service with mismatched stance on the target domain is ignored.

### C. Backend — Routes CRUD endpoints

- [x] `GET /api/services/:id/routes` (admin/backend/src/routes/services.js:1394)
      — Lands as the first endpoint in a new `==================== PHASE 2b ROUTES CRUD ====================` section block just above the existing service `DELETE /:id`. Returns `{routes: [...]}` ordered by `length(path_prefix) DESC, created_at ASC` so more-specific prefixes appear first (matching Caddy's source-order matching). Each row is mapped to camelCase (`id`, `serviceId`, `domain`, `pathPrefix`, `targetPort`, `websocketEnabled`, `sslEnabled`, `forceHttps`, `maxUploadSize`, `createdAt`) with SQLite integer booleans coerced to JS `true`/`false`. Returns 404 when the parent service id does not exist. **Verified:** Established the Section C Express integration test harness (`app.listen(0)` with a fake `req.user` middleware, fetch against the random port) and exercised 3 scenarios — (1) seeded a container_service with zero routes, GET returned `{routes: []}`; (2) seeded a container_service with three routes on `three.test` (`/`, `/api`, `/api/v2`) and varying field values, GET returned all three in length-DESC order (`/api/v2`, `/api`, `/`) with correct camelCase fields, integer→boolean coercion, and a spot-checked `/api` row (`targetPort=8001`, `websocketEnabled=true`, `maxUploadSize='500M'`, non-empty `createdAt`); (3) GET with an unknown service id returned 404 with error "Service not found". Harness fix: `req.ip` is a getter in Express 4 and cannot be assigned in middleware; removed the assignment.

- [x] `POST /api/services/:id/routes` (admin/backend/src/routes/services.js:1459)
      — New endpoint with a dedicated `createRouteSchema` zod shape (`domain`, `pathPrefix` default `/`, `targetPort`, `websocketEnabled`, `sslEnabled`, `forceHttps`, `maxUploadSize`). Runs three validation gates in order: (1) `(domain, path_prefix)` uniqueness against `service_http_routes` with the same error message Phase 2 surfaces; (2) `(domain, path_prefix)` uniqueness against the legacy `services` table — wrapped in try/catch for post-D.14 installs; (3) `assertRoutesShareSslStance` against every existing sibling on the domain. Backs up the merged file for the affected domain, INSERTs the route, regenerates the merged config via `regenerateDomainCaddyConfig`, runs `caddy adapt`, then `reloadCaddy()`. On any downstream failure, `rollbackRouteCreate()` deletes the inserted row AND restores the backed-up merged file (or unlinks if no backup). Audit-logs `ROUTE_CREATED` with `{service_id, route: {id, domain, pathPrefix, targetPort}}`. Rejects admin services with 403. **Verified:** Express integration test against 8 scenarios — (1) happy path: seed a `container_service` LXC + POST `{domain: 'real.test', pathPrefix: '/', targetPort: 8000}` → 201 with DB row present and merged file at `CADDY_SITES_DIR/real.test` containing `reverse_proxy 10.0.0.5:8000`; (2) duplicate tuple against `service_http_routes` → 400 with "Domain + path prefix combination already exists"; (3) duplicate tuple against a pre-existing legacy services row → 400 with same error; (4) SSL stance mismatch against an existing sibling route → 400 with error message naming the conflicting sibling (`"SslLxc" route /`); (5) unknown parent service id → 404; (6) admin service id → 403 with "Cannot add routes to admin service"; (7) invalid domain ("not a valid domain") → 400 ZodError from createRouteSchema; (8) `audit_log` row for action='ROUTE_CREATED' present with correct `service_id` and nested `route` payload. Used a stub `caddy` binary at `/tmp/pp2b/bin/caddy` that exits 0 for all subcommands so the `caddy adapt` + `reloadCaddy()` path completes successfully under test. **Note:** better-sqlite3 9.6.0 enforces foreign_keys=ON by default (confirmed via PRAGMA inspection), so the `audit_log.user_id` FK to `users(id)` requires a real user row during tests — the test harness seeds a `test-user` row. The Phase 2 comment in `db.js` claiming FKs are not enforced is stale but not fixed in this commit (out of scope).

- [x] `PUT /api/services/:id/routes/:routeId` (admin/backend/src/routes/services.js:1695)
      — New endpoint that accepts a `createRouteSchema.partial()` payload, merges it onto the existing route, and runs the same three validation gates C.2 does (uniqueness against `service_http_routes`, uniqueness against legacy `services`, `assertRoutesShareSslStance` with `excludeRouteId` set to the row being updated). Uniqueness gates only re-run when the `(domain, path_prefix)` tuple actually changed, so a no-tuple-change update does not false-positive against its own row. When `domain` changes: snapshots both the new-domain and old-domain merged files, UPDATEs the row, regenerates both merged files (new-domain picks up the moved route, old-domain shrinks or unlinks), and on any downstream failure `rollbackRouteUpdate` restores BOTH merged files AND reverts the DB row to the pre-update snapshot. Audit-logs `ROUTE_UPDATED` with `{service_id, before: {domain, pathPrefix, targetPort}, after: {...}}`. **Verified:** Express integration test against 8 scenarios — (1) partial `{targetPort: 8003}` update flips the DB row and the merged file's `handle_path /api*` body from port 8001 to 8003 with no other handlers touched; (2) `{domain: 'other.test'}` migrated the `/api` route — `mul.test` merged file lost its `handle_path /api*` block (but kept the root handler at `10.0.0.5:8000`), new `other.test` merged file now carries the moved route; (3) attempt to rename `/api` back onto an occupied `(mul.test, /)` tuple → 400 "already exists"; (4) attempt to flip a sibling route to `sslEnabled=false` when the other sibling on the same domain has SSL on → 400 naming `"SslLxc"`; (5) no-tuple-change `{maxUploadSize: '2G'}` update returns 200 without false-positive uniqueness rejection; (6) unknown service id → 404; (7) valid service but unknown route id → 404 "Route not found"; (8) `audit_log` ROUTE_UPDATED row from the domain-change scenario contains both `before: {domain: 'mul.test', pathPrefix: '/api', ...}` and `after: {domain: 'other.test', pathPrefix: '/api', ...}` snapshots.

- [x] `DELETE /api/services/:id/routes/:routeId` (admin/backend/src/routes/services.js:2004)
      — New endpoint that snapshots the pre-delete row + the merged file for the route's domain, DELETEs the row, regenerates the merged file (shrinks if siblings remain, unlinks otherwise), runs `caddy adapt` + `reloadCaddy()`, and on any downstream failure `rollbackRouteDelete` re-INSERTs the row via a full column list AND restores the backed-up merged file. Audit-logs `ROUTE_DELETED` with `{service_id, route: {id, domain, pathPrefix, targetPort}}`. The parent service row is NOT touched — a service with zero routes is legal in Phase 2b; the operator can add routes back without recreating the service. Admin services rejected with 403. **Verified:** Express integration test against 7 scenarios — (1) seeded two routes on `mul.test` (`/` + `/api`), DELETE `/api` → 200, merged file still contains `handle {` root `reverse_proxy 10.0.0.5:8000` but no `handle_path /api*`; (2) seeded a service with one route on `alone.test`, DELETE → 200, merged file unlinked via `fs.access` rejection; (3) the parent service `svc-alone` survives with zero route rows after the last-route delete; (4) unknown service id → 404; (5) valid service but unknown route id → 404 "Route not found"; (6) admin service delete rejected with 403 and the admin route stays in the DB; (7) `audit_log` contains two `ROUTE_DELETED` rows matching the two successful deletes, each carrying `service_id` + full route tuple.

- [x] Service-level `DELETE /api/services/:id` cascade (admin/backend/src/routes/services.js:2135)
      — Updated the existing handler so that before the DELETE statement runs, it collects the full set of affected domains from BOTH sources — the legacy `services.domain` column AND the distinct set of `service_http_routes.domain` values where `service_id = :id`. After `DELETE FROM services WHERE id = ?` (the ON DELETE CASCADE FK wipes every child route row via better-sqlite3's foreign_keys=ON default), the handler iterates the collected domain set and calls `regenerateDomainCaddyConfig` once per domain so every affected merged file either shrinks (siblings remain on the domain) or unlinks (last entry gone). Reload Caddy once at the end so N domain rewrites share a single reload. Audit-logs `SERVICE_DELETED` with a payload that nests the full `routes: [{id, domain, pathPrefix, targetPort}]` array AND still includes the legacy `{domain, pathPrefix}` fields for backward compatibility. **Verified:** Express integration test against 5 scenarios — (1) the big cross-domain cascade: seeded `svc-main` owning three routes across two domains (`shared.test/`, `solo.test/`, `solo.test/api`) plus `svc-sibling` with a route on `shared.test/api`; DELETE `svc-main` → 200, `svc-main` gone from services, all three child routes gone via FK cascade, `svc-sibling` intact with its route, `shared.test` merged file rewritten with only the sibling's `/api` handler and no trace of the deleted root, `solo.test` merged file unlinked, and the placeholder domain from the legacy `services.domain` column also unlinked; (2) legacy-only service (no route rows) delete unlinks its single merged file; (3) `audit_log` SERVICE_DELETED row for `svc-main` contains a 3-element `routes` array covering all cascaded routes AND the legacy `domain`/`pathPrefix` fields; (4) admin service delete rejected with 403; (5) unknown service id → 404.

### D. Backend — Service endpoint refactor (rewire to the new shape)

> **Section D strategy revision (mid-session):** rather than changing the zod schemas and payload shapes, Section D uses a dual-write approach: every legacy Phase 2 endpoint keeps accepting its existing payload unchanged, and internally calls `syncPrimaryRouteFromLegacy` to mirror the legacy columns into `service_http_routes`. GET endpoints additively nest a `routes` array in the response while retaining the legacy top-level fields for backward compat. This keeps the Phase 2 frontend working unchanged through D.1–D.13. D.14 then drops the legacy columns via a table rebuild, at which point the GET responses start synthesizing legacy fields from the primary route (until Section H refactors the frontend).

- [x] D.1 Add `syncPrimaryRouteFromLegacy(db, serviceId, legacy)` helper (admin/backend/src/routes/services.js:1408, exported from the module)
      — Mirrors legacy Phase 2 route-owned fields (`domain`, `pathPrefix`, `targetPort`, `sslEnabled`, `forceHttps`, `websocketEnabled`, `maxUploadSize`) onto the "primary" route for a service, where "primary" is defined as the earliest-created `service_http_routes` row owned by that service_id. If no route row exists yet, inserts a new one. If one exists, updates it in place. Secondary routes added later via C.2 (POST /:id/routes) have later timestamps and are left untouched — the helper only manages the primary. Path prefix is normalized via `normalizePathPrefix` (trailing slash stripped, empty → `/`). Booleans coerce to SQLite integers. Returns the synced route id. **Verified:** unit test against 5 scenarios — (1) first call on a service with zero routes inserts a new primary and returns its id; (2) second call on the same service with different values updates the same primary in place (no duplicate insert); (3) seeded a secondary route via direct INSERT with a later `created_at`, called the helper again with fresh primary values → earliest-created row updated, secondary left at its original `(domain, path_prefix, target_port)`; (4) trailing slash in `pathPrefix='/api/'` normalized to `/api`, empty `pathPrefix=''` normalized to `/`; (5) `sslEnabled=false`/`forceHttps=false`/`websocketEnabled=true` coerced to `0`/`0`/`1` in the DB.

- [x] D.2 `POST /api/services` create now dual-writes a primary route (admin/backend/src/routes/services.js:800)
      — Two minimal additive changes to the existing Phase 2 create handler: (1) the INSERT statement now also writes `kind` (inferred from legacy `type`: static → static_site, docker/proxy → container_service), `runtime` (docker → 'docker', else NULL), and `target_ip` (copied from legacy `target` for non-static services) so the B.3 routes JOIN query can render newly-created services without waiting for the next A.3 backfill pass; (2) immediately after the services INSERT the handler calls `syncPrimaryRouteFromLegacy` to mirror the (domain, pathPrefix, port, sslEnabled, forceHttps, websocketEnabled, maxUploadSize) tuple into a matching `service_http_routes` row. If the sync call throws (e.g. a UNIQUE violation from a racing route on the same domain+prefix), the services row is immediately deleted and a 400 returned — no half-committed state. The outer `rollbackCreate()` path is unchanged; it still cleans up the services row (which also cascades to the route row via the FK) on any downstream Caddy-adapt or reload failure. **Verified:** Express integration test against 5 scenarios — (1) docker service create produced both a services row with `kind='container_service'`, `runtime='docker'`, `target_ip='10.0.0.5'` AND a matching `service_http_routes` row with `(docker.test, /, 3000)`; (2) static site create produced `kind='static_site'`, `runtime=NULL`, `target_ip=NULL` with a primary route at `target_port=NULL`; (3) the merged `docker.test` Caddy file contains `reverse_proxy 10.0.0.5:3000` (proving `target_ip` was written correctly and the routes JOIN reads it); (4) kind inference from legacy type verified for both cases; (5) seeded a pre-existing route at `(race.test, /)`, then attempted to POST a new service on the same tuple — the uniqueness pre-check on legacy services still passes (race.test had no legacy row), the INSERT services succeeds, then `syncPrimaryRouteFromLegacy` fails with a UNIQUE violation, the handler deletes the services row, returns 400, and no orphan is left behind.

- [x] D.3 `PUT /api/services/:id` update dual-writes primary route (admin/backend/src/routes/services.js:1087)
      — Same dual-write strategy as D.2: the existing Phase 2 PUT handler keeps its legacy payload shape unchanged, and internally (a) adds `target_ip` to the UPDATE SET list, derived from `updatedData.target` for non-static services, and (b) calls `syncPrimaryRouteFromLegacy` after the UPDATE to mirror the new (domain, pathPrefix, port, ssl flags, ...) tuple onto the primary route. If the sync call fails (e.g. a UNIQUE violation from the new tuple colliding with another service's route), the handler immediately reverts the services row UPDATE and returns 400. The existing `rollbackUpdate()` closure (triggered by any downstream Caddy-adapt / reload failure) now also calls `syncPrimaryRouteFromLegacy` a second time with the PRE-update values to revert the primary route back in lockstep with the services row revert. C.2 secondary routes are left untouched because `syncPrimaryRouteFromLegacy` only updates the earliest-created route. **Verified:** Express integration test against 5 scenarios — (1) PUT `{port: 3001}` flowed into `services.port`, `service_http_routes.target_port` on the primary, and the merged Caddy file's `reverse_proxy 10.0.0.5:3001` line; (2) PUT `{domain: 'svc1-new.test'}` migrated both tables AND regenerated both merged files (old unlinked, new populated); (3) PUT `{target: '10.0.0.99'}` updated `services.target`, `services.target_ip`, and the merged file's reverse_proxy line; (4) seeded a C.2 secondary route at `/api`, then PUT `{port: 3002}` — primary route port flipped to 3002 while the secondary route's `target_port` stayed at its C.2-created value of 3099; (5) seeded a sibling service at `(occupied.test, /)` then PUT svc1's domain onto that tuple — Phase 2's uniqueness check caught it first, returned 400, both services and route rows stayed at their pre-PUT state.

- [x] D.4 Refactor `POST /:id/obtain-certificate` (admin/backend/src/routes/services.js:251)
      — SSL flags live on routes in Phase 2b, so the service-scoped enable action now flips `ssl_enabled=1, force_https=1` on BOTH the legacy `services` columns AND every child `service_http_routes` row owned by the service, in lockstep. The handler collects every distinct domain across those routes (plus the legacy `services.domain` fallback for the transitional dual-source state), validates SSL stance against EXTERNAL sibling routes via a new `assertSiblingsMatchStance(db, domain, serviceId, targetSsl, targetForce)` helper that excludes every row owned by this service so pre-flip state doesn't false-conflict against the target, backs up the merged file for each affected domain, mutates both tables, regenerates every affected merged file, runs `caddy adapt` + `reloadCaddy()`, and on ANY downstream failure rolls back both tables row-by-row AND restores every backed-up merged file. `assertRoutesShareSslStance` could not be used directly because it only excludes one route id at a time; the new helper mirrors its dual-source scan + `(service_id, path_prefix)` dedupe logic but takes a `serviceId` for the exclusion. Audit payload bumped to `{service_id, domains: [...]}`. **Verified:** Express integration test against 16 assertions across 5 scenarios with a stub `caddy` binary at `/tmp/pp2b/bin/caddy` — (1) happy path single-domain single-route: 200, `services.ssl_enabled=1`, `service_http_routes.ssl_enabled=1`, merged file switches from `http://svc1.test {` to auto-TLS `svc1.test {`, `SSL_ENABLED` audit row contains the domain list; (2) multi-domain service (two routes on two domains): 200, both routes flipped, both merged files regenerated with auto-TLS; (3) single-domain three-route service (`/`, `/api`, `/api/v2`): 200, all three routes flipped, merged file retains length-DESC ordering with all three handlers under an auto-TLS site address; (4) sibling-SSL-stance conflict: target service on `conflict.test/` + sibling service on `conflict.test/sibling` with SSL off → 400 with error message naming the `Sibling` and `conflict.test`, DB rows (both services and routes tables) untouched, proving the pre-mutation guard caught the conflict; (5) unknown service id → 404.

- [x] D.5 Refactor `DELETE /:id/certificate` (admin/backend/src/routes/services.js:437)
      — Mirror of D.4: flips `ssl_enabled=0, force_https=0` on both the legacy services columns AND every child `service_http_routes` row in lockstep. Reuses the `assertSiblingsMatchStance` helper from D.4 with `targetSsl=false, targetForce=false` so the pre-mutation stance check catches any sibling route still running with SSL on. Same dual-source domain collection (routes table + legacy fallback), same per-domain merged-file backup + regenerate loop, same row-by-row rollback path. TOTP guard and destructive-action semantics are preserved — the TOTP check remains ahead of any DB mutation. Audit payload bumped to `{service_id, domains: [...]}`. **Verified:** Express integration test against 13 assertions across 5 scenarios with the stub caddy binary — (1) happy path single-domain single-route: 200, both `services` and `service_http_routes` flipped to 0, merged file switches from `svc1.test {` to `http://svc1.test {`; (2) multi-domain service: both routes flipped, both merged files switched to plain `http://`; (3) sibling route with SSL still on blocks the disable with a 400 naming `Sibling` on `a.test`, proving pre-mutation guard rejected the flip with both services + route rows untouched; (4) unknown service id → 404; (5) audit log `SSL_DISABLED` row contains both `service_id` and the affected domains list.

- [x] D.6 Refactor `POST /:id/regenerate-config` (admin/backend/src/routes/services.js:679)
      — Collects the full set of affected domains from both `SELECT DISTINCT domain FROM service_http_routes WHERE service_id = ?` AND the legacy `services.domain` fallback (for the pre-A.3 transitional state where the routes table may be empty). Calls `regenerateDomainCaddyConfig` once per distinct domain, tracking successes vs failures in a `{regenerated, failed}` pair. Caddy is reloaded exactly once at the end so N domain rewrites share a single reload. Audit payload bumped to `{service_id, domains: [...]}`. Response now includes the `domains` list and a `failed` array so the frontend can surface partial failures. **Verified:** Express integration test against 10 assertions across 5 scenarios with stub caddy binary — (1) service with routes on two domains: both merged files rewritten from `STALE_A` / `STALE_B` markers to real `reverse_proxy` directives, response `domains` contains both; (2) single-route single-domain service: 200 with 1-element `domains`, merged file content correct; (3) unknown service id → 404; (4) audit log row for `CONFIG_REGENERATED` contains both `a.test` and `b.test`; (5) legacy-only transitional state (services row with `domain='a.test'`, zero route rows) — handler still picks up `a.test` via the fallback, regenerates the merged file with `reverse_proxy 10.0.0.5:5000` sourced from the legacy `services.port` column.

- [x] D.7 Refactor `POST /caddy/regenerate-all` loop (admin/backend/src/routes/services.js:760)
      — `uniqueDomains` is now assembled from BOTH the routes-side JOIN `SELECT DISTINCT r.domain FROM service_http_routes r INNER JOIN services s ON s.id = r.service_id WHERE s.is_admin = 0` AND the legacy `SELECT DISTINCT domain FROM services WHERE is_admin = 0 AND domain IS NOT NULL` query (wrapped in try/catch for post-D.14 compatibility). The two result sets are merged into a single `Set` so the transitional dual-source state renders every domain exactly once, and post-D.14 the legacy query throws and the helper collapses to a routes-only scan. Admin domain detection still uses the legacy `services.domain` column where `is_admin=1`, which the installer still populates via a fixed row; admin services have no route rows by design so the legacy fallback is effectively required here. The existing full-directory backup/revert logic + one-shot reload at the end are unchanged. **Verified:** Express integration test against 10 assertions across 3 scenarios with stub caddy binary — (1) mixed multi-service multi-domain (`svc-a` with two routes on `ex1.com` at `/` + `/api`, `svc-b` one route on `ex2.com`, `svc-c` static_site on `ex3.com`): 200, three domains regenerated, `ex1.com` merged file rewritten from `STALE1` marker to a real `handle_path /api*` + root `handle` block with both `reverse_proxy 10.0.0.5:3000` and `:3001`, `ex2.com` rewritten to `reverse_proxy 10.0.0.5:4000`; (2) admin-service isolation: user service on `ex1.com` regenerates normally, admin service on `admin.test` (no route rows, legacy columns only) is skipped with `admin.test (skipped - admin)` in the success list and the pre-existing `ADMIN_CONFIG_DO_NOT_TOUCH` file is left untouched; (3) legacy-only transitional state: a services row on `legacy-only.test` with no route rows is picked up via the legacy fallback and its merged file is written with `reverse_proxy 10.0.0.5:9000` sourced from the legacy `services.port` column.

- [x] D.8 Refactor `POST /:id/revert-config/:versionId` (admin/backend/src/routes/services.js:1692)
      — Under the Section D dual-write strategy, saved snapshots remain in the legacy flat shape (D.2/D.3 save only legacy fields). The revert handler now (a) snapshots the pre-revert services row for rollback, (b) applies the legacy fields from the saved config wrapped in try/catch so a `services.(domain, path_prefix)` UNIQUE violation returns 400 instead of 500, (c) calls `syncPrimaryRouteFromLegacy(db, serviceId, {...})` to propagate the revert into the primary route — if this throws (typically a UNIQUE violation against another service's route on the reverted tuple), the services UPDATE is reverted row-by-row back to the pre-revert snapshot and 400 is returned, (d) if the domain changed, regenerates BOTH the old and new domain's merged files so the stale domain shrinks or unlinks and the reverted domain is rewritten, (e) also adds `target_ip` to the UPDATE SET list derived from `config.target` for non-static services so the B.3 routes JOIN query renders the reverted state correctly. Audit payload bumped to `{service_id, revertedToVersion, domain}`. **Verified:** Express integration test against 13 assertions across 4 scenarios — (1) revert port change: v1 snapshot at port=3000, service currently at 3001, revert → both services.port and service_http_routes.target_port back at 3000, merged file contains `reverse_proxy 10.0.0.5:3000` and no `:3001`; (2) revert domain change: v1 snapshot at `old.test`, service currently at `new.test` with stale merged file, revert → both tables back at `old.test`, `new.test` merged file no longer has the reverse_proxy, `old.test` merged file populated with the service's reverse_proxy; (3) collision with sibling route: sibling service owns `(old.test, /)`, target service at `(new.test, /)`, revert target to `(old.test, /)` → 400, services row reverted to pre-revert state (`new.test`), route untouched at `new.test`; (4) unknown version id → 404.

- [x] D.9 Refactor `POST /export` (admin/backend/src/routes/services.js:3430)
      — `serviceExport` objects now nest a `routes` array sourced from `service_http_routes` ordered by `created_at ASC, id ASC` and carrying `{id, domain, pathPrefix, targetPort, websocketEnabled, sslEnabled, forceHttps, maxUploadSize, createdAt}` (camelCase, booleans coerced). Service-level Phase 2b fields are added: `kind`, `runtime`, `targetIp`, `lxcContainerName`, `dataDir`. `exportData.version` bumped to `'2.0'`. **Backward compat:** legacy top-level route-owned fields (`domain`, `pathPrefix`, `port`, `sslEnabled`, `forceHttps`, `websocketEnabled`, `maxUploadSize`) are RETAINED so Phase 2 importers keep working; D.10 prefers the nested `routes` array when present. Audit payload carries `{count, version: '2.0'}`. **Verified:** Express integration smoke test against 10 assertions — seed `svc-a` (container_service / runtime='lxc' / target_ip='10.0.0.5') with two routes (`a.test/` port 3000 and `a2.test/api` port 3001 maxUpload 500M, both SSL on) and `svc-b` (static_site / type='static') with one route on `b.test/`; POST `/export` with both ids → 200, `version='2.0'`, two services in the payload, svc-a has a two-element `routes` array with correct ids/domains/pathPrefixes/targetPorts/ssl flags/maxUploadSize, Phase 2b top-level fields (`kind='container_service'`, `runtime='lxc'`, `targetIp='10.0.0.5'`) present, legacy top-level fields still populated for backward compat, svc-b exported as `kind='static_site'` with a one-element routes array.

- [x] D.10 Refactor `POST /import` (admin/backend/src/routes/services.js:3562)
      — Accepts both the new nested-`routes` shape (D.9 `version=2.0`) AND the Phase 2 flat shape. For each entry: (1) normalize to `{service fields, routes: [...]}` — if `routes` array is present use it directly, otherwise synthesize a single-element array from the legacy top-level `{domain, pathPrefix, port, ssl flags, maxUploadSize}`; (2) infer Phase 2b service-level fields (`kind`, `runtime`, `target_ip`) from legacy `type` when the payload didn't supply them (`type='static'`→`kind='static_site'`, `type='docker'`→`kind='container_service'`+`runtime='docker'`, `type='proxy'`→`kind='container_service'`+`runtime=NULL`); (3) pre-check every route's `(domain, path_prefix)` tuple against BOTH `service_http_routes` AND the legacy `services` table (wrapped in try/catch for post-D.14 compatibility), collecting colliding service ids; (4) if collisions exist and `overwrite=false` → skip the entire entry; (5) if `overwrite=true` → collect every domain the soon-to-be-deleted services touched (so the regen loop at the end picks them up) then delete the colliding services rows (cascade FK wipes their routes); (6) INSERT the services row with Phase 2b fields AND legacy route-owned columns populated from the PRIMARY route (first element of the routes array) so the pre-D.14 dual-source state stays consistent; (7) call `syncPrimaryRouteFromLegacy` for the primary route, then INSERT each secondary route directly; (8) after the loop, regenerate every touched domain once and reload Caddy. **Verified:** Express integration test against 15 assertions across 4 scenarios with stub caddy binary — (1) nested-shape import: two-route entry with Phase 2b fields (`kind='container_service'`, `runtime='lxc'`, `targetIp='10.0.0.10'`, `lxcContainerName='lxc-a'`) → 200 with one imported service, Phase 2b columns land correctly, legacy columns populated from the primary route (`domain='a.test'`, `port=3000`), both routes present in `service_http_routes` in created_at order with the primary on `a.test/` + 3000 and secondary on `a2.test/api` + 3001 `maxUploadSize='500M'`; (2) legacy flat-shape import: single-entry with `type='docker'` + `target='127.0.0.1'` + `port=8080` + `websocketEnabled=true` + `maxUploadSize='200M'` → `kind='container_service'`, `runtime='docker'`, `target_ip='127.0.0.1'` inferred, one synthesized route row with correct fields; (3) skip-mode collision: pre-seeded route at `(a.test, /)`, import attempts the same tuple → skipped with `Route collision at a.test/`, existing services row at port 3000 untouched; (4) overwrite mode: pre-seeded service+route on `(ow.test, /)` at port 3000, import same tuple with `overwrite=true` → old row deleted (cascade wipes its route), new row imported at port 5555.

- [x] D.11 Refactor `POST /discover/import` (admin/backend/src/routes/services.js:4660)
      — A discovered site becomes one `container_service` / `static_site` with exactly one route at `path_prefix='/'`. The handler now (a) duplicate-checks the domain against BOTH `services.domain` AND `service_http_routes.domain` so the transitional state catches collisions in either source, (b) infers the Phase 2b service-level fields from the discovered `type` (`type='static'` → `kind='static_site'`+`runtime=NULL`+`target_ip=NULL`; `type='docker'` → `kind='container_service'`+`runtime='docker'`+`target_ip=target`; `type='proxy'` → `kind='container_service'`+`runtime=NULL`+`target_ip=target`), (c) INSERTs the services row with BOTH legacy route-owned columns AND Phase 2b columns, (d) calls `syncPrimaryRouteFromLegacy` to mirror the discovered row into `service_http_routes`. If the sync throws (e.g. a UNIQUE violation from a racing route on `(domain, /)`), the services row is deleted and 400 returned so there's no orphan. Audit payload bumped to `{service_id, domain, type, kind, rootDir}`. **Verified:** Express integration test against 9 assertions — (1) discover-import a docker site on `disc.test:4000` with SSL on: 200, `services.kind='container_service'`, `runtime='docker'`, `target_ip='127.0.0.1'`, `domain='disc.test'`, `port=4000`, `ssl_enabled=1`, matching route row at `(disc.test, /, target_port=4000, ssl_enabled=1)`, merged file at `sitesDir/disc.test` exists; (2) duplicate domain on the same `disc.test` rejected with 400; (3) discover-import a static site: 200, `kind='static_site'`, `runtime=NULL`, `target_ip=NULL`.

- [x] D.12 Refactor `GET /api/services` list (admin/backend/src/routes/services.js:944)
      — The list endpoint now uses `SELECT *` from services + a single `SELECT * FROM service_http_routes ORDER BY created_at ASC` (grouped in-memory by `service_id`), so the request is bounded to 2 queries regardless of service count. Each returned service object exposes Phase 2b service-level fields (`kind`, `runtime`, `targetIp`, `lxcContainerName`, `dataDir`) and a nested `routes` array in primary-first order (created_at ASC, matching D.1's primary-route semantics). Legacy top-level route-owned fields (`domain`, `pathPrefix`, `port`, `sslEnabled`, `forceHttps`, `websocketEnabled`, `maxUploadSize`) are retained for backward compatibility — pre-D.14 they're read from the services row directly, post-D.14 they're synthesized from the primary route via `??` fallback. `SELECT *` + optional chaining handles both states in one code path without extra branching. Zero-route legacy-only services (pre-A.3 transitional state) still render with `routes: []` and legacy top-level fields sourced from the services row. **Verified:** Express integration test against 10 assertions — seed three services: `svc-a` container_service LXC with two routes on `a.test` (`/` + `/api`, created_at 2024-01-01 and 2024-01-02 respectively), `svc-b` static_site with one route on `b.test`, `svc-c` container_service with legacy columns on `c.test:7000` and zero route rows; GET → 200, three services returned, svc-a has `routes.length === 2` with `routes[0].id === 'ra1'` (primary first by created_at), Phase 2b fields (`kind='container_service'`, `runtime='lxc'`, `targetIp='10.0.0.10'`, `lxcContainerName='lxc-a'`) present, legacy top-level fields (`domain='a.test'`, `pathPrefix='/'`, `port=3000`, `sslEnabled=true`) still populated, svc-b `kind='static_site'` `runtime=null`, svc-c `routes=[]` with legacy fallback (`domain='c.test'`, `port=7000`), svc-a `isFavorite=true`.

- [x] D.13 Refactor `GET /api/services/:id` single fetch (admin/backend/src/routes/services.js:1056)
      — Same shape as D.12's list endpoint: `SELECT * FROM services WHERE id = ?` + a scoped `SELECT * FROM service_http_routes WHERE service_id = ? ORDER BY created_at ASC` gives a two-query single-service fetch. The response carries Phase 2b service-level fields (`kind`, `runtime`, `targetIp`, `lxcContainerName`, `dataDir`), a nested `routes` array in primary-first order, and the legacy top-level route-owned fields synthesized from the primary route (same `??` fallback pattern as D.12) for backward compatibility with the pre-Section-H frontend. 404 on unknown id is preserved. **Verified:** Express integration test against 6 assertions — (1) seeded container_service LXC `svc-a` with two routes (`ra1` at `a.test/` + `ra2` at `a.test/api`, created_at 2024-01-01 and 2024-01-02), GET `/api/services/svc-a` → 200, `routes.length === 2` with `routes[0].id === 'ra1'` (primary first), Phase 2b fields (`kind='container_service'`, `runtime='lxc'`, `targetIp='10.0.0.10'`, `lxcContainerName='lxc-a'`) present, legacy top-level fields (`domain='a.test'`, `port=3000`, `sslEnabled=true`) still populated; (2) unknown id → 404.

- [x] **D.14: Drop legacy route columns from `services` via table-rebuild** (admin/backend/src/db.js + admin/backend/src/routes/services.js — atomic refactor)
      — Added `dropLegacyRouteColumnsFromServices(db)` helper in `db.js` that inspects `sqlite_master.sql` for any legacy-column marker (` domain `, ` path_prefix `, ` port `, ` ssl_enabled `, ` force_https `, ` websocket_enabled `, ` max_upload_size `) and if found runs a transaction that creates `services_new` with the Phase 2b-only column set (`id, name, kind, runtime, type, target, target_ip, lxc_container_name, root_dir, container_name, data_dir, status, is_admin, is_favorite, created_at, updated_at`), copies rows via `INSERT INTO services_new SELECT ... FROM services` with per-column `has()` coalescing so columns the deployed install lacks land on NULL, `DROP TABLE services`, `ALTER TABLE services_new RENAME TO services`. Wired into `initDatabase()` directly after `migrateServicesToRoutes(db)`. Idempotent — second boot is a no-op via the substring guard. Also added a D.14 guard at the top of `migrateServicesUniqueConstraint` (returns early if `domain` column is gone) and another guard around the Phase 2 `path_prefix` ALTER TABLE (skips re-adding if `domain` is gone), so the rebuilt post-D.14 table isn't accidentally repaired back into legacy shape on subsequent boots. **The endpoint refactor**: because the drop physically removes the legacy columns and `services.domain` is NOT NULL pre-drop, D.14 had to land atomically with the removal of every remaining write to a legacy route-owned column. Changes in `services.js`: (1) `ssl-status/:domain` GET now reads `ssl_enabled` from `service_http_routes`; (2) D.2 POST / uniqueness + SSL stance checks now scan `service_http_routes` via `assertRoutesShareSslStance`, INSERT only writes Phase 2b columns, `syncPrimaryRouteFromLegacy` remains the single source of route-owned state; (3) D.3 PUT /:id fetches the primary route up front to source pre-update values, UPDATE drops legacy columns, rollback snapshots both `services` row and primary route row and reverts both; (4) D.4 obtain-certificate no longer writes `services.ssl_enabled` / `force_https` (route table only); (5) D.5 delete-certificate mirror of D.4; (6) D.6 regenerate-config derives `sslCertificateExists` from the primary route instead of `services.ssl_enabled`; (7) D.8 revert-config UPDATE + rollback UPDATE drop the legacy columns, pre-revert domain sourced from primary route; (8) D.9 export synthesizes legacy top-level backcompat fields from the primary route; (9) D.10 import INSERT drops legacy columns; (10) D.11 discover/import INSERT drops legacy columns; (11) C.5 service DELETE audit payload synthesizes the legacy `{domain, pathPrefix}` tuple from the first cascaded route; (12) `caddy-config` GET + PUT fetch the primary route's domain instead of `service.domain`; (13) `/discover/caddy-sites` lists existing domains from `service_http_routes`. **Verified:** 17-assertion Express integration smoke test — (1) schema post-drop: all 7 legacy columns gone from `PRAGMA table_info(services)`, all 16 Phase 2b columns retained; (2) second `initDatabase()` boot is a no-op (helper guard skips); (3) POST / create end-to-end: 201, services row has `kind='container_service'`/`runtime='docker'`/`target_ip='10.0.0.5'`, matching `service_http_routes` row with `target_port=3000`, merged Caddy file written with `reverse_proxy 10.0.0.5:3000`; (4) GET / list: `routes` array nested, legacy top-level `domain`/`port` fields synthesized correctly from primary route; (5) PUT `{port: 3001}`: services row `updated_at` bumped, route `target_port=3001`, merged file regenerated; (6) obtain-certificate: route `ssl_enabled=1, force_https=1`; (7) DELETE /:id: services row gone, child route cascade-deleted, merged file unlinked.

### E. Backend — LXC integration

- [x] E.1 `GET /api/lxc/containers/with-ip` (admin/backend/src/routes/lxc.js:205)
      — Returns `{containers: [{name, status, ipv4, ipv6}]}` for every `pp-`-prefixed Incus container. Added a small `extractIPv6` helper alongside `extractIPv4` that skips loopback (`::1`) and link-local (`fe80`) addresses. The endpoint filters `incus list --format json` to `pp-`-prefixed instances, strips the prefix from the exposed name, and coerces the status to lowercase. Stopped containers without `state.network` return `ipv4: null, ipv6: null`. **Verified:** Express integration test against 5 assertions with a stub `incus` binary on PATH returning three containers (pp-web running with ipv4=10.0.0.5 + ipv6=fd00::5, pp-db stopped with no state, `other-container` without the pp- prefix) — GET returns 200, exactly 2 pp- containers in the response, `web` has correct `status='running'`/`ipv4='10.0.0.5'`/`ipv6='fd00::5'`, `db` has `status='stopped'`/`ipv4=null`/`ipv6=null`, non-pp container filtered out.

- [x] E.2 `POST /api/services/:id/refresh-ip` (admin/backend/src/routes/services.js:2066)
      — Validates that `services.lxc_container_name` is non-null (400 "Service has no associated LXC container" otherwise). Queries `incus list pp-<name> --format json`, extracts the current IPv4 by walking `state.network`, and if it differs from `services.target_ip` updates the DB row, regenerates the merged Caddy config for every distinct domain the service's routes touch, validates via `caddy adapt`, and reloads Caddy. No-op path (`changed: false`) when the IP is unchanged — still emits an `LXC_IP_REFRESHED` audit row with `old_ip === new_ip` for observability. On any downstream failure (gen / adapt / reload) the handler rolls back the DB row to the prior `target_ip` AND restores every backed-up merged file. Audit payload carries `{service_id, lxc_container_name, old_ip, new_ip}`. **Verified:** Express integration test against 8 assertions with a stub `incus` binary that reads the IP from a scratch file — (1) seeded a service at `target_ip='10.0.0.5'` with two routes on two domains, first POST returned 200 with `changed=true`/`oldIp='10.0.0.5'`/`newIp='10.0.0.99'`, DB row updated to 10.0.0.99, both merged files contain `reverse_proxy 10.0.0.99:<port>`, audit row present with both IPs; (2) second POST (stub IP unchanged) returned 200 with `changed=false`; (3) service without `lxc_container_name` returned 400; (4) unknown service id returned 404.

### F. Backend — Audit log payload updates

- [x] F.1 `SERVICE_CREATED` / `SERVICE_UPDATED` / `SERVICE_DELETED` payloads nest routes (admin/backend/src/routes/services.js)
      — Each of the three `logAudit` calls now emits `{service: {id, name, kind, runtime, target_ip, lxc_container_name}, routes: [{id, domain, pathPrefix, targetPort}], legacy: <original flat payload>}`. Legacy flat fields (the raw `data` / `updatedData` object, plus the legacy `{domain, pathPrefix}` tuple on DELETE) are retained under `legacy:` / top-level for backward compatibility with replay tools built against pre-Phase-2b audit logs. The CREATE payload pulls the newly-inserted primary route from `service_http_routes` immediately after `syncPrimaryRouteFromLegacy` returns; UPDATE pulls every current route for the service (primary-first); DELETE uses the `routeRows` snapshot captured before the DELETE cascade. **Verified:** Express integration test against 6 assertions — (1) POST / create: audit row has `service.kind='container_service'`/`runtime='docker'`, one-element `routes` array carrying `audit.test/`, `legacy.name='AuditSvc'` preserved; (2) PUT port=3001 update: audit row has `service.id` matching and `routes[0].targetPort=3001`; (3) DELETE: audit row has `service.kind='container_service'`, one-element `routes` array, legacy `domain='audit.test'`/`pathPrefix='/'` retained.

- [x] F.2 `ROUTE_CREATED` / `ROUTE_UPDATED` / `ROUTE_DELETED` audit payloads (admin/backend/src/routes/services.js)
      — Verified that the existing C.2 / C.3 / C.4 implementations already emit the required shapes: `ROUTE_CREATED` carries `{service_id, route: {id, domain, pathPrefix, targetPort}}`, `ROUTE_UPDATED` always carries `{service_id, before: {...}, after: {...}}` (the spec asked for before/after only when domain/pathPrefix changed — the implementation is a strict superset that always includes them for richer replay), `ROUTE_DELETED` carries `{service_id, route: {id, domain, pathPrefix, targetPort}}`. These were already verified in the Section C smoke tests (`audit_log ROUTE_CREATED row present with correct service_id and nested route payload`, `ROUTE_UPDATED row from domain-change scenario contains both before + after snapshots`, `audit_log contains two ROUTE_DELETED rows matching the two successful deletes`). F.2 is satisfied by Section C as-is — no further code changes required.

### G. Frontend — api.js client helpers

- [x] G.1 Add `getLxcContainersWithIp` (admin/frontend/src/lib/api.js:381)
      — `getLxcContainersWithIp: () => request('/lxc/containers/with-ip')` placed directly below the existing `getLxcContainers` call. Returns `{containers: [{name, status, ipv4, ipv6}]}` with the `pp-` prefix already stripped. **Verified:** `node --check src/lib/api.js` passes (syntax clean). Functional verification against the E.1 backend endpoint is covered by the E.1 smoke test; the helper is a thin wrapper around `request('/lxc/containers/with-ip')` so its behavior matches the backend response directly.

- [x] G.2 Add route CRUD helpers `getServiceRoutes`, `createRoute`, `updateRoute`, `deleteRoute` (admin/frontend/src/lib/api.js:100)
      — Four helpers added inside the Services block right after `toggleFavorite`: `getServiceRoutes(id) => request('/services/'+id+'/routes')`, `createRoute(id, route) => request('/services/'+id+'/routes', {method:'POST', body: JSON.stringify(route)})`, `updateRoute(id, routeId, route) => request('/services/'+id+'/routes/'+routeId, {method:'PUT', body: JSON.stringify(route)})`, `deleteRoute(id, routeId) => request('/services/'+id+'/routes/'+routeId, {method:'DELETE'})`. **Verified:** `node --check src/lib/api.js` passes. The Section C smoke tests already verified the corresponding backend endpoints return the documented shapes.

- [x] G.3 Add `refreshLxcIp` (admin/frontend/src/lib/api.js:119)
      — `refreshLxcIp: (serviceId) => request('/services/'+serviceId+'/refresh-ip', {method: 'POST'})` placed right below the route CRUD helpers inside the Services block. **Verified:** `node --check src/lib/api.js` passes. The backend E.2 smoke test exercised the endpoint and confirmed the response shape `{success, changed, oldIp, newIp, ...}`.

- [x] G.4 Update the Phase-2 services-section comment (admin/frontend/src/lib/api.js:73)
      — The comment now reads: "Phase 2b audit: a service now represents one logical workload (typically an LXC or Docker container) that can expose multiple HTTP routes via a nested `routes: [...]` array on every GET response. The service is identified by `id` alone — (domain, pathPrefix) is now a property of individual routes in `service_http_routes`, not of the service itself. The only direct domain reference in this file is `checkSslStatus(domain)` (a fire-and-forget GET, no cache). Nothing in this client keys any cache on domain." **Verified:** `node --check src/lib/api.js` passes; comment reads accurately against the refactored endpoints.

### H. Frontend — Add Service wizard (mobile-first)

- [x] H.1 Step 0 kind picker tiles (admin/frontend/src/pages/Dashboard.jsx:2964)
      — Replaced the four-tile grid (Static Site / Proxy Container / Docker Compose / LXC Container) with two top-level tiles: **Static Site** (`data-testid="wizard-kind-static-site"`, `kind='static_site'`) and **Container Service** (`data-testid="wizard-kind-container-service"`, `kind='container_service'`). Grid is `grid grid-cols-1 sm:grid-cols-2 gap-4 py-4` so the picker stays single-column at `<sm`. Shrank the dialog `max-w` from `sm:max-w-4xl` to `sm:max-w-2xl` now that the grid only needs two columns. Added a new `handleKindSelect(kind)` handler that writes `formData.kind` + resets `runtime` to `null` + clears `lxcContainerName`/`targetIp` from any previous pass; for backward compatibility until H.3 lands it still sets the legacy `type` field (`static_site → 'static'`, `container_service → 'docker'`) so the existing Step 1 form continues to render during this intermediate commit. `resetForm()` also resets the four new fields. The Step 1 badge row now renders a human label ("Static Site" / "Container Service") sourced from `formData.kind` rather than `formData.type`. Docker Compose and LXC container *creation* (not proxying) are still accessible via their dedicated dashboard tabs and are no longer tangled into the Add Service dialog. **Verified:** puppeteer at 360×640 and 1280×800 against a live backend+frontend — (1) after logging in (via the real /auth/initial-setup + /auth/complete-totp-setup handshake) and opening the Add Service dialog, exactly two `[data-testid^="wizard-kind-"]` tiles render in a single column at 360 and two columns at 1280; (2) `document.documentElement.scrollWidth === clientWidth` at step 0 and at step 1 for both kinds on both viewports; (3) tile bounding-rect heights on 360 are 196/215 (both ≥ 44 touch-target floor); (4) clicking the Static Site tile advances to step 1 and the badge row contains "Static Site"; (5) clicking Change → Container Service tile advances to step 1 with the badge row containing "Container Service".

- [ ] Step 1 container picker for `kind='container_service'` (admin/frontend/src/pages/Dashboard.jsx, new conditional block inside the wizardStep ≥ 1 branch)
      — When `formData.kind === 'container_service'` and `formData.runtime` is null, render a "Pick runtime" sub-step with LXC + Docker tiles (same grid pattern as Step 0). On LXC select: call `api.getLxcContainersWithIp()`, render a dropdown of the running containers showing `name — ipv4 (status)`, and on selection populate `formData.lxcContainerName` + `formData.targetIp` (both read-only afterwards). On Docker select: render the existing Docker container / IP / port picker from today's wizard, populating `formData.containerName`, `formData.targetIp`, and an initial route port. Static Site skips this step entirely and goes straight to Step 2. **Success:** puppeteer at 360×640 and 1280×800, backend seeded with one LXC container (via a stubbed `GET /api/lxc/containers/with-ip` handler) — pick Container Service → LXC → open dropdown → select the container; confirm the IP field populates, the container name is displayed, and advancing to Step 2 carries the values in `formData`. No horizontal scroll at either width.

- [ ] Step 2 routes builder UI (admin/frontend/src/pages/Dashboard.jsx, new component-scoped block replacing the current single-route inputs inside the wizardStep ≥ 1 branch)
      — Renders `formData.routes` as an array of inline rows, each with `domain`, `pathPrefix`, `targetPort`, an SSL toggle, and a trash icon. An "Add route" button appends a new empty row. Minimum one route — the trash icon is disabled on the last remaining row. Uses `flex flex-col sm:flex-row sm:items-end gap-3` for the row layout so it stacks on mobile and inlines on desktop. Touch targets on the trash + add buttons are `h-11 w-11 sm:h-10 sm:w-10` per MOBILE_FIRST.md §5. Static Site hides the `targetPort` field and shows a single "Root directory" field instead, carried on the service not the route. **Success:** puppeteer at 360×640 and 1280×800 — add a route, edit its domain/prefix/port, add a second route, remove the first route, confirm the array state tracks correctly and the row layout stacks at 360 and inlines at 1280 with no horizontal scroll and with ≥44×44px touch targets on the trash + add buttons.

- [ ] Generalize `existingPrefixesForDomain` memo to all routes across all services (admin/frontend/src/pages/Dashboard.jsx:540)
      — The memo now flattens `services.flatMap(s => s.routes || []).filter(r => r.domain.toLowerCase() === typed)` and returns the `pathPrefix` list. The Phase 2 banner text is unchanged ("Domain already in use. Existing path prefixes: /, /api...") but now scans all routes, not all services. Renders per-route-row inside the routes builder so each in-progress route sees the banner for its own typed domain. **Success:** puppeteer at 360×640 and 1280×800 with a backend seeded with one service that has two routes on `mul.test` (`/` + `/api`) — typing `mul.test` into a new route row in the wizard surfaces the banner with `Existing path prefixes: /, /api`; typing a fresh domain hides it. No horizontal scroll.

- [ ] Client-side collision guard in `handleAddService` generalized to the new payload (admin/frontend/src/pages/Dashboard.jsx:936)
      — Before `api.createService(submitData)`, walk `formData.routes` and for each row: (a) ensure no two in-progress rows in the wizard share the same `(domain, pathPrefix)` tuple (fail with a toast naming the duplicate); (b) ensure no in-progress row collides with an existing route across all services (same toast text the backend returns). **Success:** puppeteer at 360×640 and 1280×800 — add a route that duplicates an in-memory wizard row → toast "Duplicate (domain, path_prefix) in this wizard" and the dialog stays open; add a route that collides with an existing backend route → toast matches the backend error ("Domain + path prefix combination already exists").

- [ ] Create flow POSTs the new nested-routes payload (admin/frontend/src/pages/Dashboard.jsx, inside `handleAddService`)
      — Assemble `submitData = {name, kind, runtime, targetIp, lxcContainerName, rootDir, containerName, dataDir, routes: formData.routes.map(normalizeRoute)}` and send it to `api.createService(submitData)`. Reset form on success, refetch services. **Success:** puppeteer at 360×640 and 1280×800 with a real backend — complete the wizard for a container_service LXC picking a container + adding two routes → 201, toast "Service created", the dashboard refreshes to show the new service card with two routes nested; no JS errors; no horizontal scroll.

### I. Frontend — Service detail / Settings dialog Routes section (mobile-first)

- [ ] Add a **Routes** card to the settings dialog Settings tab (admin/frontend/src/pages/Dashboard.jsx:5035, inside the `settingsTab === 'settings'` branch)
      — New card below the Service Info block titled "HTTP Routes" listing every route for `settingsService` from `api.getServiceRoutes(id)`. Each row: `domain / pathPrefix → :targetPort` + an edit pencil + a delete trash. Inline edit opens a small form in-place (no extra dialog) wiring `api.updateRoute`. An "Add route" button at the bottom of the card opens the same in-place form in create mode. Mobile layout: stacked rows with `flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3`, 44×44px touch targets on edit/delete buttons, card uses `max-w-full h-full rounded-none sm:max-w-<X> sm:h-auto sm:rounded-lg` same as the parent dialog. **Success:** puppeteer at 360×640 and 1280×800 — open the settings dialog on a container_service with two routes, confirm both rows render, add a third route inline → the UI updates and the backend returns 201; edit a route's port → inline form updates the row; delete a route → row disappears. No horizontal scroll.

- [ ] **Refresh IP** button for LXC container_services (admin/frontend/src/pages/Dashboard.jsx, inside the Service Info block at the top of the settings dialog Settings tab)
      — When `settingsService?.kind === 'container_service'` and `settingsService?.runtime === 'lxc'` and `settingsService?.lxcContainerName`, render a small "Target IP: 10.0.0.5" line with a `Refresh IP` button next to it. Clicking calls `api.refreshLxcIp(settingsService.id)` and toasts the old→new IP on success. Button is 44×44 on mobile (`h-11 w-11 sm:h-10 sm:w-10`). A static note beneath reads "Tip: assign a static IP via an Incus profile to avoid needing this after each container restart." **Success:** puppeteer at 360×640 and 1280×800 with a stubbed refresh endpoint — click Refresh IP, confirm the toast appears with the new IP, the settings dialog refreshes the displayed IP, and the static tip is legible at 360 without horizontal scroll.

- [ ] Delete confirmation sibling warning generalizes to routes (admin/frontend/src/pages/Dashboard.jsx:3884)
      — The existing warning counts services on the same domain. Generalize it to count every route on the same domain across all services (minus any routes owned by the service being deleted). Grammar stays singular/plural. The warning surfaces when at least one surviving sibling route exists on ANY domain that the service-to-delete owns a route on — uses a map so the warning text can name each affected domain once. **Success:** puppeteer at 360×640 and 1280×800 — seed a service with a route on `a.test` that also has a sibling route on another service; open the delete dialog, confirm the amber warning lists `a.test` with the correct sibling count; seed a service whose routes have no siblings, confirm no warning renders.

### J. Frontend — Services grid domain grouping generalization

- [ ] Domain-grouped header row generalizes to routes (admin/frontend/src/pages/Dashboard.jsx:3597)
      — The existing `domain-group-header` IIFE groups services by `service.domain`. After Phase 2b a service can touch multiple domains, so re-source the grouping from routes: flatten `services.flatMap(s => (s.routes || []).map(r => ({service: s, route: r})))`, group by `route.domain`, emit a header row when a domain has ≥2 distinct services. Each service card may appear under multiple headers (once per domain its routes touch). Single-service single-domain layouts stay headerless. **Success:** puppeteer at 360×640 and 1280×800 — seed a service with routes on `a.test` + a second service with a route on `a.test` (2 distinct services on `a.test`) + a third service with a route on `b.test` only; confirm exactly one `"2 services on a.test"` header appears above the first two cards, no header above the third, no horizontal scroll.

### K. Verification (mobile + end-to-end)

- [ ] **Mobile: 360px Add Service wizard end-to-end** — puppeteer at 360×640, backend seeded with one LXC container and one existing service with a `/api` route on `mul.test`, run through Step 0 → Container Service → LXC → pick container → Step 2 → add two routes on `mul.test` (`/` + `/v1`) → submit. Expected: successful create, both routes rendered on the dashboard, no horizontal scroll, no JS errors, all touch targets ≥44×44px.

- [ ] **Mobile: 360px Service detail Routes section** — puppeteer at 360×640 on a service with three routes, add a fourth route inline, edit one, delete one; confirm every interaction completes without a horizontal scroll and the in-place form fits the viewport.

- [ ] **Mobile: 360px Delete confirmation sibling warning** — puppeteer at 360×640 on a service whose routes overlap with another service's routes on the same domain; open delete dialog, confirm the amber warning text names the sibling-owning domain(s) and no horizontal scroll.

- [ ] **Mobile: 1280px desktop regression** — puppeteer at 1280×800 running the same three flows (wizard, routes section, delete warning). None of the Phase 1 / Phase 2 baseline layouts regress. `document.documentElement.scrollWidth === document.documentElement.clientWidth` at every screenshot point.

- [ ] **Backend: Caddy `adapt` defense-in-depth** — every generated merged config writes through the production `caddy adapt --config` gate just like Phase 2. The test environment uses a stub caddy binary so real `adapt` is not run, but every create/update/delete path that writes a merged file must exercise the adapt-then-reload sequence through the stub. Documented in the commit note.

- [ ] **Phase-spec Verification checklist sign-off** — walk the "Verification" bullet list at the top of `phase-02b-container-service-model.md` one by one, confirming every item is green by pointing at either an A/B/C/D/E/F/G/H/I/J verification note above OR a fresh check added in this pass. Any gap gets a new checklist item inserted into the relevant section above rather than silently skipped.
