✔ G3 configuration requires verified G2, exact host, explicit role policy and protected refs; stale revision refuses (51.391766ms)
✔ G3 explicit account proof links issuer+subject without changing account data; no email auto-link or silent admin (47.373761ms)
✔ G3 real OIDC validation rejects bad signature, issuer, audience, expiry, nonce, state, PKCE and replay (94.088802ms)
✔ G3 sudo requires the linked subject, fresh required assurance and live initiating session; SSO login never elevates (46.812575ms)
✔ G3 passkey settings and exact client are checked read-only using 26.7.4 resident-key precedence (11.911981ms)
✔ G3 revocation permits at most 60 seconds; disable/delete and outage deny SSO while local recovery stays valid (31.594011ms)
✔ G3 activation enforces configuration-bound administrator, SSO, sudo, route and separate-browser recovery; offline disable works (44.321828ms)
✔ G3 saved configuration invalidates old callback and activation evidence; jobs carry references only and resume safely (14.642235ms)
✔ G3 Caddy recovery route reuses protected IP options, rejects collisions and has a fenced durable job (6.624322ms)
✔ G3 actual local password/TOTP login and step-up work offline on recovery; trusted devices cannot skip its second factor (15.974126ms)
✔ G3 activation closes public local-login bypass while retaining independent local recovery and credentials (625.007058ms)
✔ G5.1 production HTTP admin, CSRF, fresh-auth, strict inputs; save is inert and encrypted (111.823515ms)
✔ G5.1 independent Agent Proxy skip and full skip install nothing (9.024063ms)
✔ G5.2 retry refuses identity/credential rotation and stale or mutable target configuration (6.494837ms)
✔ G5.2 audited exact effective rules reject broader and hidden folder grants (13.230129ms)
✔ G5.1/.5 managed production runtime and Caddy adapters create once, reuse keys and preserve unrelated state (119.185369ms)
✔ G5.2 incomplete handoff fails truthfully and retry preserves services and credentials (115.355417ms)
✔ G5.3 conflicting existing disposable secret is never overwritten (47.819627ms)
✔ G5.1 connect Infisical leaves its runtime/Caddy untouched; Agent Proxy skipped reports application only (17.614137ms)
✔ G5.1 existing Agent Proxy verification is read-only and refuses changed command/environment/listeners (7.760692ms)
✔ G5.5 foreign Docker collision and missing protected keys fail without replacement (8.074575ms)
✔ G5.5 failed runtime and Caddy failures retry with the same references; raw failures redacted (131.511803ms)
✔ G5.5 runner interruption reconciles and resumes from saved keys; backend never runs host setup (70.356811ms)
✔ G5.5 recorded slow route child keeps parent queued; unrelated VM lock prevents delivery (62.267215ms)
✔ G5.4 isolation refuses containers, shared devices, raw VM options and non-local private listeners (0.595117ms)
✔ G5.5 job schema refuses commands/secrets; review token and platform intention are mandatory (5.9329ms)
✔ G5 transport pins DNS, refuses redirects and strips error bodies (1.029277ms)
✔ G5.5 real API process restart preserves encrypted references and queued job (879.65802ms)
✔ G5.4 retry rejects volume driver and private network drift before proxy start (9.170399ms)
✔ G5.1 Connect accepts an existing dedicated bridge/volume without renaming or writing external resources (8.254199ms)
✔ actual API → saved revision → durable external job → runner verification; auth, CSRF, fresh auth, stale and skip refusals; no takeover (980.213352ms)
✔ managed operation uses actual executor, protected files and route SQL/render handoff; readiness + discovery required; no duplicate resources or rotation (486.797052ms)
✔ slow recorded Caddy handoff keeps Keycloak queued under the route locks and automatically completes verification (24.082412ms)
✔ dead runner after database creation is reconciled and resumed using the same credentials/resources; failed public verification remains failed and retry reuses (42.707682ms)
✔ runner validation, collision refusal, unavailable runtime and backend policy do not mutate unrelated resources (6.148451ms)
✔ discovery refuses wrong issuer, cross-origin keys and unusable keys; network rejects loopback, link-local, mapped IPv6 (2.515543ms)
✔ real HTTPS transport: TLS hostname/CA validation, pinned DNS, no redirects, bounded body and successful discovery/key parsing (151.848272ms)
✔ current D.14 service schema works; Caddy collisions and render failure preserve other routes; lease loss stops writes (5.204913ms)
✔ actual loss of runner epoch fences the operation before further runtime writes; long operations renew runner and job evidence (2.916864ms)
✔ API restart reconciliation resumes an interrupted Caddy handoff with the same route and no new credentials (27.304496ms)
✔ database readiness failure never schedules a public route or records verified state (6.859959ms)
✔ G6.4 production credential-flow adapter issues/uses, proves denied write, revokes its token and rejects a new connection (scripted PostgreSQL) (1.234217ms)
✔ G6.4 outage after revocation is not accepted as credential denial; excessive grants fail and cleanup revokes (0.63211ms)
✔ G6.4 selected database preflight rejects superuser and accepts only the intended disposable target (0.366009ms)
✔ G6.4 public CA selection is bounded and never accepts a private key; both consumers verify TLS (4.268374ms)
✔ G6.1 HTTP admin/CSRF/fresh-auth, inert saved plan and encrypted separate credentials (99.482533ms)
✔ G6.1 skipped/changed plan and unsupported external initialization install nothing (7.53258ms)
✔ G6.1/.2 production Docker/Caddy adapters, encrypted initialization and receipt; retry never initializes twice (82.836036ms)
✔ G6.2 lost/interrupted handoff is recovery required without reset or second init (6.737468ms)
✔ G6.2/.3 operator shares/bootstrap transient, scoped owned resources, repeatable credentials and preservation (71.952795ms)
✔ G6.3 machine positive/denied checks, broad policy and owned-config drift rejected (38.423635ms)
✔ G6.3 dedicated Keycloak client exact callback/groups verified read-only; unsafe clients refused (6.153639ms)
✔ G6.5 connect runtime remains untouched; seal/unavailable and identity changes never verify (63.146125ms)
✔ G6.5 runner restart reconciliation and backend refusal preserve the queued revision (64.910258ms)
✔ G6.5 interrupted operator request is recorded without replay or a guest recovery instruction (6.890483ms)
✔ G6.1/.5 foreign resources, runtime drift and missing configuration fail without replacement (6.812119ms)
✔ G6.5 runner job schema refuses command and secret additions (6.376027ms)
✔ G6.2 plaintext initialization response is refused without writing recovery material or retrying init (5.802432ms)
✔ G6.3 connect collisions and missing registered machine credential preserve foreign resources and never reissue (53.144106ms)
✔ G6.5 slow recorded route child keeps parent queued; unrelated lock refuses operator share (42.359169ms)
✔ G6.1 managed transport refuses redirects and special-use destinations; sealed and unavailable states differ (1.436194ms)
✔ G6.4 managed public CA mount is read-only, reused and drift-protected (6.611723ms)
✔ G6.5 failed first bootstrap still binds the observed cluster before retry (13.921014ms)
✔ strict non-secret schema: all five choices, origins, no browser state or credentials (6.774673ms)
✔ empty inventory never declares fresh or installed; intended state remains separate (2.813823ms)
✔ preflight uses existing read interfaces, reports missing capabilities and route/dependency conflicts truthfully (8.344854ms)
✔ live and stale runner evidence; runtime responses; missing capacity never reads as pass (1.437787ms)
✔ real HTTP save → reload → separate API process restart → reopen; auth, CSRF, fresh auth, CAS and unchanged settings (969.277422ms)
✔ G4 API enforces admin, CSRF, sudo, strict inputs and secret-free inert save; skip is inert (107.776042ms)
✔ G4 preserves G3 and refuses its client, missing verification, stale revisions and secret rotation (6.054042ms)
✔ G4.2 validates exact dedicated Keycloak client, callbacks, flow and S256; wildcards fail (0.422081ms)
✔ G4.3 review binds full route and small verified subject allow list; machine and unsupported routes refuse (5.803644ms)
✔ G4.3 SQL route ownership prevents aliases, edits, delete and moving the protected upstream (4.032286ms)
✔ G4.3 native private listener proof rejects wildcard, duplicate public listener, Docker publish and missing process identity (0.496341ms)
✔ G4.4 API → durable runner → existing Caddy renderer; repeat apply reuses credentials and routes (47.418998ms)
✔ G4.4 failed runtime leaves denied route; retry uses same resource/credentials and succeeds (26.891377ms)
✔ G4.4 invalid Caddy apply retains denial, does not restore old direct file, and retries durably (25.781044ms)
✔ G4.4 restart/retry resumes saved intent and fenced writer issues no later mutation (21.280808ms)
✔ G4.4 explicit removal requires new review, preserves route restrictions and never drops native app authentication (31.103859ms)
✔ G4.1 external handoff verification preserves unrelated routes and refuses overlapping/weakening policy (4.518042ms)
✔ G4.1 protected files retain credentials, reject drift/missing marker, and never emit secrets to state/jobs (3.901423ms)
✔ G4.6 probes perform real HTTP command contracts, reject direct success and inspect spoof refusal (0.82215ms)
✔ G4.6 test app executes signed assertion contract: authorized identity, forgery, spoof, audience, issuer, expiry and direct access (9.722253ms)
✔ G4.4 failed gateway verification queues durable denial before reporting failure (20.922972ms)
✔ G4.4 unavailable Caddy never certifies initial denial and names the remaining runtime bypass (6.972404ms)
✔ G4.1 existing Core connection is read-only, preserves its keys and unrelated route, and retries without restart (5.445035ms)
✔ G4.4 real API process restart retains encrypted references, policy and queued job; repeat submission is idempotent (861.715939ms)
✔ G4.5 dashboard, identity, recovery and machine API route rendering remains byte-identical (28.83149ms)
✔ G4.1 official image supports repeat runtime apply and protecting a route after installation (32.121084ms)
✔ G4.1 only the exact pinned CA default is allowed; unauthorized environment overrides remain blocked (4.154266ms)
✔ refusal (real process): with no mechanism the wrapper prints CONTAINMENT:none, exits 97 and never runs the body (12.644878ms)
✔ refusal on the record: a guest without containment ends the deploy as recovery_required/containment_unavailable with nothing executed and the lease released; the app deploys once the guest has a mechanism (22.773911ms)
✔ superseded by what runs: a follow-up that finds another commit/build records `superseded` (value null) on both records and certifies nothing (8.384084ms)
✔ an older follow-up queued behind a newer deploy is an obligation kept: it waits out the deploy's lease, records superseded when the deploy changed what runs, and still verifies its revision when the deploy failed before changing anything (24.904728ms)
✔ a recovery that brings an interrupted deploy's app up still owes the application-owned check: queued before the recovery reports done, landing on the recovery and on the deploy it recovered (6.978564ms)
✔ a follow-up that meets a held lease is requeued with a not-before, skipped by the claim until due, and runs afterwards; a plain probe is still deferred (12.772015ms)
✔ a deploy that fails after the stop queues a post-failure verification; it runs and lands a checked state on the failed record (6.658714ms)
✔ ratchet: install.sh writes backend-allowed on a fresh install, preserves an existing policy on re-run, promotes to runner-required only after the unit is active AND the runner opened the database, never downgrades, and errors loudly otherwise (1.417152ms)
✔ ratchet: update.sh records runner-required only on the same evidence, and warns in red when the runner is not there (including when the policy already names it) (0.558072ms)
✔ ratchet: the backend warns when policy runner-required has no live runner heartbeat, and never drains jobs in-process under that policy (0.450553ms)
✔ ratchet: the executor reads the guest revision before the application-owned check and the reaper counts only live processes (0.714562ms)
✔ the application rung is a persisted follow-up: queued before the deploy reports done, survives an API restart, runs exactly once, lands on both records (24.802928ms)
✔ the application rung has distinct outcomes: verified, failed (legacy or unreadable rows behind a 200), no protected credentials, no verification credentials, unreachable, superseded (69.576381ms)
✔ the maintenance boundary sits before the migration; protected copies are retained versions; a migration in flight or failed is recorded with its retry class and never rolled back on its own (18.056506ms)
✔ containment on the record: a survivor after the reap ends the job as recovery_required with the lease KEPT and flagged, and conflicting operations are refused with that reason (12.637166ms)
✔ executorPolicy: explicit values, the safe reading of an unknown value, the documented default (1.29042ms)
✔ runner-required: with no runner or a stale heartbeat the deploy is QUEUED and reported unavailable; no guest command runs in the backend; a fresh heartbeat hands it to the runner (16.823916ms)
✔ backend-allowed (legacy / development): with no runner the backend runs the SAME executor in-process — deploy, then the follow-up verification — with its own owner, and defers to a live runner (9.982588ms)
✔ index.js drains in-process only under backend-allowed, and the runner is the shared executor (4.140256ms)
✔ deploy-op: the full path — reap, install/migrate/build, checkpoint BEFORE the stop, mint, unit, start, health, verify; recovery references outlive the stopped-app marker (18.000383ms)
✔ deploy-op: a repeat or retry mints nothing new and reuses what exists; an unchanged manifest skips the install (2.613988ms)
✔ deploy-op: a stored row under the development key with the marker present mints through the bridge; a marker missing defers the key and says why (2.658815ms)
✔ deploy-op: failures are distinct and never leave the app down silently — install (before the stop), stop, unit start (restart attempted), health (5.177083ms)
✔ deploy-op: a responding port with an unverified or failed credential is exactly that (2.391611ms)
✔ deploy-op: no run contract → skipped; the contract is read from the guest when the plan has none; a previous writer that survives the reap refuses the deploy (1.337239ms)
✔ interpretCredentialUse: the application-owned rung reads the app's own answer (0.427619ms)
✔ submitDeployJob validates and de-duplicates; the runner executes the job to a terminal record every caller reads back as the same result shape (19.623703ms)
✔ waitForJob replays step events to the caller and returns the terminal row; a runner heartbeat is what makes the backend hand a deploy over (9.231412ms)
✔ deployProject with a live runner submits and returns the job id (detach) without touching any guest (4.618941ms)
✔ cancel: honoured at a safe checkpoint before the stop (nothing stopped); after the stop the deploy finishes bringing the app up and records the cancel as declined (11.761818ms)
✔ fence: when ownership moves mid-deploy the runner stops before its next guest command; the new owner's record is untouched by it (2.41768ms)
✔ interruption at every checkpoint: a dead owner is reconciled to resume, recover or verify — never silently cleared (42.084283ms)
✔ the backend boot sweep reads a dead runner deploy the same way, and never touches a live one (1.41235ms)
✔ contention: a restore is refused while the runner deploys (no change), and a deploy defers while a restore holds the app (no guest command) (3.096609ms)
✔ a guest script carrying the deploy marker is really killed by the reap and none survive (real child process) (1265.30828ms)
✔ a deploy submitted by a process that then exits is executed by the runner (real child process) (144.358306ms)
✔ every /api/setup mutation is admin + fresh sudo behind the global CSRF check, audited; the runner accepts only its kinds and validated parameters (0.926414ms)
✔ owner identity round-trips and a fresh instance is a different owner (1.685162ms)
✔ lockVerdict: free, reasserted by the same owner, held by a live one, STALE when the lease lapsed — and stale is not free (0.591401ms)
✔ store: acquire, renew, release are fenced by owner and epoch; a stale lease refuses an ordinary acquirer (1.415274ms)
✔ store: a takeover bumps the epoch, fences the dead holder, and is recorded on the job (2.367065ms)
✔ store: claimNextJob is compare-and-swap — two claimants, one winner; only listed kinds; the epoch moves (1.370858ms)
✔ store: checkpoint, generated resources, finish — fenced; a finished job takes no more writes (1.464496ms)
✔ redaction: secret-looking keys and values never land in a job row or an event (1.329377ms)
✔ verification ladder: states are distinct, a port answering is never "healthy", an unchecked rung caps the state, a failed rung is recovery required (0.455311ms)
✔ reconcileDecision: live lease → nothing; dead before the stop → record interrupted; dead after the stop → recover (or record recovery required when nothing can act) (1.796054ms)
✔ recoveryJobFrom carries references only, from the checkpoint and the config refs (1.334124ms)
✔ boot sweep: the successor records what its predecessor left — interrupted-and-released, or recovery-required with a queued recover_app and a stale, kept lease (3.538991ms)
✔ boot sweep never touches a runner's jobs or a job whose lease is live (0.874818ms)
✔ retryPlan keeps the approved plan and lists every generated resource for reuse; runner jobs are validated (1.155732ms)
✔ requestAppRecovery does not duplicate an open recovery job, and verify-only is its own kind (0.806297ms)
✔ withContainerLock (persistent): the lease and the job exist while fn runs, fn gets a checkpoint handle, and both are closed with the outcome (125.482284ms)
✔ withContainerLock (persistent): a failed result and a throw are both recorded, sanitized, and the lease released (3.658316ms)
✔ withContainerLock (persistent): refused by another live holder (the runner), and refused with the RECORDED condition when a dead holder's lease is stale (1.397107ms)
✔ withContainerLock without a configured store behaves as before: in-process only, fn gets a no-op handle (0.103762ms)
✔ the migration text is the store's schema, registered as 1000 in db.js (1.033131ms)
APP  OWNER  OPERATION  EPOCH  STALE  RECOVERY JOB
─────────────────────────────────────────────────
ID        KIND        APP   STATUS     PHASE       STATE            REASON                                               
─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
0ed762d7  verify_app  pp-x  succeeded  probe_port  port_responding  pp-x: running: port responding (not verified healthy)
✘ unknown action 'bogus' (serve, once, reconcile, status)
✔ probe scripts refuse unsafe unit names, ports and env paths, and parse their own markers (2.753043ms)
✔ credentialVerdict follows the gate-one MASTERKEY_ROWS codes and never returns the key (1.799079ms)
✔ verificationFromObservations climbs the ladder only as far as what was observed, and a port answering after an inactive status read is the port's call (0.648165ms)
✔ executeJob recover_app: starts the stopped unit, probes, verifies the credential, records credential_decryptable and releases the lease (9.445747ms)
✔ executeJob: a port that never answers is recovery_required with the procedure, never "recovered"; an unhealthy app likewise; a mismatched key is recovery_required at the credential rung (6.605455ms)
✔ executeJob: no data guard → app_healthy with the deferral named; verify_app never starts the unit; a refused job kind or a plan with a command is refused before any guest command (2.781886ms)
✔ executeJob: a live foreign lease defers the job; a dead backend's lease is taken over (epoch bumped) and the origin job gets the result (4.147837ms)
✔ executeJob: the fence — when ownership moves mid-run, the runner stops before the next guest command and writes nothing more (1.639094ms)
✔ reconcile: a dead backend that stopped an app gets a recovery job and a stale, kept lease; one that had not gets interrupted and released; a dead runner's resumable job is requeued and then runs (13.531764ms)
✔ the backend boot sweep and the runner reconcile agree: a sweep-queued recovery is what the runner executes, once (3.828356ms)
✔ serve: reconciles on start, drains the queue each tick, stops when asked (2.279106ms)
✔ setup-runner command: root only; once/reconcile/status over the file-backed database (8.306571ms)
✔ hostGuestExec runs `incus exec <guest> -- sh` with the script on stdin and bounds it with a timeout (28.423599ms)
✔ the unit runs the installed CLI as root with Restart=always, and install.sh / update.sh install and enable it (2.760284ms)
✔ G3 credential transport uses real TLS, pinned DNS, exact origin, redirect refusal and bounded responses (259.77139ms)
✔ U1: failed PM2/native startup restores the backup only after its database writer stops (414.429812ms)
✔ U1: current layout stops an open runner, rebuilds, restarts and checks the final path (149.553135ms)
✔ U1: current recovery for false closes both writers and restores the matching layout (279.340152ms)
✔ U1: current recovery for exit 17 closes both writers and restores the matching layout (294.928839ms)
✔ U1: current recovery for kill -TERM $$ closes both writers and restores the matching layout (281.934382ms)
✔ U1: current recovery for kill -INT $$ closes both writers and restores the matching layout (239.004419ms)
✔ U1: legacy layout stops an open runner, rebuilds, restarts and checks the final path (208.970878ms)
✔ U1: legacy recovery for false closes both writers and restores the matching layout (363.2723ms)
✔ U1: legacy recovery for exit 17 closes both writers and restores the matching layout (312.756223ms)
✔ U1: legacy recovery for kill -TERM $$ closes both writers and restores the matching layout (340.094554ms)
✔ U1: legacy recovery for kill -INT $$ closes both writers and restores the matching layout (317.019834ms)
✔ U1: unsafe stop {"refuseStop":true} refuses relocation AND restore with the connection active (54.302854ms)
✔ U1: unsafe stop {"falseStop":true} refuses relocation AND restore with the connection active (56.902722ms)
✔ U1: unsafe stop {"queryFail":true} refuses relocation AND restore with the connection active (50.195737ms)
✔ U1: no backup WAL, real failed-update WAL/SHM: restore removes stale sidecars after stopping the runner (48.995512ms)
✔ U1: a WAL-bearing backup restores its own WAL; SHM is rebuilt, never copied (81.579525ms)
✔ U1: legacy failure {"failBuild":true} restores original path and config before runner readiness (266.783051ms)
✔ U1: legacy failure {"failEnvWrite":true} restores original path and config before runner readiness (267.87579ms)
✔ U1: failure to stop Docker refuses restore and does not restart services (95.815497ms)
✔ U1: a runner that refuses to stop during failure recovery prevents restore (225.938856ms)
✔ U2: updating the unit preserves existing CLI data and configuration (58.98172ms)
✔ U1: a pre-runner installation can relocate and install its first runner (140.655172ms)
✔ U2: unit grants only the existing CLI DB and atomic sysctl directory writes, preserving hardening (1.020612ms)
ℹ tests 184
ℹ suites 0
ℹ pass 184
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 17284.787453
