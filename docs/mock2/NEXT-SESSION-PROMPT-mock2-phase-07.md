You are continuing the build of the Mock2 module inside ProxyPilot. A planning session read the whole codebase and produced the plan you are executing — do not re-derive the architecture, and do not trust your instincts over the plan's citations; they were verified against the code. Phases M0 (skeleton + absence), M1 (parent domains + per-slug TLS), M2 (project registry + container + bare repo + live URL), M3 (archive + rehydrate + idle-stop groundwork), M4 (network isolation) and M5 (connectors + quotas + framework registry) are built and merged to main. Your phase is M6, and only M6.

M6 is the **cycle runner and checkout lock** — the machine that execs into a fenced container, makes a targeted change, runs the pinned gates, and checkpoints, triggered by a canned API call (no chat yet — chat is M7). It is the first phase that consumes everything M5 built: the pinned model slots, `canStartCycle`, the pinned gate scripts, and the already-fenced M4 container whose only egress is the M4 squid proxy.

## What M0–M5 already shipped (do NOT re-implement)

**The gate + module + DB (M0).** `admin/backend/src/mock2/gating.js` — `resolveMock2Gate(...)` (pure), `MOCK2_ENABLED` (default false) + the hard-off pin. Conditional dynamic-import wiring in `admin/backend/src/index.js` (after `/api/notifications`): when enabled it imports `./mock2/index.js`, runs `initMock2Db()` + `sweepMock2OnBoot()` + `seedFrameworkV1(null)` + `reconcileMock2Domains()` + `reconcileMock2Firewall()` + `reconcileMock2Egress()` + `sweepIdleStops()`, and mounts `app.use('/api/mock2', authenticateToken, createMock2Router())`. Add M6 routes to `createMock2Router()` in `routes.js`. An `/api` JSON-404 guard sits before the SPA catch-all — keep it. `mock2/db.js` opens `data/db/mock2.db` (WAL, busy_timeout, 0700/0600), applies block-500 migrations via the main DB's `runMigration()` guard, foreign keys OFF, secrets encrypted at rest with `TOTP_ENCRYPTION_KEY`.

**Migrations 500–505 have run; your M6 tables ALREADY EXIST.** Block 502 defined `mock2_locks`, `mock2_cycles`, `mock2_change_records`, `mock2_audit_questions`, `mock2_queue_items`. Block 500 defined `mock2_locks`. So M6 is mostly runner + lock + state-machine logic + routes, NOT schema. **If you need a new column, append a new migration 506+ — never edit 500–505** (505 is M4; M5 added NO migration — 503 already had the connector/quota/framework tables). Migration 506 is unused and free if you need it.

**Connectors + slots + prices + quotas + git connectors + framework registry (M5).** All CRUD + test endpoints on the backup-destinations pattern:
- `mock2/connectors.js` (data access) + `mock2/connector-logic.js` (pure). `getSlot(slot)` / `listSlots()` return the pinned connector+model for each of the **7 slots** (`concept_chat`, `mockup`, `audit`, `classifier`, `build_runner`, `summary`, `remediation`). Capability enforcement is real: `slotAssignmentError(capabilities, slot)` rejects a chat-only model for `build_runner`/`remediation` (they require `agentic_build`). `decryptConnectorKey(row)` gives the plaintext key **orchestrator-side only** — the M6 runner reads it to call the model; it never enters a container. `publicConnectorShape` never returns the key. Providers: anthropic, openai, gemini, ollama, openai_compatible. Default cloud model ids are NOT hardcoded anywhere — the admin sets `model` per slot; when you write the runner's model call, load the **claude-api skill** and default to the latest Claude models (Opus 4.8 / Sonnet 5 / Haiku 4.5), never a stale id.
- `mock2/quotas.js` + `mock2/quota-logic.js`. **`canStartCycle(estimate, quota, usage) → {ok, reason}` is built and unit-tested** (`mock2-quotas.test.js`). M6's cycle-start MUST call it: `getApplicableQuota(projectId, period)` → `periodUsage(...)` → `canStartCycle({estCostCents}, {budgetCents, bufferPct, maxConcurrentCycles}, {spentCents, runningCycles})`. A false result is the real terminal cycle status `refused_quota` (migration 502 CHECK). Write spend to the ledger with `insertLedgerEntry(...)` after each model call; `costCentsForUsage(usage, price)` prices it. R5 stands: the estimate is an ENVELOPE (per-stage p90 × safety factor) — the mid-cycle buffer stop is the real guard.
- `mock2/git-connectors.js` + `mock2/git-logic.js`. `getProjectRemote(projectId)` + `exportProjectZip(repoPath)` exist. **Orchestrator-side push after checkpoint is the M6 hook**: if a project has a remote with `push_on_checkpoint`, push from the bare repo after the checkpoint fetch (credentials never enter the container — ADR-006). `mock2_project_remotes.last_push_at/last_push_error` are the status columns.
- `mock2/framework.js` + `mock2/framework-logic.js`. `getCurrentFrameworkVersion()` returns the row a new cycle pins; `getFrameworkVersion(id)` fetches a pinned version's content. **The gate scripts live in `gates_json`** (a JSON array of `{name, script, order}`) on the pinned version — M6 copies THESE (the pinned version's, never "latest") into the container at cycle start (ADR-003). The framework registry has an admin-gated editor (publish → v2, revert → v3 carrying old content) and `logAudit` on publish/revert. **Seed v1 is a vendored PLACEHOLDER (risk R8 — see below).**

**M5 → M4 seam (already wired).** `mock2/egress.js` `reconcileMock2Egress()` now folds `configuredConnectorEgressHosts()` (from `connectors.js`) into every project's squid allowlist, so a configured connector's API host is reachable from project containers; the static `DEFAULT_EGRESS_ALLOWLIST` (network-logic.js) is the fallback. Connector create/update/delete re-reconcile egress. **You do not re-plumb egress for the runner** — `template.buildContainerSetupScript` bakes `HTTP(S)_PROXY`/`NO_PROXY`, and the runner's exec-in-container inherits it.

**M4 fence (build on it, don't touch its internals).** Each project has its own bridge `m2br<id>` on a deterministic /24; an nftables fence (dedicated `table inet mock2`) default-denies egress and non-web inbound; squid is the only egress path. The runner execs into an **already-fenced** container — that is the whole point of M4 preceding M6.

**Module conventions to mirror (R7 naming — the AI build component is the RUNNER, `mock2/runner.js`, NOT `agent`):** pure decision layer in a `*-logic.js` (unit-tested stub-first — import ONLY the pure module, never `db.js`/native), thin native data-access in a sibling module, routes in `routes.js` with Zod + `{ error }` shape + `requireAdmin`/`requireSudo`/`requireMock2Role` + `logAudit`. The house 202+poll job pattern (`activeCreations`, `lxc.js:738-1143`; mirrored in `mock2/provision.js`) is the model for the cycle job. The boot orphan-sweep pattern is already stubbed in `db.js` `sweepMock2OnBoot()` — it fails any `running`/`estimating` cycle at startup.

## Read these first, in order (all in docs/mock2/)
1. `04-phased-plan.md` — your phase is **M6**. Read its verify checklist.
2. `02-adrs.md` — **ADR-004** (checkout lock: one per project, last-write idle timer default 15 min via `mock2_settings`, checkpoint-then-release, auto-release always commits first or escalates to `awaiting_admin`, warn-before-expiry, takeover request, admin force-release audit-logged, viewers never acquire; the lock guards the CONTAINER so branch-per-user is foreclosed) and **ADR-003** (framework pinning: the cycle snapshots `current_framework_version_id` at start into `mock2_cycles.framework_version_id`, copies THAT version's gate scripts into the container, change records stamp it immutably).
3. `03-data-model.md` — `mock2_cycles` (the status state machine + `interrupt_request` + `retries` + est/used tokens/cost), `mock2_change_records` (append-only, hash-chained: `hash = sha256(prev_hash + canonical_json(payload))`, mirrored to the repo as `state/changes/<seq>.json`), `mock2_locks`, `mock2_queue_items` (`retries_exhausted`, `quota_exhausted`).
4. `05-risks-and-open-questions.md` — **R5** (cost estimation speculative — canStartCycle is the envelope, mid-cycle buffer stop is the guard), **R4** (SQLite write pressure — keep cycle transactions short, WAL + busy_timeout already set), **R3** (host ops through `lib/host-exec.js` / `mock2/host.js`), **R8** (framework content status — below).

## Scope of this session — Phase M6 only
- **`mock2_locks` per ADR-004 exactly**: last-write idle timer, checkpoint-then-release, warn+keep-working, takeover request, admin force-release, viewer exclusion. Every mutating project endpoint checks it. Pure decision layer in a `*-logic.js` (who-holds, time-remaining, may-acquire, should-auto-release) unit-tested stub-first.
- **`mock2_cycles` state machine**: status table + boot orphan-sweep (extend the existing `sweepMock2OnBoot` — it already flips `running`/`estimating` → `failed`) + poll endpoint (the house pattern). Cycle start: estimate → `canStartCycle` quota check (`refused_quota` terminal) → pin `current_framework_version_id` → take lock → copy pinned gate scripts into container.
- **The runner** (`mock2/runner.js`): provider-agnostic agentic loop driven by the `build_runner` slot (tool set: exec-in-container, read/write file via `incus file push/pull`, run gates). Checkpoint commit after each targeted change and each green gate battery; orchestrator fetch into the bare repo after each checkpoint; hash-chained change record per checkpoint; **orchestrator-side push if the project has a `push_on_checkpoint` remote** (M5 git-connectors). Load the **claude-api skill**; default to the latest Claude models; use the per-slot `model` string, never a hardcoded id.
- **Interrupts**: `queue_after_step`/`stop_after_step`/`abandon` honored at step boundaries; admin stop-all; retries-exhausted → `awaiting_admin` + `retries_exhausted` queue item + handoff (container name, branch, log). Gate reports stored per gate ("review, not error"). Buffer-crossed mid-cycle: finish current step, checkpoint, stop, say so.
- **Frontend (MOBILE_FIRST)**: a canned "run a cycle" control on ProjectDetail + the cycle status/gates-going-green view + lock banner (holder + time remaining + request-takeover). Reuse the phase-stepper pattern (`LxcContainers.jsx:2304-2340`). No chat yet (M7).
- Tests stub-first so the suite gains no NEW failures — the 4 pre-existing native-module failures (`cves`, `incus`, `webauthn`, `vpn-mtu`) are in `docs/known-issues.md`; leave them.

## Do NOT implement (later phases)
Chat / mockups / design approval (M7); audit / rule questions / admin queue UI / drift (M8); classifier / adaptive summary / idle-stop enforcement (M9). Do NOT re-touch the M4 fence internals or the M5 connector/quota/framework CRUD beyond calling their read APIs. Absence (ADR-001): the runner is backend code reached only through the gated router — "the agent binary is not installed" maps to "the runner code path is never imported" on a disabled host.

## Hard constraints
Naming (R7 — `runner`, never `agent`). Absence (ADR-001). Migrations append-only (506+; 502 already has your tables). Docker/nsenter (R3 — container exec + host ops via `mock2/host.js` / `lib/host-exec.js`). Zod, `{ error }` shape, `requireAdmin`/`requireSudo`/`requireMock2Role`, `logAudit`. Change-record hash chain must verify. Tests stub-first. Frontend passes `MOBILE_FIRST.md`.

## What "done" looks like (the M6 verify checklist in 04-phased-plan.md)
Scripted cycle ("add /health endpoint") on a template project: lock taken → checkpoints appear in the bare repo → gates run → green → lock released. Kill the backend mid-cycle → boot sweep fails the cycle, branch at last checkpoint, lock released with commit. Quota set to $0 → cycle refuses to start (`refused_quota`). Force-release is audit-logged. Chain verification over change records passes. Plus `cd admin/backend && node --test 'src/__tests__/*.test.js'` (no NEW failures) and `cd admin/frontend && npm run build`. A `MOCK2_ENABLED=false` host stays byte-for-byte unchanged.

Commit as `mock2-M6: <description>`. (M0–M5 merged to main; branch M6 off main, open its own PR.)

Before you finish: write `docs/mock2/NEXT-SESSION-PROMPT-mock2-phase-08.md` for Phase M7 (chat, mockup, design approval — ADR-002 / the `docs/mock2/design/` reference) in this same format: what M0–M6 shipped (especially the runner, the lock, the pinned framework + gate-copy mechanism, and the `concept_chat`/`mockup` slots the chat stage calls), plan-bundle pointers, and M7 scope. Restate the R8 framework-content status.

## ⚠ External prerequisite carried forward (R8) — still unmet
Framework seed v1 is a **clearly-labelled placeholder** vendored under `admin/backend/src/mock2/framework-seed/` (constitution/skills/gates are stubs; the design system is grounded in the operator's `docs/mock2/design/07-chat-mockup-design-reference.md` but marked provisional). The operator's REAL framework content — constitution prose, the four skills, gate scripts, the locked design system, the project template — was NOT supplied in M5 and is still owed. M6 pins and copies whatever gate scripts the current version carries, so **on placeholder gates the runner runs a green no-op gate**. This does not block M6's plumbing, but flag in your summary that real v1 content is still owed, and that M6 (and M7's design-system-constrained mockups) run against placeholder framework content until it arrives. Once supplied, publish it through the admin editor as a new version (content rows are immutable — never edit the seeded v1 row in place).
