You are continuing the build of the Mock2 module inside ProxyPilot. A planning session read the whole codebase and produced the plan you are executing — do not re-derive the architecture, and do not trust your instincts over the plan's citations; they were verified against the code. Phases M0 (skeleton + absence), M1 (parent domains + per-slug TLS), M2 (project registry + container + bare repo + live URL), M3 (archive + rehydrate + idle-stop groundwork), M4 (network isolation), M5 (connectors + quotas + framework registry) and M6 (cycle runner + checkout lock) are built and merged to main. Your phase is M7, and only M7.

M7 is **Stage 1 — Concept: chat, mockup, design approval** — the first user-facing stage. A Builder describes an idea in a chat panel; the platform generates a non-functional, interactive HTML mockup rendered live at the project's preview URL; iteration is conversational; and the *only* exit is a design-approval gesture that extracts a structured design inventory, discards the mockup code, and unlocks Build. It is the first phase that consumes the `concept_chat` and `mockup` model slots M5 pinned, and the first that writes to a project through chat rather than a canned API call.

## What M0–M6 already shipped (do NOT re-implement)

**The gate + module + DB (M0).** `admin/backend/src/mock2/gating.js` — `resolveMock2Gate(...)` (pure), `MOCK2_ENABLED` (default false) + the hard-off pin. Conditional dynamic-import wiring in `admin/backend/src/index.js` (after `/api/notifications`): when enabled it imports `./mock2/index.js`, runs `initMock2Db()` + `sweepMock2OnBoot()` + `seedFrameworkV1(null)` + `reconcileMock2Domains()` + `reconcileMock2Firewall()` + `reconcileMock2Egress()` + `sweepIdleStops()` + `sweepMock2Locks()` (+ a 60s lock-sweep timer), and mounts `app.use('/api/mock2', authenticateToken, createMock2Router())`. Add M7 routes to `createMock2Router()` in `routes.js`. An `/api` JSON-404 guard sits before the SPA catch-all — keep it. `mock2/db.js` opens `data/db/mock2.db` (WAL, busy_timeout, 0700/0600), applies block-500 migrations via the main DB's `runMigration()` guard, foreign keys OFF, secrets encrypted at rest with `TOTP_ENCRYPTION_KEY`.

**Migrations 500–506 have run; your M7 tables ALREADY EXIST.** Block 502 defined `mock2_chats` (one per project, UNIQUE project_id) and `mock2_chat_messages` (`kind IN ('user','assistant','rule_question','rule_answer','system','gate_report')`, `acting_as_admin`, `question_id`, `cycle_id`). So M7 is mostly chat loop + mockup generation + inventory extraction + routes, NOT schema. `state/inventory.json` and the mockups live in the **repo** (03-data-model.md), not new tables. **If you need a new column, append a new migration 507+ — never edit 500–506** (506 is M6's `mock2_cycles.instruction`). Migration 507 is unused and free if you need it.

**The M6 runner + lock + pinned framework + gate-copy (M6).** This is the machinery M7 iterates the Concept stage on top of:
- `mock2/runner.js` — the provider-agnostic agentic loop (`startCycle` → `runCycle`), driven by a model slot, with a fixed tool set (exec-in-container, read/write file, run_gates, finish). `mock2/model-client.js` `callModelTurn({connector, apiKey, model, system, tools, transcript})` is the **provider-agnostic single-turn call** (anthropic + openai-compatible for openai/openai_compatible/ollama + gemini) — M7's concept-stage loop calls the SAME `callModelTurn` with the `concept_chat`/`mockup` slots and a DIFFERENT, restricted tool policy (see below). `mock2/runner-logic.js` (pure) holds `RUNNER_TOOLS`, the system-prompt assembly from pinned framework content, and the loop decisions — M7 adds a concept-stage tool set + prompt the same way (pure, stub-first).
- `mock2/locks.js` + `mock2/lock-logic.js` — the checkout lock (ADR-004): one per project, `acquireLock`/`releaseLock`/`touchLock`, `mayAcquire`/`shouldAutoRelease` (pure), the idle sweep. **A human chat session that mutates the container/repo must take the lock** (M6 only wired it for cycles — M7 is where the *human* holder is first created: acquiring on a mockup write, releasing on idle/approval). Viewers never acquire.
- `mock2/cycles.js` + `mock2/cycle-logic.js` — the cycle state machine. Concept-stage work is a cycle with `stage='concept'` (migration 502 CHECK allows it). The design-approval gesture is a `concept` cycle's terminal success.
- `mock2/framework.js` — `getCurrentFrameworkVersion()` returns the row a cycle pins; the pinned version carries `design_system_md` (the LOCKED mockup design system) and `skills_json` (the four skills). **The concept stage is CONSTRAINED to the pinned framework's design system** (brief §4 / ADR-003): the mockup model's system prompt injects `design_system_md`, exactly as the runner injects `constitution_md`. Copy THIS version's content (never "latest").
- `mock2/connectors.js` — `getSlot('concept_chat')` / `getSlot('mockup')` return the pinned connector+model for the two Concept-stage slots. `decryptConnectorKey(row)` gives the plaintext key **orchestrator-side only** — it never enters a container. `slotAssignmentError(capabilities, slot)` enforces capability (`concept_chat`/`mockup` require `chat`). Default cloud model ids are NOT hardcoded — load the **claude-api skill** and default to the latest Claude models (Opus 4.8 / Sonnet 5 / Haiku 4.5), never a stale id.
- `mock2/change-records.js` + `mock2/change-logic.js` — append-only, hash-chained change records (`hash = sha256(prev_hash + canonical_json(payload))`), mirrored to `state/changes/<seq>.json`. `insertChangeRecord(...)`, `verifyProjectChain(...)`. **The design-approval gesture writes a change record** (inventory extracted, mockup discarded). The chain must keep verifying.
- `mock2/quotas.js` + `mock2/quota-logic.js` — `canStartCycle(...)` + the ledger. A concept cycle spends model tokens (chat + mockup generation) → `insertLedgerEntry(...)` per model call, `canStartCycle` at start, mid-cycle buffer stop.
- `mock2/git-connectors.js` — `pushProjectRemote(project)` pushes the bare repo to a `push_on_checkpoint` remote after a checkpoint (credentials never enter a container — ADR-006). The mockup/inventory commits are checkpoints too.

**M6 → the container (already wired).** Provisioning bakes `HTTP(S)_PROXY`/`NO_PROXY` and a dev server on the declared `web` port; the container is M4-fenced (squid-only egress). The mockup is an HTML artifact **committed under `state/mockups/` and served through the project's preview URL path** (a new tab, per the brief) — you write it into the container working tree over the ADR-011 mount and checkpoint it, exactly as M6 writes code. `template.buildContainerSetupScript` + the placeholder dev server are what serve it; you may need the dev server to serve `state/mockups/<id>.html` at a preview path.

**Module conventions to mirror (R7 naming — the AI build component is the RUNNER, `mock2/runner.js`, NOT `agent`):** pure decision layer in a `*-logic.js` (unit-tested stub-first — import ONLY the pure module, never `db.js`/native), thin native data-access in a sibling module, routes in `routes.js` with Zod + `{ error }` shape + `requireAdmin`/`requireSudo`/`requireMock2Role` + `logAudit`. Chat polling mirrors the rest of the app (whole-message updates; SSE only if polling proves inadequate). Admin actions inside someone else's project stamp `acting_as_admin` on chat messages + change records (ADR-007) — `req.mock2Access.actingAsAdmin` is already resolved by `requireMock2Role`.

## Read these first, in order (all in docs/mock2/)
1. `04-phased-plan.md` — your phase is **M7**. Read its verify checklist.
2. `02-adrs.md` — **ADR-002** (audit routing split — M8 formalizes it, but M7's design approval is sign-off #1 and the concept stage structurally cannot write backend code or rules), **ADR-003** (framework pinning: the concept cycle snapshots `current_framework_version_id`; the mockup is constrained to that version's `design_system_md`), **ADR-004** (the lock — M7 first creates a *human* holder), **ADR-006** (the repo is the recovery path; inventory + mockups live there).
3. `design/07-chat-mockup-design-reference.md` + `design/chat-mockup-reference.html` — the operator's design reference the placeholder `design_system_md` is grounded in. The mockup UI + the design system the model is constrained to come from here (provisional until real v1 content arrives — R8).
4. `03-data-model.md` — `mock2_chats` / `mock2_chat_messages` (kinds, `acting_as_admin`, `question_id`), and the in-repo half: `state/inventory.json` (the concept-stage exit artifact), `state/mockups/` (HTML artifacts). Derived status: `awaiting user` is DERIVED from open questions, never set by hand.
5. `05-risks-and-open-questions.md` — **R8** (framework-content status — below), **R5** (cost — concept cycles spend too), **R7** (naming), **R3** (host ops through `mock2/host.js`).

## Scope of this session — Phase M7 only
- **Chat UI in project detail** (polling like the rest of the app; whole-message updates; SSE only if polling proves inadequate). `mock2_chat_messages`, `acting_as_admin` stamped. Chat + approval must work at 360px (MOBILE_FIRST gate).
- **The Concept-stage loop on the `concept_chat`/`mockup` slots**, constrained to the pinned framework's `design_system_md`. Reuse `callModelTurn` (model-client.js) with a Concept-stage system prompt + a **restricted tool policy**: the concept stage can write the mockup HTML under `state/mockups/` and nothing else — it structurally CANNOT write backend code or rules (enforce in the orchestrator's tool dispatch, not the prompt). The mockup is committed and served through the project's preview URL path (new tab, per the brief).
- **The stage's *only* exit: the design-approval gesture** writes `state/inventory.json` (every screen, field-with-type, action, and state extracted from the approved mockup), commits, writes a hash-chained change record, and unlocks Build. The mockup code itself is then discarded (the inventory — not the mockup — is the UI spec). Sign-off #1.
- **Stage/progress indicator** persistent in project detail (Concept → Define → Build → Run).
- Tests stub-first so the suite gains no NEW failures — the 4 pre-existing native-module failures (`cves`, `incus`, `webauthn`, `vpn-mtu`) are in `docs/known-issues.md`; leave them.

## Do NOT implement (later phases)
Audit / rule questions / admin queue UI / drift (M8); classifier / adaptive summary / idle-stop enforcement (M9); Stage 2 Define interview + rules confirmation (M8's audit plumbing precedes it). Do NOT re-touch the M4 fence internals, the M5 connector/quota/framework CRUD, or the M6 runner/lock/change-record internals beyond calling their read/data APIs. Absence (ADR-001): the concept loop is backend code reached only through the gated router — on a disabled host, no route, no state file, no runner/loop code path is imported.

## Hard constraints
Naming (R7 — runner, never agent). Absence (ADR-001). Migrations append-only (507+; 502 already has your chat tables). Docker/nsenter (R3 — container exec + host ops via `mock2/host.js` / `lib/host-exec.js`). Zod, `{ error }` shape, `requireAdmin`/`requireSudo`/`requireMock2Role`, `logAudit`, `acting_as_admin` stamped. The concept stage cannot write backend code or rules (orchestrator-enforced tool policy). Change-record hash chain must keep verifying. The lock guards the container (a human chat write takes it). Tests stub-first. Frontend passes `MOBILE_FIRST.md`.

## What "done" looks like (the M7 verify checklist in 04-phased-plan.md)
Non-technical-path walkthrough: describe an app in chat → a mockup renders at the preview URL (new tab) → iterate conversationally → approve the design → `state/inventory.json` appears in the repo with a commit + a hash-chained change record → the Build button appears only after approval. Chat and approval work at 360px (MOBILE_FIRST gate). Plus `cd admin/backend && node --test 'src/__tests__/*.test.js'` (no NEW failures) and `cd admin/frontend && npm run build`. A `MOCK2_ENABLED=false` host stays byte-for-byte unchanged.

Commit as `mock2-M7: <description>`. (M0–M6 merged to main; branch M7 off main, open its own PR.)

Before you finish: write `docs/mock2/NEXT-SESSION-PROMPT-mock2-phase-09.md` for Phase M8 (audit, rule questions, admin queue — ADR-002) in this same format: what M0–M7 shipped (especially the concept loop, the inventory-extraction/design-approval gesture, the chat plumbing, and the `audit`/`classifier` slots M8 calls), plan-bundle pointers, and M8 scope. Restate the R8 framework-content status.

## ⚠ External prerequisite carried forward (R8) — still unmet
Framework seed v1 is a **clearly-labelled placeholder** vendored under `admin/backend/src/mock2/framework-seed/` (constitution/skills/gates are stubs; the design system in `design-system.md` is grounded in the operator's `docs/mock2/design/07-chat-mockup-design-reference.md` but marked provisional). The operator's REAL framework content — constitution prose, the four skills, gate scripts, the **locked design system**, the project template — was NOT supplied in M5 and is still owed. **M7's design-system-constrained mockups run against the PLACEHOLDER design system until it arrives**, so the mockup house style is provisional; the plumbing (constrain-to-pinned-design-system, extract-inventory, discard-mockup) is what M7 delivers and is correct regardless. M6 already runs its pinned gate battery against a placeholder green no-op gate for the same reason. Flag in your summary that real v1 content is still owed. Once supplied, publish it through the admin editor as a new version (content rows are immutable — never edit the seeded v1 row in place).
