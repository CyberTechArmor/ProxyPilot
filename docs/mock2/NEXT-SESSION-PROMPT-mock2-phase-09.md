You are continuing the build of the Mock2 module inside ProxyPilot. A planning session read the whole codebase and produced the plan you are executing — do not re-derive the architecture, and do not trust your instincts over the plan's citations; they were verified against the code. Phases M0 (skeleton + absence), M1 (parent domains + per-slug TLS), M2 (project registry + container + bare repo + live URL), M3 (archive + rehydrate + idle-stop groundwork), M4 (network isolation), M5 (connectors + quotas + framework registry), M6 (cycle runner + checkout lock) and M7 (Stage 1 — Concept: chat, mockup, design approval) are built and merged to main. Your phase is **M8, and only M8**.

M8 is **Audit, rule questions, admin queue** — the two-way routing that gates Build (ADR-002). When a Builder presses Build, an **audit** step compares the approved inventory + any existing `state/rules.md` + the pinned framework and produces a list of questions, split by route: **editor questions** (domain rules) render as tappable choices in the chat and their answers append to `state/rules.md` (the rules-confirmation sign-off — **sign-off #2**); **framework deviations** materialize as items in the **admin queue**. No questions → Build starts immediately. It is the first phase that consumes the `audit` model slot M5 pinned, and the first that surfaces the `mock2_queue_items` table as a real admin-facing page.

## What M0–M7 already shipped (do NOT re-implement)

**The gate + module + DB (M0).** `admin/backend/src/mock2/gating.js` — `resolveMock2Gate(...)` (pure), `MOCK2_ENABLED` (default false) + the hard-off pin. Conditional dynamic-import wiring in `admin/backend/src/index.js` (after `/api/notifications`): when enabled it imports `./mock2/index.js`, runs `initMock2Db()` + `sweepMock2OnBoot()` + `seedFrameworkV1(null)` + `reconcileMock2Domains()` + `reconcileMock2Firewall()` + `reconcileMock2Egress()` + `sweepIdleStops()` + `sweepMock2Locks()` (+ a 60s lock-sweep timer), and mounts `app.use('/api/mock2', authenticateToken, createMock2Router())`. Add M8 routes to `createMock2Router()` in `routes.js`. An `/api` JSON-404 guard sits before the SPA catch-all — keep it. `mock2/db.js` opens `data/db/mock2.db` (WAL, busy_timeout, 0700/0600), applies block-500 migrations via the main DB's `runMigration()` guard, foreign keys OFF, secrets encrypted at rest with `TOTP_ENCRYPTION_KEY`.

**Migrations 500–507 have run; your M8 tables ALREADY EXIST.** Block 502 defined `mock2_audit_questions` (`route IN ('editor','admin')`, `kind IN ('domain_question','framework_deviation','rule_contradiction','rule_gap')`, `choices_json`, `status`, `answer`, `rules_md_anchor`, `cycle_id`) and `mock2_queue_items` (`kind IN ('framework_deviation','drift','retries_exhausted','flag','orphaned','port_drift','quota_exhausted','provisioning_failed','renewal_failed')`, `ref_table`/`ref_id`, `dedupe_key UNIQUE`, `status`). `mock2_chat_messages` already allows the `rule_question`/`rule_answer` kinds and carries `question_id`. So M8 is mostly the audit step + question plumbing + the admin queue page + drift detection + routes, NOT schema. `state/rules.md` lives in the **repo** (03-data-model.md), seeded empty by the M2 template. **If you need a new column, append a new migration 508+ — never edit 500–507** (507 is M7's `mock2_projects.design_approved_at` / `design_inventory_seq` / `current_mockup_id`). Migration 508 is unused and free if you need it.

**M7's concept loop + inventory + chat (the machinery M8 builds Build's gate on top of):**
- `mock2/concept.js` — the Stage-1 orchestration (`startConceptTurn` → `runConceptTurn`, `startDesignApproval` → `runDesignApproval`), 202+poll like the runner. It drives the `concept_chat` + `mockup` slots via the SAME `callModelTurn` (model-client.js) with a **restricted tool policy** — the concept model's ONLY tool is `generate_mockup`; the orchestrator writes the mockup model's HTML to a FIXED `state/mockups/` path, so the stage structurally cannot write backend code or rules (enforced in dispatch, not the prompt). `mock2/concept-logic.js` (pure, stub-first) holds `CONCEPT_CHAT_TOOLS`, the design-system-injected prompts (`buildConceptChatSystemPrompt`/`buildMockupSystemPrompt` inject the pinned `design_system_md`), the mockup-HTML extraction, the **design-inventory parse** (`parseInventory` → `{screens[], entities[], notes}`), the chat→transcript mapping (`buildConceptTranscript`), the persistent stage indicator (`conceptStageInfo` → Concept → Define → Build → Run), and the cost envelope. **M8 mirrors this shape**: add an `audit` tool/prompt policy the same way (pure module + orchestration + routes).
- **The inventory-extraction / design-approval gesture (sign-off #1).** `runDesignApproval` reads the approved mockup, calls the model to extract a structured inventory, writes `state/inventory.json` (the UI spec — every screen, field-with-type, action, state), **discards the mockup code** (`state/mockups/*.html` removed), checkpoints, writes a **hash-chained change record** (`insertChangeRecord`, `changeRecordMirror` → `state/changes/<seq>.json`), stamps `mock2_projects.design_approved_at` + `design_inventory_seq`, and **releases the human lock**. `state/inventory.json` is the input M8's audit reads (it is the contract, not the mockup pixels). The chain must keep verifying.
- **The chat plumbing.** `mock2/chats.js` — `getOrCreateChat`, `listMessages`, `insertMessage({ kind, body, questionId, cycleId, actingAsAdmin })`. Chat is one row per project (UNIQUE `project_id`); messages are append-only with a `kind`. M7 uses `user`/`assistant`/`system`; **M8 adds `rule_question` (body carries the choices JSON) and `rule_answer` (links back via `question_id`)** — the plumbing already accepts them. `acting_as_admin` is stamped on every human/admin write (ADR-007), resolved by `req.mock2Access.actingAsAdmin`. Chat polls via `GET /api/mock2/projects/:id/chat` (whole-message updates); the frontend surface is `admin/frontend/src/components/mock2/ConceptStage.jsx` (the chat panel + stage indicator + preview + approve).
- **The Build unlock.** `mock2_projects.design_approved_at` (migration 507) drives `conceptStageInfo(project).build_unlocked`; `publicProjectShape` exposes `stage`/`design_approved_at`/`current_mockup_id`/`preview_url`. The frontend renders the M6 `CycleCard` (Build) only when `project.stage.design_approved`. **M8's audit fires on the Build press** — before the M6 cycle actually starts, run the audit and, if it raises editor questions, block in `awaiting_user` until they are answered; framework deviations block in `awaiting_admin` (both derived from open questions/queue items, never set by hand — 03-data-model.md).

**The audit + classifier slots M8 (and M9) call.** `mock2/connectors.js` `getSlot('audit')` returns the pinned connector+model for the audit step; `getSlot('classifier')` is M9's (rule-change classifier). `decryptConnectorKey(row)` gives the plaintext key **orchestrator-side only** — never enters a container. `slotAssignmentError(capabilities, slot)` enforces capability (`audit` requires `chat` — see `SLOT_REQUIRED_CAPABILITY` in `connector-logic.js`; `classify`/`summarize` map to their slots). Default cloud model ids are NOT hardcoded — load the **claude-api skill** and default to the latest Claude models (Opus 4.8 / Sonnet 5 / Haiku 4.5), never a stale id.

**The M6 runner + lock + pinned framework (unchanged; M8 gates the runner, doesn't touch its internals).**
- `mock2/runner.js` (`startCycle` → `runCycle`) + `mock2/runner-logic.js` (pure) — the build cycle. M8's audit runs **before** `startCycle` for a Build; when it clears (no open questions), the existing Build cycle starts as today. `mock2/model-client.js` `callModelTurn(...)` is the provider-agnostic single-turn call (anthropic + openai-compatible + gemini) — the audit step calls the SAME `callModelTurn` with the `audit` slot and its own tool/prompt policy.
- `mock2/locks.js` + `mock2/lock-logic.js` — the checkout lock (ADR-004). M7 created the first human holder (chat writes); M8's answer-a-question writes to `state/rules.md`, so it too takes the lock on the write.
- `mock2/framework.js` — `getCurrentFrameworkVersion()` (the pin); the pinned version carries `constitution_md` + the four `skills_json` skills + `gates_json` + `design_system_md`. The audit compares against the **pinned** framework (ADR-003). **Drift** = compare `mock2_projects.last_built_framework_version_id` vs current on each audit → a `drift` queue item + an "update available" banner; remediation is an explicit consented `Mock2 X → Y` cycle only.
- `mock2/change-records.js` + `mock2/change-logic.js` — append-only, hash-chained. A rules-confirmation answer that appends to `rules.md` is a checkpoint too; the chain must keep verifying.
- `mock2/queue.js` — `raiseQueueItem({ kind, project_id, dedupe_key, ref_table, ref_id, detail })` / `resolveQueueItem(dedupe_key, ...)`. M0–M7 already raise `provisioning_failed`/`renewal_failed`/`flag`/`retries_exhausted`/`quota_exhausted`/`port_drift` items with the notifications-style dedupe UPSERT; **M8 adds `framework_deviation` and `drift`, and builds the admin queue PAGE that reads all of them** (until now they surface only via the notifications bell).

**Module conventions to mirror (R7 naming — the AI build component is the RUNNER, `mock2/runner.js`; the Stage-1 component is the CONCEPT loop, `mock2/concept.js`; NOTHING is named `agent`):** pure decision layer in a `*-logic.js` (unit-tested stub-first — import ONLY the pure module, never `db.js`/native), thin native data-access in a sibling module, routes in `routes.js` with Zod + `{ error }` shape + `requireAdmin`/`requireSudo`/`requireMock2Role` + `logAudit`. Chat polling mirrors the rest of the app (whole-message updates; SSE only if polling proves inadequate). Admin actions inside someone else's project stamp `acting_as_admin` on chat messages + change records + queue items (ADR-002/007) — `req.mock2Access.actingAsAdmin` is already resolved by `requireMock2Role`.

## Read these first, in order (all in docs/mock2/)
1. `04-phased-plan.md` — your phase is **M8**. Read its verify checklist.
2. `02-adrs.md` — **ADR-002** (the audit routing split M8 FORMALIZES: domain questions → editors → `rules.md`; framework deviations → admin queue; a project's answer never writes the framework; the three-outcome classifier reuses the same question plumbing — but the classifier is M9), **ADR-003** (framework pinning + drift = pinned vs current, remediation is a consented `Mock2 X → Y` cycle only), **ADR-004** (the lock — an answer that writes `rules.md` takes it), **ADR-007** (`acting_as_admin`; editors can be non-technical, so question text is model-generated plain language with fixed choices + a free-text escape hatch).
3. `03-data-model.md` — `mock2_audit_questions` (route/kind/choices/status/answer/`rules_md_anchor`), `mock2_queue_items` (the admin's real object — kinds, `dedupe_key`, status), `mock2_chat_messages` (`rule_question`/`rule_answer` kinds, `question_id`), and the in-repo half: `state/rules.md` (rule answers only), `state/inventory.json` (the M7 exit artifact the audit reads). **Derived status:** `awaiting user` ← open editor question; `awaiting admin` ← open admin question/queue item; `drift` ← open drift queue item — all DERIVED, never set by hand. Implement in the ONE `deriveProjectStatus` (project-logic.js), never a fork.
4. `05-risks-and-open-questions.md` — **R8** (framework-content status — below), **R5** (cost — the audit step spends too), **R7** (naming), **R3** (host ops through `mock2/host.js`).

## Scope of this session — Phase M8 only
- **Audit step on Build press** (`audit` slot): mockup (if still present) + `state/inventory.json` + existing `state/rules.md` + the pinned framework → a question list, split by `route`. **No questions → Build starts immediately** (hand off to the existing `startCycle`). Pure decision layer for the routing (which kind → which route) unit-tested stub-first.
- **Editor questions**: tappable choices rendered in the chat (`rule_question` message, body carries choices JSON); an answer appends to `state/rules.md` (commit + change record + `rules_md_anchor`) and posts a `rule_answer`; the Build cycle stays blocked in `awaiting_user` until every editor question is answered. **Free text is always allowed** (the escape hatch).
- **Admin queue page**: the queue-of-items view (filter by project, kind, status), backed by `mock2_queue_items`. Framework deviations, drift, retries-exhausted, port-drift, flags all land here; the bell stays an attention-getter only. This is the first real admin-facing surface for the table M0–M7 have been writing to.
- **Drift detection each audit**: compare `mock2_projects.last_built_framework_version_id` vs current → a `drift` queue item + an "update available" project banner; a remediation cycle is explicit-consent only, labelled `Mock2 X → Y`, full gate battery. **Nothing auto-remediates.**
- **"Flag an Admin" button** already exists for editors (M2) — extend it to viewers per the plan, `!` overlay + queue item (the plumbing is in `routes.js` `/flag` + `raiseQueueItem`).
- Frontend passes `admin/frontend/MOBILE_FIRST.md` (tappable question choices ≥44px; the queue page and the in-chat questions complete on a 360px screen). Tests stub-first so the suite gains no NEW failures — the 4 pre-existing native-module failures (`cves`, `incus`, `webauthn`, `vpn-mtu`) are in `docs/known-issues.md`; leave them.

## Do NOT implement (later phases)
The rule-change **classifier** (M9 — three outcomes, biased to flag, reconfirm flow), the **adaptive summary** (`mock2_summaries`, M9), **idle-stop enforcement** + derived-status polish + quota UX (M9), and the hardening pass (M10). Do NOT re-touch the M4 fence internals, the M5 connector/quota/framework CRUD, the M6 runner/lock/change-record internals, or the M7 concept loop/inventory/chat internals beyond calling their read/data APIs. **Absence (ADR-001):** the audit + queue are backend code reached only through the gated router — on a disabled host, no route, no state file, no audit/queue code path is imported. A `MOCK2_ENABLED=false` host must stay byte-for-byte unchanged.

## Hard constraints
Naming (R7 — runner/concept, never `agent`). Absence (ADR-001). Migrations append-only (508+; 502 already has `mock2_audit_questions` + `mock2_queue_items` + the `rule_question`/`rule_answer` chat kinds). Docker/nsenter (R3 — container exec + host ops via `mock2/host.js` / `lib/host-exec.js`). Zod, `{ error }` shape, `requireAdmin`/`requireSudo`/`requireMock2Role`, `logAudit`, `acting_as_admin` stamped. The audit output routes by kind, never by convenience (ADR-002); a project's answer never writes the framework. Change-record hash chain must keep verifying. `awaiting user` / `awaiting admin` / `drift` are DERIVED. The lock guards the container (an answer that writes `rules.md` takes it). Tests stub-first. Frontend passes `MOBILE_FIRST.md`.

## What "done" looks like (the M8 verify checklist in 04-phased-plan.md)
Seed a mockup/inventory that implies an ambiguous domain rule → press Build → the audit asks the editor a plain-language question with tappable choices in chat → the answer lands in `state/rules.md` (commit + change record) and Build proceeds. Seed a request implying a forbidden stack choice (e.g. MySQL) → a `framework_deviation` lands in the admin queue and Build is blocked (`awaiting_admin`) until resolved. Bump the framework version → the next audit raises `drift`, nothing auto-remediates, an "update available" banner appears. The flag button (editors + viewers) raises `!` + a queue item. Plus `cd admin/backend && node --test 'src/__tests__/*.test.js'` (no NEW failures beyond the 4 known) and `cd admin/frontend && npm run build`. A `MOCK2_ENABLED=false` host stays byte-for-byte unchanged.

Commit as `mock2-M8: <description>`. (M0–M7 merged to main; branch M8 off main, open its own PR.)

Before you finish: write `docs/mock2/NEXT-SESSION-PROMPT-mock2-phase-10.md` for Phase M9 (iteration: classifier, adaptive summary, idle-stop enforcement, derived-status polish, quota UX — ADR-002's classifier) in this same format: what M0–M8 shipped (especially the audit step, the question routing + `rules.md` writes, the admin queue page, and drift detection), plan-bundle pointers, and M9 scope. Restate the R8 framework-content status.

## R8 status — framework CONTENT is real; only the runtime scaffold remains
Framework seed v1 is real content authored from the operator's *The Mock2 Framework* spec (v1.1), vendored under `admin/backend/src/mock2/framework-seed/`: a real constitution (stack/scaffold/auth/security/the four stages), the four stage skills (concept/define/build/review) as prompt templates (`skills.json` is a JSON array — `parseFrameworkSkills` reads name + description), a real deterministic Tier-1 gate battery (typecheck, constitution-lint, rule-coverage, security-scan, test), and the **locked design system** (`design-system.md`, from the operator's `docs/mock2/design/07-chat-mockup-design-reference.md`). M8's audit therefore compares against a REAL constitution + a real `rules.md` shape; the design system is what M7's mockups were constrained to, and it is genuine. The gate scripts are self-adapting (skip green when the TS toolchain is absent, enforce for real when present). **The one piece still owed is the runtime scaffold** — the actual TypeScript/Express/Drizzle project code the container is seeded from is still ProxyPilot's placeholder (`mock2/template.js`; M7 extended its dev server to also serve the Stage-1 mockup at `/_preview` from `state/mockups/`, but the app itself is still the M2 placeholder). `project_template_ref` names the intended scaffold (`builtin:mock2-ts-express-drizzle-v1`) but the seed implementation is a separate infra deliverable, and the mockup preview + generated apps run against the placeholder until it lands. Flag this in your summary. Content revisions publish a new framework version through the admin editor (append-only — never edit the seeded v1 row in place).
