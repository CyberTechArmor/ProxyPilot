You are continuing the build of the Mock2 module inside ProxyPilot. A planning session read the whole codebase and produced the plan you are executing — do not re-derive the architecture, and do not trust your instincts over the plan's citations; they were verified against the code. Phases M0 (skeleton + absence), M1 (parent domains + per-slug TLS), M2 (project registry + container + bare repo + live URL), M3 (archive + rehydrate + idle-stop groundwork), M4 (network isolation), M5 (connectors + quotas + framework registry), M6 (cycle runner + checkout lock), M7 (Stage 1 — Concept: chat, mockup, design approval) and M8 (Audit, rule questions, admin queue) are built and merged to main. Your phase is **M9, and only M9**.

M9 is **Iteration: classifier, adaptive summary, lifecycle polish** — the steady-state loop the user actually lives in (ADR-002's classifier; the brief's adaptive-summary / container-lifecycle / status sections). Every iteration message runs the **rule-change classifier** (three outcomes, biased to flag); the **adaptive summary** regenerates on qualifying cycles; container **idle-stop** enforces for real; the **derived-status** function is finalized across every state; and the **quota UX** (pre-cycle refusal, mid-cycle buffer stop, `quota exhausted` status) is completed. It is the first phase that consumes the `classifier` and `summary` model slots M5 pinned, and the first that writes `mock2_summaries`.

## What M0–M8 already shipped (do NOT re-implement)

**The gate + module + DB (M0).** `admin/backend/src/mock2/gating.js` — `resolveMock2Gate(...)` (pure), `MOCK2_ENABLED` (default false) + the hard-off pin. Conditional dynamic-import wiring in `admin/backend/src/index.js` (after `/api/notifications`): when enabled it imports `./mock2/index.js`, runs `initMock2Db()` + `sweepMock2OnBoot()` + `seedFrameworkV1(null)` + `reconcileMock2Domains()` + `reconcileMock2Firewall()` + `reconcileMock2Egress()` + `sweepIdleStops()` + `sweepMock2Locks()` (+ a 60s lock-sweep timer), and mounts `app.use('/api/mock2', authenticateToken, createMock2Router())`. Add M9 routes to `createMock2Router()` in `routes.js`. An `/api` JSON-404 guard sits before the SPA catch-all — keep it. `mock2/db.js` opens `data/db/mock2.db` (WAL, busy_timeout, 0700/0600), applies block-500 migrations via the main DB's `runMigration()` guard, foreign keys OFF, secrets encrypted at rest with `TOTP_ENCRYPTION_KEY`.

**Migrations 500–507 have run; the `mock2_summaries` table ALREADY EXISTS.** Block 503 defined `mock2_summaries` (`project_id`, `version`, `body_md`, `derived_from_change_seq INTEGER NOT NULL`, `UNIQUE (project_id, version)`) — the versioned, diffable adaptive summary M9 writes. `mock2_cycles.classifier_outcome` (`'implements'|'contradicts'|'unaddressed'`, migration 502) is the column the classifier stamps. `mock2_settings` (block 500 singleton k/v) already holds `idle_stop_days` (via `settings.js` `getIdleStopDays`/`IDLE_STOP_DAYS_KEY`) and `lock_idle_minutes`. So M9 is mostly the classifier + summary + idle enforcement + status polish + quota UX, NOT schema. **If you need a new column, append a new migration 508+ — never edit 500–507.** Migration 508 is unused and free if you need it.

**M8's audit step + question routing + admin queue (the gate M9's classifier reuses).**
- `mock2/audit.js` — the Build-press gate. `startBuild(...)` guards (active + `design_approved_at` + `audit` slot ready + `build_runner` ready), quota-checks, runs **drift detection** (see below), then fires a background `runAudit`. `runAudit` reads `state/inventory.json` + `state/rules.md` from the container, calls the `audit` slot via the SAME `callModelTurn` (tools `[]` — a JSON question list out, like the M7 inventory extraction), parses + **routes by kind** (`audit-logic.routeForKind`), then: **no questions → `proceedToBuild` (stamps `last_built_framework_version_id`, resolves the drift item, hands off to the runner's `startCycle`)**; **editor questions → `insertQuestion(route:'editor')` + a `rule_question` chat message (body = `buildRuleQuestionBody({question, choices})`)**; **admin deviations → `insertQuestion(route:'admin')` + a `framework_deviation` queue item** (`dedupe_key mock2-deviation:<qid>`, `ref_table='mock2_audit_questions'`). It creates ONE **audit cycle** per Build (`stage:'define'`, status `awaiting_user`/`awaiting_admin` when blocked) that remembers the Build `instruction` to resume with. `answerAuditQuestion(...)` takes the lock, appends the answer to `state/rules.md` (`audit-logic.appendRule` → commit + **hash-chained change record** + `rules_md_anchor`), posts a `rule_answer`, releases the lock, and `maybeResumeBuild` (resumes the deferred `startCycle` once `auditGateCleared` — both counts DERIVED). `resolveFrameworkDeviation(...)` dismisses the linked admin question and resumes.
- `mock2/audit-logic.js` (pure, stub-first, `mock2-audit.test.js`) — `AUDIT_KINDS`, `routeForKind` (framework_deviation → admin; else editor), `buildAuditSystemPrompt` (injects the pinned `constitution_md`, exactly as the runner injects it), `buildAuditTask`, `parseAuditQuestions` (routes each kind, drops invalid), `splitQuestionsByRoute`, `buildRuleQuestionBody`/`parseRuleQuestionBody`, `appendRule`/`ruleAnchor` (the sign-off #2 rules.md write), `auditGateCleared`/`blockedBuildStatus`, `isFrameworkDrifted`/`driftLabel`, `publicQuestionShape`/`publicQueueItemShape`, `estimateAuditTokens`. **M9's classifier mirrors this shape**: a pure `classifier-logic.js` (the three-outcome decision, biased to flag) + orchestration that REUSES the same `insertQuestion`/`rule_question` plumbing (ADR-002: outcome 2 and 3 create **editor** questions).
- `mock2/questions.js` — audit-question data access: `insertQuestion`, `getQuestion`, `answerQuestion`, `dismissQuestion`, `listOpenEditorQuestions`, `countOpenEditorQuestions`, `countOpenAdminQuestions`. The classifier writes through these.
- **The admin queue PAGE.** `mock2/queue.js` gained `listQueueItems({projectId,kind,status})`, `getQueueItem`, `queueCounts`, `setQueueItemStatus`, `countAwaitingAdminItems`, `hasOpenDrift`. Routes: `GET /api/mock2/queue` (filters + counts, admin), `POST /api/mock2/queue/:id/status` (resolving a `framework_deviation` calls `resolveFrameworkDeviation` → unblocks the build). Frontend: `admin/frontend/src/pages/AdminQueue.jsx` at `/projects/queue`, linked from `Projects.jsx`. The notifications bell stays an attention-getter only.
- **Drift detection (ADR-003).** `audit.detectDrift(project, framework)` compares `mock2_projects.last_built_framework_version_id` vs the current framework id (`audit-logic.isFrameworkDrifted`) → raises a `drift` queue item (`dedupe_key mock2-drift:<pid>`) + surfaces `framework_update_available` on the project shape → the **"update available" banner** in `CycleCard`. Nothing auto-remediates; the banner offers an explicit-consent **update cycle** (a normal Build with a `Mock2 vX → vY` instruction). `proceedToBuild` resolves the drift item + stamps `last_built_framework_version_id` when a build actually starts.

**Derived status (M8 extended, M9 finalizes).** `mock2/project-logic.js` `deriveProjectStatus(project, ctx)` is still the ONE implementation. M8 added `openEditorQuestions>0 → 'awaiting_user'`, `openAdminItems>0 → 'awaiting_admin'`, `driftOpen → 'drift'` to `ctx`; `shapeProject` (routes.js) computes those via `countOpenEditorQuestions` + `countOpenAdminQuestions`+`countAwaitingAdminItems` + `hasOpenDrift`, and passes `frameworkUpdateAvailable`/`frameworkCurrentVersion`/`frameworkLastBuiltVersion` through `publicProjectShape`. **M9 must finalize this function across ALL states** — the data-model precedence still owes `building` (cycle running, with `current_gate`), `checked out` (lock holder + remaining time), `quota exhausted` (ledger vs budget), and `stopped`/`idle` from live Incus state. Add those inputs to the SAME `ctx`, never a fork. Frontend chips live in `admin/frontend/src/lib/mock2-status.jsx` (`awaiting_user`/`awaiting_admin`/`drift` already added).

**The M7 concept loop + M6 runner + lock + change-records (unchanged; M9 does not re-touch their internals).**
- `mock2/concept.js` + `concept-logic.js` — Stage 1 (chat, mockup, design approval → `state/inventory.json`, sign-off #1). `mock2/runner.js` (`startCycle` → `runCycle`) + `runner-logic.js` — the Build cycle (M8's audit hands off to `startCycle` when the gate clears). `mock2/model-client.js` `callModelTurn(...)` — provider-agnostic single-turn (anthropic + openai-compatible + gemini); the classifier + summary call the SAME `callModelTurn` with their slots. `mock2/locks.js` + `lock-logic.js` — the checkout lock (ADR-004; `sweepMock2Locks` on a 60s timer). `mock2/change-records.js` + `change-logic.js` — append-only, hash-chained; a rules-confirmation answer is a checkpoint too and the chain must keep verifying.
- **The classifier + summary slots M9 calls.** `mock2/connectors.js` `getSlot('classifier')` / `getSlot('summary')` return the pinned connector+model; `decryptConnectorKey(row)` gives the plaintext key **orchestrator-side only** (never into a container); `slotAssignmentError(capabilities, slot)` enforces capability (`classifier` needs `classify`, `summary` needs `summarize` — see `SLOT_REQUIRED_CAPABILITY` in `connector-logic.js`). Mirror `audit.auditReady()` / `concept.conceptReady()` for a `classifierReady()` / `summaryReady()`. Default cloud model ids are NOT hardcoded — load the `claude-api` skill and default to the latest Claude models (Opus 4.8 / Sonnet 5 / Haiku 4.5), never a stale id.

**Module conventions to mirror (R7 naming — the AI build component is the RUNNER, `mock2/runner.js`; Stage-1 is the CONCEPT loop, `mock2/concept.js`; the Build gate is the AUDIT, `mock2/audit.js`; NOTHING is named agent):** pure decision layer in a `*-logic.js` (unit-tested stub-first — import ONLY the pure module, never `db.js`/native), thin native data-access in a sibling module, routes in `routes.js` with Zod + `{ error }` shape + `requireAdmin`/`requireSudo`/`requireMock2Role` + `logAudit`. Chat polling mirrors the rest of the app (whole-message updates; the frontend `ConceptStage.jsx` polls `GET /chat`, which now returns `audit_job`, `open_question_ids`, `open_editor_questions`). Admin actions inside someone else's project stamp `acting_as_admin` on chat messages + change records + queue items (ADR-002/007) — `req.mock2Access.actingAsAdmin` is already resolved by `requireMock2Role`.

## Read these first, in order (all in `docs/mock2/`)

- **`04-phased-plan.md`** — your phase is **M9**. Read its verify checklist (the classifier fixture suite: implements / contradicts / unaddressed × N paraphrases → outcome 3 → question → rules.md → build; summary regenerates only on qualifying cycles and diffs cleanly; idle project stops after N days and wakes on visit; quota exhaustion mid-cycle checkpoints and reports plainly).
- **`02-adrs.md`** — **ADR-002** (the three-outcome classifier reuses the audit question plumbing — outcome 2 and 3 create **editor** questions; **an admin answer is rejected for domain rules**; bias toward flagging — a false positive costs a tap), **ADR-003** (framework pin; drift), **ADR-004** (the lock — the classifier/summary do not write the container, but a resulting rule question's answer does, and takes it), **ADR-007** (`acting_as_admin`; editors can be non-technical).
- **`03-data-model.md`** — `mock2_summaries` (versioned, `derived_from_change_seq` is the change-record high-water mark, NOT chat), `mock2_cycles.classifier_outcome`, and the **Derived status** section (the single-source-of-truth `deriveProjectStatus` M9 finalizes). `state/rules.md` (rule answers only) + `state/inventory.json` (the M7 exit artifact) live in the repo.
- **`05-risks-and-open-questions.md`** — R8 (framework-content status — below), R5 (cost — the classifier + summary spend too, price them on their slots like the audit does), R7 (naming), R3 (host ops through `mock2/host.js`).

## Scope of this session — Phase M9 only

- **Rule-change classifier** (`classifier` slot) on every iteration message: three outcomes (**implements** an existing rule → build directly; **contradicts** a rule → outcome-2 **reconfirm flow**, editor-only, an admin answer is rejected for domain rules; **unaddressed** by any rule → outcome-3 lazy **rule question** then build). Bias to flag. Stamp `mock2_cycles.classifier_outcome`. REUSE `questions.insertQuestion` + the `rule_question` chat plumbing — do NOT invent a second question path. Pure `classifier-logic.js` (the outcome decision) unit-tested stub-first.
- **Adaptive summary** (`summary` slot): a **deterministic trigger** (a cycle that touched `state/rules.md` / `state/inventory.json` / screens±actions), generated from **change records + `rules.md` only** (never chat), versioned + diffable (`mock2_summaries`, `derived_from_change_seq` = the change high-water mark). Surface it in project detail.
- **Container idle-stop enforcement**: `incus stop` after N days idle (`mock2_settings.idle_stop_days` — the M3 groundwork + `sweepIdleStops` already exist; M9 makes it enforce), restart-on-visit (the `wake` route exists), `stopped` in derived status.
- **Derived-status function finalized** across all states (building / checked out / quota exhausted / stopped / idle added to the SAME `deriveProjectStatus` + `ctx`); tile + detail use it.
- **Quota UX**: pre-cycle refusal message, mid-cycle buffer stop message, `quota exhausted` derived status; wall-clock / concurrency caps for self-hosted slots (`mock2_quotas.budget_wall_clock_min` / `max_concurrent_cycles` already exist).

## Do NOT implement (later phase)

The **M10 hardening pass** (disabled/pinned-host audit, container-escape review of the runner tool set, egress-proxy bypass attempts, the `GET /verify-chain` tool, restore-onto-pinned-host behavior, operator runbook + compliance language, load test). Do NOT re-touch the M4 fence internals, the M5 connector/quota/framework CRUD, the M6 runner/lock/change-record internals, the M7 concept loop/inventory internals, or the M8 audit/queue internals beyond calling their read/data APIs. **Absence (ADR-001)**: the classifier + summary are backend code reached only through the gated router — on a disabled host, no route, no state file, no classifier/summary code path is imported. **A `MOCK2_ENABLED=false` host must stay byte-for-byte unchanged.**

## Hard constraints

Naming (R7 — runner/concept/audit, never agent). Absence (ADR-001). Migrations append-only (508+; `mock2_summaries` + `classifier_outcome` already exist). Docker/nsenter (R3 — container exec + host ops via `mock2/host.js`). Zod, `{ error }` shape, `requireAdmin`/`requireSudo`/`requireMock2Role`, `logAudit`, `acting_as_admin` stamped. The classifier reuses the audit question plumbing and routes by outcome, never by convenience (ADR-002); **an admin answer is rejected for a domain rule** (the reconfirm flow is editor-only). Change-record hash chain must keep verifying. `awaiting user` / `awaiting admin` / `drift` / `building` / `checked out` / `quota exhausted` are all **DERIVED** — one `deriveProjectStatus`, never a fork. The lock guards the container (a rule answer that writes `rules.md` takes it). Tests stub-first. Frontend passes `admin/frontend/MOBILE_FIRST.md`.

## What "done" looks like (the M9 verify checklist in `04-phased-plan.md`)

"schedulers should see other practices' shifts" routed through the classifier fixture suite (implements / contradicts / unaddressed × N paraphrases) → outcome 3 → question → `rules.md` → build; the summary regenerates ONLY on qualifying cycles and diffs cleanly; an idle project stops after N days and wakes on visit; quota exhaustion mid-cycle checkpoints and reports plainly. Plus `cd admin/backend && node --test 'src/__tests__/*.test.js'` (no NEW failures beyond the 4 known — `cves`, `incus`, `webauthn`, `vpn-mtu` in `docs/known-issues.md`) and `cd admin/frontend && npm run build`. A `MOCK2_ENABLED=false` host stays byte-for-byte unchanged.

Commit as `mock2-M9: <description>`. (M0–M8 merged to main; branch M9 off main, open its own PR.)

Before you finish: write `docs/mock2/NEXT-SESSION-PROMPT-mock2-phase-11.md` for **Phase M10 (hardening pass)** in this same format: what M0–M9 shipped, plan-bundle pointers, and M10 scope. Restate the R8 framework-content status.

## R8 status — framework CONTENT is real; only the runtime scaffold remains

Framework seed v1 is real content authored from the operator's *The Mock2 Framework* spec (v1.1), vendored under `admin/backend/src/mock2/framework-seed/`: a real constitution (stack/scaffold/auth/security/the four stages — this is what M8's audit measures a `framework_deviation` against, and what the classifier's rules are confirmed under), the four stage skills (concept/define/build/review) as prompt templates, a real deterministic Tier-1 gate battery (typecheck, constitution-lint, rule-coverage, security-scan, test), and the locked design system (`design-system.md`). The gate scripts are self-adapting (skip green when the TS toolchain is absent, enforce for real when present). **The one piece still owed is the runtime scaffold** — the actual TypeScript/Express/Drizzle project code the container is seeded from is still ProxyPilot's placeholder (`mock2/template.js`; M7 extended its dev server to serve the Stage-1 mockup at `/_preview`, but the app itself is still the M2 placeholder). `project_template_ref` names the intended scaffold (`builtin:mock2-ts-express-drizzle-v1`) but the seed implementation is a separate infra deliverable; the mockup preview + generated apps run against the placeholder until it lands. **Flag this in your summary.** Content revisions publish a new framework version through the admin editor (append-only — never edit the seeded v1 row in place).
