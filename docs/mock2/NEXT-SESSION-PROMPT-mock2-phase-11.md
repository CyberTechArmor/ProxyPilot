You are continuing the build of the Mock2 module inside ProxyPilot. A planning session read the whole codebase and produced the plan you are executing — do not re-derive the architecture, and do not trust your instincts over the plan's citations; they were verified against the code. Phases M0 (skeleton + absence), M1 (parent domains + per-slug TLS), M2 (project registry + container + bare repo + live URL), M3 (archive + rehydrate + idle-stop groundwork), M4 (network isolation), M5 (connectors + quotas + framework registry), M6 (cycle runner + checkout lock), M7 (Stage 1 — Concept: chat, mockup, design approval), M8 (Audit, rule questions, admin queue) and M9 (Iteration: classifier, adaptive summary, lifecycle polish) are built and merged to main. Your phase is **M10, and only M10**.

M10 is the **Hardening pass (pre-adoption gate)** — the compliance story holds under adversarial review. This is the last phase: no new user-facing feature, all verification + threat-modelling + one new read-only endpoint + docs. It proves the four absence mechanisms are real on a disabled/pinned host, that the runner tool set can't escape its container, that the egress fence can't be bypassed from a hostile container, that the change-record chain can be verified on demand, that restoring `mock2.db` onto a pinned host is safe, and it ships the operator runbook + the honest compliance language ADR-001 demands, plus a small load test.

## What M0–M9 already shipped (do NOT re-implement)

**The gate + module + DB (M0).** `admin/backend/src/mock2/gating.js` — `resolveMock2Gate(...)` (pure), `MOCK2_ENABLED` (default false) + the hard-off pin `/etc/proxypilot/mock2.production.pin`. Conditional dynamic-import wiring in `admin/backend/src/index.js` (after `/api/notifications`): when enabled it imports `./mock2/index.js`, runs `initMock2Db()` + `sweepMock2OnBoot()` + `seedFrameworkV1(null)` + `reconcileMock2Domains()` + `reconcileMock2Firewall()` + `reconcileMock2Egress()` + `sweepIdleStops()` (+ an **hourly idle-stop timer**, M9) + `sweepMock2Locks()` (+ a 60s lock-sweep timer), and mounts `app.use('/api/mock2', authenticateToken, createMock2Router())`. An `/api` JSON-404 guard sits before the SPA catch-all — keep it. `mock2/db.js` opens `data/db/mock2.db` (WAL, `busy_timeout=5000`, dir 0700 / file 0600), applies block-500 migrations via the main DB's `runMigration()` guard, foreign keys OFF, secrets encrypted at rest with `TOTP_ENCRYPTION_KEY`. **Migrations 500–508 are the ceiling** (M9 needed none — the tables it used already existed; 508 is still free). Append-only: never edit an applied migration.

**M9 — Iteration: classifier, adaptive summary, lifecycle polish (the steady-state loop; do NOT re-touch its internals beyond calling its read/data APIs).**
- **Rule-change classifier (ADR-002).** `mock2/classifier-logic.js` (pure, stub-first, `mock2-classifier.test.js`) — `CLASSIFIER_OUTCOMES` (`implements`/`contradicts`/`unaddressed`), `normalizeOutcome` (the **bias to flag**: ONLY an explicit `implements` proceeds; everything else — unknown, hedged, missing — collapses to `unaddressed`), `proceedsToBuild`, `classifierQuestionKind` (contradicts→`rule_contradiction`, unaddressed→`rule_gap`; both EDITOR-routed via `audit-logic.routeForKind`), `buildClassifierSystemPrompt` (injects the pinned `constitution_md`), `buildClassifierTask`, `parseClassifierResult` (tolerant; a hard non-JSON failure is NOT a silent build), `fallbackQuestion`, `estimateClassifierTokens`. `mock2/classifier.js` (orchestration) — `classifierReady()` (mirrors `auditReady()`/`conceptReady()`), `startIteration(...)` is the **post-approval `/chat` entry**: guards (active + `design_approved_at` + no held lock + `classifier` slot ready + `build_runner` ready + no open editor question), inserts the user chat message, **quota PRE-CYCLE check** (`refused_quota` cycle + a plain "this change wasn't started — …" system message), creates ONE `stage:'define'` classifier cycle carrying the iteration instruction + `trigger_message_id`, stamps `mock2_cycles.classifier_outcome`, then routes: **implements → `proceedToBuild` (the SAME audit hand-off)**; **contradicts/unaddressed → `questions.insertQuestion(route:'editor')` + a `rule_question` chat message**, cycle `awaiting_user`. The existing `answerAuditQuestion` (M8) appends to `state/rules.md`, takes the lock, and `maybeResumeBuild` resumes the deferred build — REUSED wholesale (no second question/answer path). `audit.js` now **exports** `proceedToBuild` + `maybeResumeBuild` for this reuse.
- **Adaptive summary.** `mock2/summary-logic.js` (pure, `mock2-summary.test.js`) — `isQualifyingRecord` (rules.md touched / gates_run / inventory-in-summary), `summaryTrigger` (regenerate iff a NEW qualifying record's seq is beyond the last summary's `derived_from_change_seq` high-water mark — so non-qualifying churn never re-triggers and the same change never triggers twice), `nextSummaryVersion`, `buildSummarySystemPrompt`/`buildSummaryTask` (from change records + rules.md **only**, never chat), `summaryBodyChanged` (skip an identical regen), `diffSummaries` (clean added/removed line diff), `publicSummaryShape`, `estimateSummaryTokens`. `mock2/summaries.js` — data access (`getLatestSummary`, `listSummaries`, `getSummaryVersion`, `insertSummary`; **M9 is the first writer of `mock2_summaries`**). `mock2/summary.js` — `summaryReady()` + `maybeRegenerateSummary(projectId, {reason, triggerCycleId})` (best-effort, non-fatal, reads rules.md via `host.js`, prices on the `summary` slot + ledger). Hooked from **the runner's green-build success**, **`answerAuditQuestion` after a rule confirmation**. Routes: `GET /api/mock2/projects/:id/summary` (latest + versions + diff + `summary_ready`), `GET /api/mock2/projects/:id/summary/:version`. Frontend: `SummaryCard` in `ProjectDetail.jsx`.
- **Container idle-stop enforcement.** `idle.js` `sweepIdleStops` (M3) is now driven by an **hourly `setInterval`** in `index.js` (plus the boot sweep). `stopProjectContainer` sets `lifecycle='stopped'`; wake is `POST /projects/:id/wake` (`startWake` → `lifecycle='active'`). `mock2_settings.idle_stop_days` (0 disables) via `settings.getIdleStopDays`.
- **Derived status FINALIZED across all states.** `project-logic.deriveProjectStatus(project, ctx)` is still the ONE implementation. M9 added `cycleRunning → building`, `quotaExhausted → quota_exhausted`, `lockHeldByHuman → checked_out` to the SAME `ctx`, with precedence: lifecycle terminals → **building** → **quota_exhausted** → awaiting_user → awaiting_admin → **checked_out** → drift → orphaned → stopped → online/idle. `shapeProject` (routes.js) computes them via `countRunningCycles(id)>0`, `getLock(id)` (human holder), `quotas.isQuotaExhausted(id)` (ledger vs budget). Frontend chips (`building`/`checked_out`/`quota_exhausted`) added to `admin/frontend/src/lib/mock2-status.jsx`.
- **Quota UX.** Pre-cycle refusal message (classifier + concept + audit), the **mid-cycle buffer stop** now posts a plain chat message ("The budget for this period was reached partway through this change. I saved a checkpoint … Raise the budget to continue.") in `runner.js`, and `quota_exhausted` is a derived status. Wall-clock / concurrency caps (`mock2_quotas.budget_wall_clock_min` / `max_concurrent_cycles`) are enforced by `canStartCycle` (M5).
- **Iteration UI.** `ConceptStage.jsx` is now the chat for the whole life of the project: before approval it drives the Concept loop; after approval the composer stays enabled and a message runs the classifier (title flips to **Iterate**, polls `classifier_job`, renders the same `rule_question` choices). `POST /api/mock2/projects/:id/chat` branches on `design_approved_at` (`startConceptTurn` vs `startIteration`); `GET /chat` now returns `classifier_job`, `classifier_ready`, `iterating`, `summary_version`.

**Everything M0–M8 shipped stands unchanged** (parent domains + per-slug HTTP-01 TLS; project registry + container + bare-repo-over-Incus-mount + slug routing; archive/rehydrate; the per-project bridge + nftables `table inet mock2` + squid egress; connectors/slots/prices/quotas/git-connectors + framework registry with the vendored seed v1; the runner + checkout lock + hash-chained change records; the concept loop; the audit + rule questions + admin queue + drift). Read the earlier `NEXT-SESSION-PROMPT-mock2-phase-0N.md` files if you need a specific subsystem's shape.

## Read these first, in order (all in `docs/mock2/`)

- **`04-phased-plan.md`** — your phase is **M10** (the last section). Its scope: disabled/pinned-host audit (fresh install with pin → grep the process, FS, and route table for any Mock2 surface); container-escape review of the runner tool set; egress-proxy bypass attempts from a hostile container (direct IP, DNS-tunneling posture, proxy CONNECT to a non-allowlisted host); the change-record chain-verification tool (`GET /api/mock2/projects/:id/verify-chain`); restore-of-`mock2.db`-onto-a-pinned-host behavior; docs (operator runbook + the compliance-claim language from ADR-001's honesty note); load test (10 projects, 2 concurrent cycles, SQLite contention / WAL `busy_timeout` tuning).
- **`02-adrs.md`** — **ADR-001** (the four absence mechanisms + the **honesty note**: state the claim as *"on a disabled host, no Mock2 route, state file, credential store, or runner code path exists"* — the LXC exec socket cannot be literally unmounted because the backend already holds host-exec for its existing LXC management; M10's docs must use exactly this honest framing). **ADR-010** (the egress fence — the `table inet mock2` default-deny + the squid allow path; the R2 cross-table matrix is verified on a host, `scripts/mock2-m4-verify.sh`). **ADR-004** (the lock — force-release is audit-logged). **ADR-006/011** (the bare repo + the shared Incus disk-device mount — `shift=true`, planned `git fsck`, the read-only + orchestrator-fetch refinement stays available if a hostile runner becomes a real threat model — this is exactly the container-escape review's remit).
- **`03-data-model.md`** — the change-record hash chain (`change-logic.verifyChangeChain` already exists and is exposed inside `GET /projects/:id/change-records`; M10 formalizes the standalone `/verify-chain` tool), the derived-status section, and the in-repo mirror (`state/changes/<seq>.json`) so a repo-only verify is possible even if `mock2.db` is lost.
- **`05-risks-and-open-questions.md`** — **R4** (SQLite under a chat + cycle workload — the M10 load test; the escape hatch is a write queue-thread, not a DB migration), **R2** (per-project bridges × existing networking — the real test matrix on a host), **R3** (the backend may itself run inside Docker — every host op through `nsenter -t 1` / `mock2/host.js`; exercise both deployment shapes), **R8** (framework-content status — below), **R9** (write all tests stub-first).

## Scope of this session — Phase M10 only

1. **Disabled/pinned-host absence audit.** Fresh install with the pin present (and separately with `MOCK2_ENABLED=false`): grep the running process, the filesystem (`data/db/mock2.db` absent, no `/etc/caddy/mock2/`, no squid `mock2.conf`, no `table inet mock2`), and the route table (`/api/mock2/*` all 404). Script it (`scripts/mock2-m10-absence-verify.sh` or similar) so it's re-runnable. Confirm the module is never `import()`ed on a disabled host.
2. **Container-escape review of the runner tool set.** The runner's tools are `exec_in_container` / `read_file` / `write_file` / `run_gates`, all through `incus exec`/`incus file` against the fenced container via `host.js`; `read_file`/`write_file` reject absolute + `..` paths (`safeRel`). Review for: path escape, the shared bare-repo mount (`/srv/repo.git`, `shift=true`) corruption/escape, gate-script injection, base64-streamed content quoting. Tighten only if a real hole is found; otherwise document the boundary. Do NOT redesign M6.
3. **Egress-proxy bypass attempts** from a hostile container (ADR-010): direct-IP egress (must hit the bridge default-deny), DNS-tunneling posture, squid CONNECT/GET to a non-allowlisted host (must be refused). Add to / extend `scripts/mock2-m4-verify.sh`. Record results; the deny half must hold regardless of the CLI firewall's presence (R2).
4. **`GET /api/mock2/projects/:id/verify-chain`** (any member) — a standalone read-only endpoint returning `verifyProjectChain(projectId)` (already implemented in `change-records.js`). Small, but it's the compliance surface the runbook points at.
5. **Restore-onto-pinned-host behavior.** `mock2.db` sits in the DB dir the config-tier backup packs (`lib/backup-scope.js`); ADR-001 says restore onto a pinned host must **skip** it. Verify/implement the skip in the restore path and document it.
6. **Docs.** Operator runbook (the two host shapes from Q5 — a dedicated enabled dev/build server + a pinned-disabled production server, and the single-host home-lab shape that leans harder on M4) + the **honest compliance-claim language** from ADR-001. Land these under `docs/mock2/` or `docs/features/`.
7. **Load test.** 10 projects, 2 concurrent cycles, SQLite contention check; tune WAL `busy_timeout` if it cracks (R4). Record numbers.

## Do NOT implement / re-touch
The M9 classifier/summary/idle/status/quota internals, the M8 audit/queue internals, the M7 concept loop, the M6 runner/lock/change-record internals, the M5 connector/quota/framework CRUD, the M4 fence internals — beyond calling their read/data APIs or hardening a **proven** hole. **Absence (ADR-001):** M10 adds only one gated read-only route + scripts + docs; a `MOCK2_ENABLED=false` / pinned host must stay byte-for-byte unchanged.

## Hard constraints
Naming (R7 — runner/concept/audit/classifier, never `agent`). Absence (ADR-001). Migrations append-only (508+ free; nothing new is expected in M10). Docker/nsenter (R3 — exercise both shapes). Zod, `{ error }` shape, `requireAdmin`/`requireSudo`/`requireMock2Role`, `logAudit`, `acting_as_admin` stamped. Change-record hash chain must keep verifying. Tests stub-first (R9). Frontend (if any) passes `admin/frontend/MOBILE_FIRST.md`.

## What "done" looks like (the M10 verify checklist in `04-phased-plan.md`)
A fresh install with the pin → grep of process/FS/route-table shows **zero** Mock2 surface; the runner-tool escape review has a written verdict + any proven hole fixed; the egress-bypass matrix (direct IP / DNS / non-allowlisted CONNECT) is refused on a host; `GET /verify-chain` returns `{ ok, count, brokenAt }`; a `mock2.db` restore onto a pinned host is skipped; the runbook + honest compliance language are committed; the load test numbers are recorded. Plus `cd admin/backend && node --test 'src/__tests__/*.test.js'` (no NEW failures beyond the 4 known — `cves`, `incus`, `webauthn`, `vpn-mtu` in `docs/known-issues.md`) and `cd admin/frontend && npm run build`. A `MOCK2_ENABLED=false` host stays byte-for-byte unchanged.

Commit as `mock2-M10: <description>`. (M0–M9 merged to main; branch M10 off main, open its own PR.) This is the final phase — no NEXT-SESSION-PROMPT after it; instead close out the plan (mark M10 done in `04-phased-plan.md` if you touch it) and note in your PR that the Mock2 build is feature-complete pending the R8 runtime scaffold below.

## R8 status — framework CONTENT is real; only the runtime scaffold remains
Framework seed v1 is **real content** authored from the operator's *The Mock2 Framework* spec (v1.1), vendored under `admin/backend/src/mock2/framework-seed/`: a real constitution (stack / scaffold / auth / security / the four stages — this is what M8's audit measures a `framework_deviation` against, and what M9's classifier confirms rules under), the four stage skills (concept / define / build / review) as prompt templates, a real deterministic Tier-1 gate battery (typecheck, constitution-lint, rule-coverage, security-scan, test), and the locked design system (`design-system.md`). The gate scripts are **self-adapting** (skip green when the TS toolchain is absent, enforce for real when present). **The one piece still owed is the runtime scaffold** — the actual TypeScript/Express/Drizzle project code the container is seeded from is still ProxyPilot's placeholder (`mock2/template.js`; M7 extended its dev server to serve the Stage-1 mockup at `/_preview`, but the app itself is still the M2 placeholder). `project_template_ref` names the intended scaffold (`builtin:mock2-ts-express-drizzle-v1`) but the seed implementation is a **separate infra deliverable**; the mockup preview + generated apps run against the placeholder until it lands. Flag this in your PR summary. Content revisions publish a new framework version through the admin editor (append-only — never edit the seeded v1 row in place).
