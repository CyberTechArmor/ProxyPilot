# Run ledger

| Date | Branch | Size | Work | Steps | Wall clock | Outcome |
|---|---|---|---|---|---|---|
| 2026-09-19 | `claude/proxypilot-zfs-storage-l9szn1` | L | ZFS storage management: agent discovery, planner + plan/confirm tokens, Incus binding, sanoid/syncoid, REST + 27 MCP tools + Storage page, alerts, unit + loop-device tests, CI, docs | survey → pure layer → service/REST/MCP → units/scripts → tests → Go agent + Storage page (delegated, reviewed) → docs → handoff | one session | 49 storage unit tests + 19 extended-MCP tests green; full backend suite 2582/2594 with only the 5 known native-module files + 1 documented flake failing; loop-device cycle runs in CI (`storage-integration.yml`), not runnable in the sandbox (no zfs module); real-hardware and Incus paths listed in handoff |
| 2026-09-19 | `claude/proxypilot-zfs-storage-l9szn1` | L | Migration agent: adopt a server, VM or container onto an Incus guest — inventory manifest (never values), the approval gate, three transports (incus-migrate / rootfs-tar / file-sync), fenced import, cutover checklist, REST + 6 MCP tools + Migrations page, cross-built agent served with its hash | survey → pure layer → service → REST/MCP → Go agent → page → tests → live end-to-end on throwaway guests → 5 defects found and fixed → docs/handoff | one session | 28 backend + 11 Go cases green, CI green, full suite 2635/2647 (only the 5 known native-module files + 1 documented flake); BOTH modes proven end to end on the operator's host (whole-machine 300 MiB in 40 s, application mode files + pg dump, running apps and intact data in both targets); incus-migrate against a real VM, a real Proxmox source and arm64 remain unverified (handoff) |
| 2026-09-19 | `claude/proxypilot-zfs-storage-l9szn1` | M | Migration readiness + the third transport proven: `migration_preflight` / `enable_incus_listener` (REST + MCP + a Readiness panel), then `incus-migrate` end to end from the throwaway LXC source, with the answer script rewritten to answer by PROMPT | preflight/listener → tests → merge → deploy → live run → 3 defects (LEARNINGS 183/184/185), each fixed, merged, deployed and re-run → docs/handoff | one session, 4 deploys | 31 migration + 14 Go cases green, CI green, full suite 2638/2650 (only the 5 known native-module files + 1 documented flake); `incus-migrate` verified live: 434.7 MiB moved, guest fenced and stopped, migrated app serving its own vhost with all four rows; the Incus listener is now on at the bridge gateway (private, one line to reverse); a VM source, a real Proxmox source and arm64 still unverified |
| 2026-09-19 | `claude/proxypilot-zfs-storage-l9szn1` | M | Migration token lifecycle (no wall clock: ends with the migration or on revoke; every token listed with its state) + the Clean up button (delete the guest a finished migration created, the record, or both) | token.js/service/REST/MCP → migration 910 → page panels → tests → merge → deploy | one session, 1 deploy | 37 migration + 19 extended-MCP cases green, CI green, full suite 2644/2650 (the 5 known native-module files + 1 documented flake); deployed at 834fe42. The five throwaway guests were already gone — the operator cancelled the migrations in the dashboard and deleted them on the host before the button existed |
| 2026-09-20 | `claude/proxypilot-zfs-storage-l9szn1` | M | Where a migration lands and whether it fits: `pool` honoured by all three transports (incus-migrate's overrides menu answered 4 → pool → size → 1), a measured capacity check at inventory and approval (blocking when it will not fit, overridable), and `set_default_storage_pool` for which pool NEW guests land in | prompts read from incus-migrate 6.0.4's source → answer rules → capacity pure layer + pool `/resources` + staging df → service-side approval gate → planner for the default pool → UI (pool picker, capacity card, override, Make default) → tests → merge → deploy | one session, 1 deploy | 46 migration + 12 storage-planner + 19 extended-MCP cases green, CI green, full suite 2654/2660 (the 5 known native-module files + 1 documented flake); deployed at 4b1e67d and `migration_preflight` verified live on the host (both pools with real free space, `default` marked default, staging read). One defect found by a test while writing it: an unparseable `df` read as zero free rather than unknown |
